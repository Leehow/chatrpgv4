# 91: 地图：完成内置模组与新 PDF 的真桌及界面验收

Tracker synchronization: pending. GitHub reads began failing with EOF during the 2026-09-13 update, before any issue write or dependency change. This local ticket is the updated assignment body; the remote issue still has its previous text/blocking links. Status: ready-for-agent subject to the dependencies below. No owner has been assigned.

## Parent

https://github.com/Leehow/chatrpgv4/issues/85

## What to build

Integrate the scoped map slices and prove the full player outcome in a real built-in campaign and a separate fresh original-PDF campaign, with actual PipiCOC viewing evidence. Repair only map-related systemic failures revealed by these checks and retain every unsuccessful attempt. This ticket is the feature acceptance gate, not a substitute for each earlier slice's own tests.

Scope: parent user stories 1, 2, 3, 7, 13, 14, 16, 21, 22, 23, 24, 28, 33, 35. All parent invariants apply. The production kernel is TypeScript; source reading uses the current tool-enabled Pi reader. This is the remaining work against an existing implementation; status and evidence below supersede the original planning-only wording.

## Status and retained evidence — 2026-09-13

Partial evidence only. Built-in bounded App proof exists; the original-PDF map and advanced disclosure/history gates are not accepted. Parent #85 remains open.

- [x] Built-in partial-map App evidence and prior kernel/extension/Electron results are retained.
- [x] Historical tests include baseline-aware Electron validation and focused source, map and recovery checks; these are not fresh suite runs for this issue update.

## Remaining work for the next owner

- [ ] Reconcile all 35 parent stories and every child checklist with current implementation and evidence; do not rely on the obsolete 'implementation complete' header.
- [ ] Integrate accepted #86/#87/#88/#89/#90 changes, preserving unrelated concurrent work and all failed attempts.
- [ ] Run the canonical real Keeper/main-session-player method from setup/opening to an ending or genuine blocker. Preserve exact model/lane provenance; do not relabel earlier non-Grok or bounded runs as full canonical acceptance.
- [ ] Demonstrate built-in and fresh-PDF map need, partial reveal, another knowledge gain, continued concealment, a secret area absent from the investigator map, revisit and restart.
- [ ] Capture actual App interaction and public pixel/payload evidence; verify no model calls when reopening existing cards.
- [ ] Run relevant final suites with actual exit codes; no concurrent pytest, new failure-baseline entries or historical Python oracle edits.
- [ ] Bind source revision, tested build, installed package and running App identity. Verify stable signing and canonical LaunchServices/Spotlight registration; the latest fast repack's registration audit is not recorded.
- [ ] Record preparation/display/reopen latency separately from Codex polling and idle time. A transport-blocked or oversized inherited draft is not successful fresh-PDF acceptance.
- [ ] Deliver the final evidence matrix and remaining limitations before closing any acceptance requirement.

## Working boundary

Continue the current TypeScript kernel, host, ModuleStore/ModuleGraph and seven Keeper verbs. Preserve all saves, failed reading artifacts and source assets. Use original page images and tool-enabled Pi readers; no Docling, Marker, MinerU/PaddleOCR, production Python, model-name heuristics, manual acceptance graph edits or new map database/scheduler. Source and safety contract changes precede implementation. The user will assign owners: this update launches no worker and grants no unrelated push, cleanup or provider refactor. Unchecked original criteria mean not fully reconciled, not necessarily unimplemented.

## Acceptance criteria (original requirements retained)

- [ ] Review the integrated implementation against all parent user stories and the canonical map contract; verify producer, projection and real Keeper/player use for map discovery, knowledge updates and image display.
- [ ] Run relevant kernel, extension and Electron regression suites using their required commands, preserving actual exit codes and the existing UI failure baseline. Do not run concurrent pytest suites or edit historical oracle results/baselines to hide regressions.
- [ ] Use a built-in The Haunting campaign with real map bytes and a separate fresh original-PDF campaign that exercises production discovery/review/publication. The supplied Rulebook's scenario can serve as the fresh source without adding another book dependency.
- [ ] Use the canonical RPC setup/play driver, Grok as Keeper and the main session as the sole player, one natural utterance at a time, from setup/opening to a natural ending or real blocker. No fake Keeper, scripted/batch player, forged map records or scenario-building shortcut counts as play.
- [ ] Observe genuine map need, a partial reveal, another meaningful knowledge gain, continued concealment, a newly discovered source-backed region absent from the initial player map, and reopening after leaving the area. Cover a later-demand PDF map and state which sample demonstrates each item.
- [ ] Capture actual PipiCOC conversation images and interactions, including zoom/pan, known-only selection and reopening without a model call. Inspect emitted data and bytes as well as screenshots for undisclosed content.
- [ ] Verify resume, a second campaign, historic cards and existing worldline/source-version behavior using retained state and scoped supporting interface checks. Resume an existing campaign through session.resume before any other campaign operation.
- [ ] Before any App build, package or GUI launch, follow the repository's macOS App hygiene policy and verify the exact canonical runtime used. Record source/test/real-table/UI/packaged evidence separately; do not claim a packaged gate that was not run.
- [ ] Record cold preparation, subsequent reveal/display and cached reopening times. Treat failed or untested cases explicitly; no fixed turn count, fabricated throughput metric or test total substitutes for the user outcome.
- [x] Retain all campaigns, transcripts, receipts, telemetry, original sources, derivatives and failed runs. Do not push, distribute assets, deploy, clean another task's worktree or close the parent spec as an incidental action.

## Blocked by

- https://github.com/Leehow/chatrpgv4/issues/86
- https://github.com/Leehow/chatrpgv4/issues/89
- https://github.com/Leehow/chatrpgv4/issues/90

#89 carries #87 and #88 transitively. #86's residual baseline evidence audit is now an explicit final prerequisite.
