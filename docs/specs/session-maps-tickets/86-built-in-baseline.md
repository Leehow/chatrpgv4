# 86: 地图：内置模组的首个已知区域在会话中安全显示

Tracker synchronization: pending. GitHub reads began failing with EOF during the 2026-09-13 update, before any issue write or dependency change. This local ticket is the updated assignment body; the remote issue still has its previous text/blocking links. Status: ready-for-agent subject to the dependencies below. No owner has been assigned.

## Parent

https://github.com/Leehow/chatrpgv4/issues/85

## What to build

Complete the smallest real path from a built-in map through a Keeper-authorized knowledge change to an actual image card in the conversation. Use The Haunting and a matching locally available Rulebook source to prepare missing map bytes through the existing host facilities. One established region is enough for this slice, but secrecy, transactional correctness, semantic identities and an actual display are required from the start. The map must be useful without developer file access or scenario-specific renderer code.

Scope: parent user stories 1, 3, 7, 15, 17, 24, 25, 26, 30, 31, 33. All parent invariants apply. The production kernel is TypeScript; source reading uses the current tool-enabled Pi reader. This is the remaining work against an existing implementation; status and evidence below supersede the original planning-only wording.

## Status and retained evidence — 2026-09-13

Implemented baseline with bounded live App evidence; remaining work is acceptance reconciliation, not a new map implementation. Keep open until the unchecked original criteria below have evidence.

- [x] 9bc885f7 supplies canonical map knowledge, host-only flattened images and conversation cards.
- [x] Retained The Haunting App run revealed only ground-entry-hall; the card opened, zoomed from 100% to 200%, survived restart and reopened without a model call.
- [x] Atomic bad-region rollback and separate-campaign checks exist. This is bounded feature evidence, not a completed campaign.

## Remaining work for the next owner

- [ ] Attach the retained source, RPC, pixel and App evidence to the original criteria; distinguish tested revision and actual Keeper/admission models.
- [ ] Fill only missing exact replay/interrupted-batch, cross-campaign access and unavailable-source verification. An absent visual update must not expose the base image or undo an independently committed game action.
- [ ] Audit local preparation of missing built-in bytes without redistributing the private Rulebook or adding an absolute developer path.
- [ ] Hand final baseline evidence to #91. Do not reimplement the existing map path.

## Working boundary

Continue the current TypeScript kernel, host, ModuleStore/ModuleGraph and seven Keeper verbs. Preserve all saves, failed reading artifacts and source assets. Use original page images and tool-enabled Pi readers; no Docling, Marker, MinerU/PaddleOCR, production Python, model-name heuristics, manual acceptance graph edits or new map database/scheduler. Source and safety contract changes precede implementation. The user will assign owners: this update launches no worker and grants no unrelated push, cleanup or provider refactor. Unchecked original criteria mean not fully reconciled, not necessarily unimplemented.

## Acceptance criteria (original requirements retained)

- [x] Record the shared map/source-region representation, public read projection, apply knowledge change, receipt, view identity, asset access and failure behavior in the canonical kernel RPC contract before implementation. This contract supports the later PDF, multi-map and history slices without parallel shapes.
- [x] Use existing ModuleGraph asset/handout nodes and source references; supply real verified built-in map bytes from the available local source. A registered path whose image is absent is not an available map. Do not hard-code a developer's source path or package private source bytes.
- [x] The Keeper can discover the map by semantic name, identify one permitted region, establish its disclosure through existing apply/admission and transaction machinery, and place its actual image in the conversation. Preserve the seven verbs and the production TypeScript-only kernel.
- [ ] Return only a flattened, authorized derivative and public metadata through a narrowly scoped existing host asset channel. Other regions, private annotations, original image URLs, source PDF bytes and private region geometry are absent from player-facing results and thumbnails.
- [ ] Opening the displayed image is read-only: no movement, clock, RNG, new knowledge, clue acquisition or physical-handout acquisition is implied. Ordinary text/image handout behavior remains compatible.
- [ ] Initial knowledge persists canonically; rejected batches roll back; replaying the same mutation does not duplicate disclosure; a fresh campaign cannot receive another campaign's derivative. Basic restart and source/view identity are not postponed to the continuity ticket.
- [ ] An absent or unreviewed image gives an honest unavailable/preparing state and cannot fall back to the full source. A separately completed game action is not undone merely because its visual reference cannot render.
- [ ] Verify through the existing TypeScript RPC, host projection/asset and conversation-renderer seams, inspect actual permitted pixels, and demonstrate the narrow behavior with the canonical real Keeper/player method plus actual PipiCOC image-display evidence. Label bounded evidence honestly.

## Blocked by

None (can start immediately once file ownership is clear).

The implementation dependency formerly blocking #87, #88 and #90 is already satisfied. This issue's remaining evidence audit now gates #91 instead.
