Status: ready-for-human
Stage: SL-28 (product path; independent of the loop tickets)
Spec: docs/specs/pi-native-single-loop.md (Ruling: "A built-in starter reads the same book a PDF module does")

# SL-28 — The built-in starter is bound to its book through the PDF path

## Evidence
- `content/starters/the-haunting/module-graph.json`: `source_refs` cite `pdf:call-of-cthulhu-keeper-rulebook-40th-the-haunting` indices 446–462 and a `source-document` node describes "the visually reviewed source window at PDF indices 446 through 462".
- Long live gate (campaign longgate-haunting-0624): five `lookup kind=source` calls answered `no_source_document`; SL-27 measured 5 of 18 looks were book lookups on this starter; the carried head now says the module has no document.
- The owner's copy: `/Users/haoli/Documents/TRPG/coc英文/Call Of Cthulhu Keeper Rulebook 40th Anniversary (Sandy Petersen).pdf` (465 pages, 51 MB).

## Scope
1. Read §14.2 (PDF material packs and `module.bind`), §20 (from PDF to playable), §22 (visual reading, demand-driven graph), §61, §90.5, and how an imported module's source store, reading lane and `lookup kind=source` are wired (kernel-ts reading/source modules; extensions/table/prescreen.ts `sourceCandidates`; the App's PDF import path). Establish what a PDF module has that the starter lacks (source store, page images/text, reading index) and where the campaign's `reading` capsule section comes from.
2. Contract first (a §14.2/§20 amendment or a new subsection): a starter with a source window declares it (`source_binding: {source_id, pdf_sha256?, pages: [446, 462]}`); registering the book (the same registration an import uses, pointed at the owner's PDF) binds the starter's campaigns to that window only; the reading lane, prescreen source candidates and `lookup kind=source` then behave as for an imported module; a campaign created without the book behaves as today and says so; the repository ships no page text or images.
3. Implement: the binding, the page window extraction into the source store (reuse the import pipeline's extraction for indices 446–462 only), the capsule's `reading` section for the starter, SL-27's carried head reading the truth. Tests through the real entry (a small fixture PDF of a few pages stands in for the rulebook in the suite; the owner's PDF is used only in a manual check you run and report).
4. Manual check on this machine: register the owner's rulebook, create a fresh haunting campaign, open a driver table, and show `lookup kind=source` answering with the Haunting's pages and the prescreen locating source cards; report bytes and timings. Do not run the table until told "go" (the Mac is busy with a live gate).

Owner amendment, 2026-09-24 (spec ruling): the Haunting's window ships built in, extracted once with the import pipeline's extractor into the starter package in the store format an imported module gets; no registration step; the binding stays generic (built-in window or registered book). The manual check becomes: a fresh haunting campaign with no registration, a driver table, `lookup kind=source` answering with the pages, the prescreen locating source cards, the carried head stating the module's source.

## Comments

### 2026-09-24 — implemented (branch `claude/sl28-20260924`; contract §14.16)

**What a PDF module had that the starter lacked.** `kernel-ts/modules/reading.ts` `bind` (≈ lines 100–160 before this change) gives an import `source.pdf` in its module directory and `module.json` `reading_version: 1`, `source_document {path, file_sha256, page_count}`, a reading state, a queue and `sections.json`; `Reading.source` (then ≈ line 160) refused any module without `source_document` and with `source != "pdf"` as `no_source_document`; `ensureIndexJob` (then line 66) queued an index only for `source == "pdf"`; `module.source.snapshot` / `module.source.materials.snapshot` (`kernel-ts/modules/index.ts` ≈ line 27, `reading.ts` `materialSnapshot`) refuse `source_unavailable` without it, which is what the prescreen's `sourceFailure` swallowed (`extensions/table/prescreen.ts` ≈ line 383); the capsule's `reading` section exists only under `reading_version` (`kernel-ts/read/assemble.ts` ≈ line 444), and SL-27's carried head reads its document sentence from that (`runtime/jev/hybrid-engine.ts` line 318). `registerStarter` (`kernel-ts/write/source.ts`) wrote none of it; the Haunting's graph named its source (`properties.source_binding.file_sha256` = the owner's book, `source-document-…` refs 446–462) but nothing read that.

**Contract.** §14.16 (seven parts) plus notes in §14.2, §20 and §127.2: the declaration `content/starters/<id>/source-binding.json` (`book {file_sha256, pages}`, optional `built_in`), beside the graph because bundled guidance pins the graph's digest; binding at registration (built in) or via `module.source.bind` + `window` (registered; the whole book refuses `source_window_required` and never becomes `book-N`); window coordinates; what changes and what does not (`playsFromReading` split); campaigns; the three ends.

**Where each piece lives.** Kernel: `kernel-ts/modules/bound-source.ts` (declaration, `bindStarterSource`, `ensureStarterSource`, the authored index, `playsFromReading`); `reading.ts` (`bindNamedBook`, `bindWindow`, the declared-window refusal, index-job condition, title/language guard); `write/source.ts` (registration binds after its own metadata writes, under the metadata lock); `modules/index.ts` (window in snapshots, starter status shape); `write/index.ts`, `setup/index.ts`, `apply/index.ts`, `resolve/index.ts`, `read/campaign.ts` (readiness, gates and read-ahead now keyed on `playsFromReading`). Host: `extensions/module/source.ts` `sourceWindow` (PDF.js `extractPages`, labels kept), `runtime/{host,tasks,source-worker}.ts` `sourceWindow`, `extensions/module/source-registration.ts` `registerSourcePdf` used by `reading-service.ts` `prepare` and `pipicoc/onboarding-worker.ts` `inspect`; `runtime/jev/prescreen-source-provider.ts` reads authored book references through the window. Content: `content/starters/the-haunting/{source.pdf,source-binding.json,README.md}`; `scripts/build-starter-source.ts` made the window (1.5 s, 4,986,143 bytes, 17 pages, labels 435–451, sha256 `31e36f72…c7db`).

**Decisions recorded in §14.16.5.** A bound starter's index is derived by machine from the authored scenes' own `source_refs` (12 rows, `index_complete: true`), so no campaign spends a reader re-indexing pages the graph covers; the capsule's 512-byte `reading` budget empties page lists before rows (existing behaviour), so the Keeper sees 8 scene names with `read: true`. Readiness, material gates, way-on repair, read-ahead and `module_reading` stay a PDF book's (`playsFromReading`): binding the document does not make the Haunting wait for a reader or fork every campaign for prefetch.

**Tests.** `tests/extension/starter-source-binding.test.mjs` (7, real kernel runtime + host PDF.js; the shipped window, the prescreen's cited-page translation, voice-bench without a declaration, a fixture book the suite writes through registered and built-in modes, window/page-count/declaration refusals, carry across a new generation, a campaign fork). Updated: `tests/kernel/test_capsule.py` (reading section; the no-document refusal moves to voice-bench), `test_capsule_budgets.py`, `test_capsule_nine.py`, `test_module_bind.py`, `tests/extension/starter-lookup.test.mjs` (voice-bench), `single-loop-looks-first-visit.test.mjs` (the head now says the Haunting has a document), `material-identity.test.mjs` (its visual-book fixture is marked `source: pdf` and sourceless). Mutations, each killed: prescreen ignores the window (1 fail), no `source_window_required` refusal (2), registered window not carried to a new generation (1), shipped-bytes check removed (1), index takes any source id (1), index takes pages outside the window (1), page-count check removed (1), window declaration check removed (1), `playsFromReading` ignores `source` (ts-kernel-write 9 fails). Not covered by a test: the index publication's title/language guard for starters (no index job runs on a bound starter).

**Manual check (driver table, fresh campaign `sl28-haunting-0933`, no registration; hybrid-v1, `PI_COC_JEV_PRESELECT=1`, grok-4.7-build-fast low; Mac shared with two other workers' PDF builds).** `campaign.create` with the first registration and the 5 MB copy: 0.63 s wall (process start included). Turn 1 (129.6 s wall): the Keeper called `lookup kind=source source_mode=answer` on Knott's briefing; answered in 54.2 s from window pages 2 and 16 (printed 436 and 450, Handout 1), `source_refs` `pdf:the-haunting` 1 and 15, status `conflict`: the book does not have Knott say the Macario father is at Roxbury Sanitarium, which the authored graph does. Evidence: `.coc/module-campaigns/sl28-haunting-0933/modules/the-haunting/work/read-1/attempt-1` (observations `read_pages [2,16]`, `review_pages [2,16]`), 8 cached page images 2.8 MB, work 3.2 MB; the capsule carried `reading {index_complete: true, sections: [...]}`. The table's prescreen did not run: `prescreen {status: not_run, reason: no_jev}` (no `EXT_JEV_APIKEY` in this session's environment; the key was not sought elsewhere), so the carried head's document sentence did not appear on the table (it appears only with carried passages; the extension-seam test shows it for the Haunting). The prescreen's source step (`preparePrescreenSources`, no model) run against the same campaign: snapshot 11 ms, 496 ms, 53 candidates (1 `reviewed_source` = the table's checked answer, 52 `native_text`), searched pages 1–17, 16 pages materialized, the query "Corbitt" located units on pages 7, 9, 12, 13, 15. To repeat with the prescreen live: `EXT_JEV_APIKEY=… PI_COC_LOOP_ENGINE=hybrid-v1 PI_COC_JEV_PRESELECT=1 uv run --frozen python tests/play/driver.py start --campaign <fresh id>`.


**Suites (leehow-pc).** Baseline on `762d639e1`: ext 2941/2941, loop 149/149, pytest 1719 passed / 2 skipped. On the branch after merging the integration branch at `9ab3e753f` (head `52ce2af54`): ext 2957/2957, loop 152/152, pytest 1719 passed / 2 skipped, all exit 0. (An intermediate ext run on `3842952a0` had three timing failures under box load, `post: a review that could not answer…`, the §135.31.1 move test and the #67 header-timeout test; none recurred.)
