Status: ready-for-human (implemented 2026-09-26)
Stage: SL-82 (P1 while SL-74 is live; the flag's step-1 call and SL-69's per-call cap)
Spec: docs/kernel-rpc.md §135.29 addendum (SL-69: cap = max(floor, turn budget/2), `PI_COC_KEEPER_CALL_CAP_FLOOR_MS`), §38.7.1 (SL-74 `first_step_thinking`, `step`), `runtime/jev/hybrid-engine.ts` `keeperCallCapMs`, `extensions/kernel/first-step-thinking.ts`

# SL-82 — With first-step thinking on, the step-1 call needs its own cap: gate #14 stranded 8/20 turns because the 22.5 s cap killed every thinking call

## Evidence (long gate #14 `longgate14-haunting-0509`, run events + campaign telemetry)
- deepseek-v4.1-flash with thinking ≈ 35 s a call (gate #9). Cap = max(20,000, 45,000/2) = 22,500 ms. Every stranded turn (t9/10/12/13/17/18/19/20): four `200` responses ≈25 s apart, error stops "Keeper call timed out: exceeded its per-call cap of 22500 ms (phase: streaming)" ×11 and "… a second time" ×35, then `ask_llm unavailable` → `model_unavailable:no_delivered_evidence`. Delivered turns were the ones whose thinking call finished under the cap. `first_step_thinking: true` on 51 of 93 requests because every retry of the killed call is again step 1.
- Gate #15 (same build, `PI_COC_KEEPER_CALL_CAP_FLOOR_MS=60000`): no cap stops on turns 1–2, both delivered (75 s / 65 s).

## Ruling (filed for the owner)
The flag owns its cost: when `COC_FIRST_STEP_THINKING` is on, the step-1 call's cap is max(the ordinary cap, a thinking-call allowance) — a named default in `content/rulesets/coc7/host-budgets.json` (`first_step_thinking.call_cap_ms`, from the measured ≈35 s) — and steps ≥ 2 keep the ordinary cap. The `keeper_call_cap` telemetry row carries `step` and the cap used. Nothing changes when the flag is off. If the flag ever becomes a setting, the run's time budget (§135.25) must add the same allowance so the compose step is not squeezed.

## Scope
- `runtime/jev/hybrid-engine.ts` (`keeperCallCapMs` → per-call by step, wired through `runtime/pi-hybrid.ts`'s `keeperCallCapMs`/`watchCallCap`), `extensions/kernel/first-step-thinking.ts`, the data file; contract §135.29 addendum 2.
- Tests (mutation-killable): flag on → step-1 cap = allowance, step-2 cap = ordinary; flag off → unchanged; the telemetry row carries `step`; the allowance is read from data (mutate the file → cap changes).

## Comments

- 2026-09-26 (worker): implemented as designed. Contract §135.29 addendum 2 written first
  (`docs/kernel-rpc.md`, commit `9ee158626`). Data: `content/rulesets/coc7/host-budgets.json`'s
  `first_step_thinking.call_cap_ms: 60000`, read by a new `firstStepThinkingBudget()` loader in
  `runtime/jev/host-budgets.ts` (mirrors SL-76's `jevStepsBudget`; commit `ee517e678`). Pure decision:
  `firstStepCallCapMs(flagOn, step, ordinaryCapMs, allowanceMs)` in
  `extensions/kernel/first-step-thinking.ts`, beside `disableStepThinking`/`isFirstStepOfTurn` (commit
  `9f60ffdff`). Vendored seam (patch `0004`, commit `949ef4d73`): `CreateAgentSessionOptions`/
  `MainOptions.keeperCallCapMs` now accepts `number | (() => number | Promise<number>)`; `sdk.ts`'s
  `streamFn` resolves it fresh immediately before every attempt instead of once at construction. The
  patch file's diff was regenerated by reconstructing the "0003 applied" baseline (reverse-applying the
  prior 0004 hunks) and confirming `git apply -p1` of the new patch reproduces the current vendor tree
  byte for byte; `npm run build:runtime` rerun so the built package's `patchSeriesDigest` matches
  (`tests/extension/vendored-pi.test.mjs` green). Wiring (`runtime/jev/hybrid-engine.ts`, commit
  `eb3ce1732`): the engine keeps its own `turn_start`/`before_agent_start` step counter (same
  reset/increment contract as the kernel extension's `table.roundTrips`, tracked independently so this
  engine never reaches into the kernel extension's private state); with the flag on, the exposed
  `keeperCallCapMs` becomes a function resolving `firstStepCallCapMs(true, step, ordinary, allowance)`
  fresh per call; with the flag off it returns the plain ordinary number exactly as before this ticket,
  byte for byte. The `keeper_call_cap` telemetry row now carries `step` unconditionally.

  Tests (commit `9fede13bd`), all passing (`node --test` on each file below):
  - `tests/extension/first-step-thinking.test.mjs`: `firstStepCallCapMs` at the flag on/off and step
    0/1/2/3 boundary; `firstStepThinkingEnabled`'s exact-string match.
  - `tests/extension/first-step-thinking-host-budget.test.mjs` (new): `firstStepThinkingBudget` reads
    `call_cap_ms` from the data file, mutating the fixture changes the resolved value, falls back on an
    out-of-range/missing/unreadable file, and the shipped file matches its own documented default.
  - `tests/extension/keeper-call-cap-first-step.test.mjs` (new): `createHybridEngine`'s own step
    counter driven against a stub extension host proves flag-off returns a plain `number` (not a
    function) and flag-on returns a function sized allowance-on-step-1/ordinary-on-step-2+, resetting
    on new player input, plus the `roundTrips===0` defensive boundary; a real-socket integration test
    (silent provider, two forced attempts) proves the vendored seam resolves a *function* value fresh
    on every attempt — a counter-backed cap answering 1200 ms then 2000 ms lands exactly that way on
    the two provider requests — and that the `keeper_call_cap` row carries `step`.

  Mutation evidence (copy-edit-run-restore, never `git checkout --`): (1) `Math.max`→`Math.min` in
  `firstStepCallCapMs` — killed by `first-step-thinking.test.mjs`. (2) removing the `step += 1` on
  `turn_start` in `hybrid-engine.ts` — killed by `keeper-call-cap-first-step.test.mjs` (step 2 read back
  the allowance instead of the ordinary cap). (3) caching the resolved cap at first use instead of
  resolving fresh per attempt in vendored `sdk.ts` — killed by the real-socket integration test in the
  same file (`keeperCallCapMs was invoked once per attempt, not once and cached: 1 !== 2`), then
  rebuilt and reconfirmed clean after restoring from the scratch copy.

  Full suite runs (`test:ext`/`pytest`) were intentionally not run per the task's scope; only the named
  files above and their siblings (`keeper-call-cap.test.mjs`, `keeper-call-cap-integration.test.mjs`,
  `vendored-pi.test.mjs`) were exercised, all green. `npm run build:runtime` was run because `build/`
  was missing at the start of this work (permitted) and again after each vendor-source edit/mutation
  restore. No `single-loop-*` file references the cap or the flag, so none needed touching for this
  ticket beyond the sanity run of `single-loop-run-driver.test.mjs`/`single-loop-turn-budget.test.mjs`/
  `single-loop-turn-close.test.mjs` (unaffected, all green).

  Nothing from the ticket's scope was left undone. One thing worth a human's eye: the ruling's own
  closing sentence ("If the flag ever becomes a setting, the run's time budget (§135.25) must add the
  same allowance") is recorded in the contract addendum but deliberately not implemented — SL-74 is
  still an experiment (env flag only, no settings key), so there is no settings path yet for that
  clause to attach to.
