Status: ready-for-human (implemented 2026-09-26, worktree `chatrpgv4-wt-sl78`; opened 2026-09-26 by the owner for `clue_follow_up` only: "线索跟进先放权，NPC 反应继续影子"; batch 14)
Stage: SL-78 (P1, Stage 2b of `docs/specs/jev-driven-steps.md`: cleared consequence candidates execute; residual telemetry)
Spec: docs/specs/jev-driven-steps.md D3/D4/D6; §135.32; §135.8 (clerk did), §135.28 (rules-default line), §32.12 (admission on compile evidence)

# SL-78 — `COC_JEV_STEPS=on`: consequence candidates run as clerk steps; the loop re-routes after each; the Keeper's residual is measured

## Scope
1. Execute path for the three classes through the one operation gateway; admission on compile evidence; a fell step returns to the Keeper as today.
2. Re-route after an executed D1 step (a cleared clue or reaction may issue a `time_cost`; a move issues the next scene's classes) under the existing anti-loop gates and `maxSteps`.
3. Projection: the executed steps under "clerk did" with the rules-default line; the Keeper reconciles or reverses (no silent undo).
4. Residual telemetry per turn `{lane:"residual", keeper_calls:{apply,resolve,look,lookup,recall}, compile_calls, clerk_calls}`; the triage script reports it.
5. Tests: execute only when `on`; re-route once per executed step; the projection carries the step; residual row shape; an outage mid-run leaves the run deliverable.
6. Gate #15 pre-registered on D6 2b before launch.

## Comments

### Worker report (2026-09-26, worktree `chatrpgv4-wt-sl78`, branch `claude/sl78-20260926`)

Implemented per the owner's per-class ruling below. Commits (on top of `efe667213`):
`2f477c112` (contract addendum 2), `1b4e1ec92` (host-budgets.json/.ts data), `880d68bfb`
(the execute path, the re-route, the residual row, and their tests), `de5fb3210`
(jev-steps-report.py's residual section). Summary:

1. **Data.** `content/rulesets/coc7/host-budgets.json`'s `jev_steps` gains `"execute": ["clue_follow_up"]`. `runtime/jev/host-budgets.ts`'s `JevStepsBudget`/`jevStepsBudget()` read and validate it (an array of strings; a non-string entry is dropped; a non-array value or a missing key falls back to `[]`, never a guess).
2. **Execute path.** `runtime/jev/hybrid-engine.ts`'s `consequenceKeysToExecute` takes a fourth argument, the execute-classes list, and only returns keys of a listed class (`shadow`/`off` unchanged: still `[]` regardless). A new `routeConsequencesAfterWrite` runs the *listed-classes-only* route right after every settled clerk write (`clerkStep`'s own tail, `ok` only) and every settled Keeper write (`modelStep`'s `WRITE_VERBS` branch) -- the point in the run a clue can still be filed and narrated, strictly before the next step, which for a settled declaration is the compose. It reuses the same fresh read `freshOf` already took (`freshOf` now also exposes `consequences()`, added with no change to what `shadow`/`off` do with that object). The unfiltered candidate list still rides on `run.consequenceCandidates`/`consequenceContext` for `turnCloseStep`'s own unconditional call, unchanged mechanism.
3. **Re-route.** Because `clerkStep`'s own tail calls `routeConsequencesAfterWrite` again after its own dispatch succeeds, an executed consequence step re-routes on the spot; termination is `run.consequenceExecuted` (a key never offered twice) plus a finite scene, with `CONSEQUENCE_INLINE_ROUNDS_CAP` (12) as a circuit breaker never hit on a real table. No new anti-loop gate was needed: the existing per-key dedupe, `maxSteps` and §135.25's run budget already bound it, since the whole chain runs inside one host step's own async body, never as an extra driven-run step.
4. **Projection.** No new mechanism needed: an executed D1 candidate already flows through `clerkStep`, which already appends to `run.clerkDid` (the "clerk did" note, §135.8) with `defaultLine`'s rules-default line (§135.28) -- this was true before SL-78 touched anything, since SL-76's `clerkStep` call for a cleared `on` candidate used the same function.
5. **Residual row.** `recordResidual` writes `{lane:"residual", turn, keeper_calls:{apply,resolve,look,lookup,recall}, compile_calls, clerk_calls, consequence_calls}` once per turn at turn close, `COC_JEV_STEPS=on` only. `keeper_calls` is `modelStep`'s own new per-verb counter (every attempt, refused or not); `compile_calls` is `decide()`'s new counter on `purpose === "compile"`; `clerk_calls`/`consequence_calls` need no new counters -- they read `run.clerkDid.length` and its `clerk === "consequence_bookkeeping"` subset, which already existed. Shape matches the brief I was given (`turn` and `consequence_calls` beyond this ticket's own Scope §4 wording, which the brief added on top of D4's; both are natural completions -- `turn` for per-turn grouping in the report, `consequence_calls` to separate SL-78's own executions from the compile-selected ones inside `clerk_calls`).
6. **Contract.** §135.32 gained "addendum 2" (written before the code, per project convention): the execution point, the re-route, the projection line, the residual row, three ends, and the test list.
7. **Report script.** `tests/play/jev-steps-report.py` gains `residual_section`: per-turn table plus median/mean/max per counter, and an explicit "no residual rows" message when a campaign ran `shadow`/`off`.
8. **Tests** (`node --test`, no live Jev, no live model): `tests/extension/consequence-shadow-gate.test.mjs` (pure `consequenceKeysToExecute`, extended for the class filter -- an unlisted cleared class never executes under `on`, an empty/omitted list executes nothing); `tests/extension/consequence-host-budgets.test.mjs` (the data file's `execute` field: read, filtered, falls back, and the shipped file's own `["clue_follow_up"]`); new `tests/extension/consequence-execute-mode.test.mjs` (engine seam, stub `DecisionPort` + stub operation-dispatcher gateway, no fake-kernel subprocess, same pattern `single-loop-settlement.test.mjs`'s "at the engine" tests use): a cleared `clue_follow_up` executes inline (before turn close) and produces a receipt; `shadow` executes nothing and asks Jev about consequences only once, at turn close (byte for byte); an `npc_reaction` candidate never executes, inline or at turn close, even though it clears; the re-route fires for a clue the kernel only offers once an earlier one is filed, and stops on its own; two outage shapes (the decision port throws; the gateway throws on the second dispatch) leave the declared write's own result unaffected; the residual row's shape and counts after a mixed turn; `shadow`/`off` write no residual row at all; the execute list is read from the shipped data file, not a literal.
9. **Mutation evidence** (scratch-copy method: `cp` to a scratch path, edit, run, `cp` back -- never `git checkout --`): (a) removing the `executeClasses.includes(row.class)` filter from `consequenceKeysToExecute` -- caught by `consequence-shadow-gate.test.mjs`'s new test and by `consequence-execute-mode.test.mjs`'s npc_reaction test (which drives both the inline point and turn close); (b) removing the `mode !== 'on'` guard from `routeConsequencesAfterWrite` -- caught by the `shadow` test's new assertion on the count of consequence-family Jev calls (the dispatch count alone did not catch it, because `consequenceKeysToExecute`'s own inner gate is a second line of defense; the extra early Jev call under `shadow` is still wrong and is what the added assertion catches); (c) removing the recursive `routeConsequencesAfterWrite` call from `clerkStep`'s tail entirely -- caught by three tests (the inline-executes test, the re-route test, the data-file test); (d) zeroing `clerk_calls`/`consequence_calls` in the residual row -- caught by the residual test.
10. **Test runs.** `node --test` on `consequence-*.test.mjs`, `single-loop-*.test.mjs` and `jev-*.test.mjs` (58 files) together: full pass, no regressions from this change (results recorded in the commit message / session log). `npm run build:runtime` was needed once (the worktree's `build/` was missing; the first attempt hit a pre-existing `ENOTEMPTY` on a read-only `build/node_modules/@earendil-works/pi-coding-agent`, fixed with `chmod -R u+w build` before retrying -- not this ticket's own defect, just a stale partial build in this worktree).

**Could not do / left for a human:** did not run the full `test:ext`/`pytest` suites (out of scope per instructions -- the lead runs those after merge). Did not attempt a live-Jev/real-table playtest (gate #17's own pre-registration, out of scope for this worker: no live Jev/model calls were used anywhere in this work). `npc_reaction`/`time_cost` remain shadow-only, per the ruling below; SL-79 (narrator-only Keeper) is not started.

Status: **ready-for-human**.

## Ruling (owner, 2026-09-26, after SL-77 on gates #14/#15/#16)
Execute mode is **per class**. `clue_follow_up` met D6 2a on three tables (agreement 6/6, 6/6, false positives 0/0/1) and executes; `npc_reaction` stays shadow (corrected agreement 0.50–0.60; the remaining misses are the Keeper not staging a first impression when engaged, SL-83 pending) until a later report meets the bar; `time_cost` never issued a candidate on the starter and stays shadow. The switch is data: `content/rulesets/coc7/host-budgets.json` `jev_steps.execute: ["clue_follow_up"]` (the env `COC_JEV_STEPS=on` enables execution for the listed classes; unlisted classes are routed and paired in shadow exactly as today). Everything else in Scope stands: the one gateway, admission on compile evidence, re-route after an executed step, the "clerk did" projection line, the `residual` row, and gate #17's pre-registration on D6 2b for this class (Keeper-chosen `apply clue` on handles the clerk already filed = 0; no clue the player did not reach; every executed clue appears in the prose or is reversed with a receipt).
