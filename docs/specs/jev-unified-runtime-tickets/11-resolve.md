Status: accepted
Execution: complete bounded ordinary resolve; independent GO
Parent design: #101
Stage: S5
Model: gpt-6-astra
Helper: gpt-5.6-terra for non-kernel resolve harness and tests only

# Route ordinary resolve exactly once through the TypeScript kernel

Move only the ordinary check family through the guarded runtime while keeping arithmetic, RNG, consent, ordering, and receipts in the current kernel.

## Depends on

- T05, T06, and T07 accepted.
- #99 decision-boundary compatibility evidenced.
- T08 only when a selected case genuinely needs new PDF material.

## Scope

- Select eligibility, RuleGraph family, skill, and bound arguments from current scene/public facts.
- Bind numeric values in the host; missing required values remain UNKNOWN, never zero/default.
- Recheck intent/admission and use a stable operation identity before one kernel call.
- Preserve combat, chase, magic, and other non-ordinary incumbents.

## Proposed exclusive write set

- `kernel-ts/runtime/resolve-operation.ts`
- the predeclared resolve registration block in `extensions/kernel/index.ts` after T05 closes
- `tests/kernel/test_jev_resolve.py`
- `tests/extension/jev-resolve-dispatch.test.mjs`

**Subassignment boundary:** Terra may own the non-kernel harness, fixtures, and tests only. Astra owns the `kernel-ts` operation and registration edit.

## Acceptance

- An accepted ordinary profile check executes once and produces the real receipt.
- Lost or ambiguous reply replays the same operation without reroll.
- Missing First Aid binding returns incomplete/unavailable.
- A genuine luck/push/defense/spend choice stops; private retrieval cannot become public consent; quiet same-goal progress respects #99.

## Retirement and rollback

Only the accepted ordinary family switches. Disable at a new attempt boundary and keep committed rolls/effects. Non-ordinary families remain on their incumbent routes without a table-wide fallback mask.

## Accepted bounded gate (2026-09-20)

Actual player input attempted to push a flat-tired car alone without accepting repairs. The public SDK plan selected the current investigator STR 15 profile, passed normal admission and Mod preparation, and settled exactly once in the TS kernel: roll 98, fumble, receipt `roll:str-t2-c1`, final commit `41788b4`. The task took 7.581 seconds and the whole turn 54.1 seconds; these are unpaired observations. Exact request journaling and receipt revision advancement were retained.

Conformance: 22 domain cases, 6 public SDK integration cases, and 16 combined kernel resolve/call-status cases. Lost-response recovery reused the same call and allowed narration; pre-call cancellation/staleness made no settlement; post-settlement cancellation preserved the actual receipt. No nonordinary family, apply, reward, product or performance claim. Evidence/review paths are in the manifest.
