Status: accepted
Execution: complete bounded source vertical; independent GO
Parent design: #101
Stage: S3
Model: gpt-6-astra
Helper: gpt-5.6-terra for non-kernel source helpers and source-vertical tests only

# Deliver the source lookup and proof vertical

Connect native navigation, typed selection, exact SourceRefs, original-page review, and accepted source publication while preserving proof-class boundaries. Astra owns this ticket because required readiness/publication changes include `kernel-ts`.

## Depends on

- T03, T05, T06, and T07 accepted.

## Scope

- Whole-corpus navigation may find candidates before graph identity exists.
- Fresh opening exposes a minimal root/candidate set without fabricated ready nodes.
- Native-extractive consultation may answer checkable plaintext only; visual or ambiguous facts still require original-page review.
- Publication happens only through the existing accepted finish path; source error or unavailable review remains not ready.
- Preserve exact/cached zero-Jev paths, existing campaigns, effective views, and domain failure policy.

## Proposed exclusive write set

- `extensions/module/source.ts`
- `extensions/module/reading-service.ts`
- `extensions/module/reader-review.ts`
- `kernel-ts/modules/reading.ts`
- `kernel-ts/modules/visual.ts`
- `tests/extension/jev-source-vertical.test.mjs`
- `tests/kernel/test_jev_source_vertical.py`

**Subassignment boundary:** Terra may own `extensions/module/source.ts`, `extensions/module/reading-service.ts`, `extensions/module/reader-review.ts`, and source-vertical tests. Astra owns every `kernel-ts` edit, contract integration, and final validation.

## Acceptance

- A late-book clinic query works from a fresh opening and an existing graph.
- Plaintext consultation cannot publish, prepare, execute, or claim scanned absence; a native map remains unknown without visual proof.
- Local exact paths need zero Jev calls; an actual source failure exercises the owner fallback.
- SOURCE MATERIAL error/review-unavailable never becomes ready.

## Retirement and rollback

Retire graph-first readiness only for fresh imports that pass the new opening contract. Existing graph/effective-view routes remain. Disabling typed routing keeps accepted source material and never upgrades unaccepted evidence.

## Implementation evidence (2026-09-20)

Native PDF text snapshots, exact SourceRefs, registered source owner operations, the shared source TaskRuntime domain, and read-only accepted visual-cache inspection are integrated behind `PI_COC_JEV_SOURCE=1`. Source/dispatcher conformance passed; the whole Jev suite had 145 passing tests before the later memory additions.

The real configured Grok Keeper queried the original Blood Road PDF from the initial gas-station scene and found the physician's Monday–Thursday 09:00–17:00 hours on physical page 32. This was native consultation with `prepared:false`, 11 Jev batches / 30 questions, 47,214 input tokens, 1,430 output tokens, listed-price estimate $0.001982988, and a 39.777-second whole turn through ordinary `lookup`/`narrate` and canonical commit. Exact original-page text was host-extracted. Evidence: `.coc/playtests/jev-source-live-20260920/source-evidence.json`. The subsequent repetition used only `narrate`, with zero Jev calls.

The first query is retained as adverse evidence: it became stale after a background NPC voice dossier changed the broad world revision; authored source revision stayed unchanged. This is a view-dependency integration gap, not a provider error. Setup used the incumbent original-page skeleton/opening owner and took 691.5 seconds over four natural setup turns; this does not validate typed fresh-import acceleration. The ticket remains open until that opening contract and concurrent view-update behavior pass. No whole-product or paired speed claim follows.

## Final source gate (2026-09-20)

Fresh v2 navigation covered 111 pages (101 classified, 10 explicitly empty), fixed the v1 statistics-role miss, and guided the actual original-page reader. The reviewed skeleton granted zero ready nodes; the selected opening became ready only after author, independent review and finish. The actual opening player queried the distant doctor while staying at the gas station: exact native page 32 answered Monday–Thursday 09:00–17:00 with `prepared:false`, 11 batches / 30 questions, 11.841 seconds lookup and 61.054 seconds whole turn. Two simultaneous narrates were correctly rejected; one incumbent source-audit stale retry preceded the single commit `5b0d260`. No mechanics, apply, resolve or reward. These are functional observations, not a speed comparison.

Evidence: `.coc/playtests/jev-fresh-setup-02-20260920/fresh-navigation-evidence.json`, `.coc/playtests/jev-fresh-live-02-20260920/source-evidence.json`; independent review `.tmp/team-lead/jev-t08-final-review.md`. V1 and other adverse evidence remain retained.
