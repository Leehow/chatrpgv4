# KIC completion tickets

Baseline: `0.9.3a` / `8fee62f7d`. Scope: the approved P0–P4 completion spec; P5 is deferred.
Spec: https://github.com/Leehow/chatrpgv4/issues/92. State: core implementation verified locally; owner closeout authorizes a scoped local commit. Performance/adoption limits below describe this implementation lane's evidence; independent nine-grid acceptance is owned elsewhere and is not included here. No push or release is part of this closeout.

| Slice | Outcome | Depends on | State |
| --- | --- | --- | --- |
| [Request lifecycle](https://github.com/Leehow/chatrpgv4/issues/93) | Refresh after actual mutations; cancel and reject stale optional work | Spec | Implemented; RPC/hook regressions pass |
| [Static evidence reuse](https://github.com/Leehow/chatrpgv4/issues/94) | Verified bodies reach the next request; scene/entity references survive revisits and restart | Request lifecycle | Implemented; automated restart and real scene-return checks pass |
| [Scene Workpad](https://github.com/Leehow/chatrpgv4/issues/95) | Scene-local drafts, dependency recheck, independent toggle and revision-safe successful delivery | Static evidence reuse | Implemented; lifecycle/isolation tests pass; live adoption unproven |
| [Optional rerank](https://github.com/Leehow/chatrpgv4/issues/96) | Safe prefilter, real need gate, independent consent/toggle, hard deadline and exact-list cache | Static evidence reuse | Implemented; offline fault/permission/budget tests pass |
| [Resource governance](https://github.com/Leehow/chatrpgv4/issues/97) | Enforced count/byte settings, common cache quotas, process-safe writes and bounded eviction | Evidence, Workpad, rerank | Implemented; storage/concurrency/budget tests pass |
| [Acceptance](https://github.com/Leehow/chatrpgv4/issues/98) | Real RPC/Pi regressions, natural Grok table, A–E measurements and honest release decision | All implementation slices | Functional slice verified; Workpad adoption and A–E performance pending |

## Continuation and ownership

- User approved implementation and tracker publication via to-spec, then explicitly approved the real TS RPC → Pi hooks → outgoing request testing seam.
- Preserve the pre-existing modified catalog cache and untracked original design document. The subsequent owner-closeout request authorizes committing only this lane's KIC specification, implementation and tests; it does not authorize a push, package, App restart or inclusion of independent acceptance evidence.
- Use this table and its linked tracker issues as the execution record. Do not mark a slice complete solely because a unit fixture passes.
- Findings to regress: evidence store has no caller; location-only projection; same-turn move leaves prepared context intact; rank can publish after generation changes; rank precedes filtering; final selector can exceed 24 entries; Workpad scope lacks scene; candidate settings have no readers.

## Validation log

- Before implementation: the seven existing workspace/Workpad/context-policy test files pass. Read-only in-memory probes reproduce the lifecycle and filtering defects above.
- Existing live telemetry: turns 0–4, ten workspace rows, nine unavailable rank fallbacks, no Workpad events. This is retained historical evidence, not the new acceptance result.
- New real-kernel integration: same-turn office → newspaper archive → office with real move receipts; source bodies reached outgoing context and L2 hits survived context-runtime reconstruction. Separate scene drafts and independent store writers passed.
- Source prefilter, independent permission and ranking flags, fitting-candidate bypass, shadow no-send, late-generation cancellation, non-cooperative deadline and cache exclusion regressions passed.
- Initial full extension run exposed three direct-TypeScript import failures and two assertions tied to the superseded reference-only/settings contract. The shared cache adapter now supports direct source loading and compiled use; the five failed areas passed 49 focused tests after correction. Final full suite is in progress.
- Concurrent owner changed Electron compaction files and the Pi host contract; those edits are outside this task and are preserved.
- The current source runtime has Grok 4.6 / low configured. No rerank provider settings or key are present in its environment; external ranking and D/E performance acceptance remain unproven.
- Full extension regression passed 1745/1745. The subsequently added rule-version/dependency-group read check passed with all seven read tests. Kernel/play pytest remains running.
- First new natural live run (`kic-completion-static-live`) finalized five turns including office → archive → office and observed persistent source-body hits. Turn six is **invalid-for-acceptance**: editing package guidance during the live run caused `campaign_not_ready: Conflicting bytes for keeper-context 1.0.0`. This is a test-procedure failure, not a KIC success or a newly attributed gameplay defect. All evidence is retained. Freeze the final package at 1.1.0 and create a new acceptance campaign; do not repair or rewrite the interrupted run.
- The old daemon is stopped, with all six turn records retained. Package 1.1.0 is built and frozen. Its package/context/real-lifecycle checks passed 26/26. New home `.coc/playtests/kic-completion-frozen-20260919`, campaign `kic-completion-frozen`, driver run `kic-completion-frozen-live`; `code-digests.json` pins the actual compiled kernel/extensions and package. Do not edit source or package bytes during this run.
- Kernel/play full pytest is still running (one process only), logging to `/tmp/kic-completion-kernel-tests.log`; at 52% it reported four failures, with details pending completion. Final extension full log is `/tmp/kic-completion-ext-verified.log` (1745/1745); later rule read log `/tmp/kic-completion-rule-read.log` (7/7); frozen package log `/tmp/kic-completion-frozen-package.log` (26/26). Keep full-suite and focused evidence distinct.
- A text clarification is pending about whether an existing rerank provider/key is saved in the installed App and authorized for this acceptance. Do not assume permission or transmit Keeper data until an answer/configuration is available. Continue local verification independently.
- Frozen-package run finished four delivered turns and a chapter close, with real office → archive → office, retained possessions/agency, persistent body hits and no package/runtime hash drift. Its telemetry revealed the driver's default model override (`xai/grok-4.5`), so it is retained as functional observation only, not the frozen Grok 4.6 protocol or comparative performance evidence. Its one admission `model_error: terminated` was reported by the normal fail-closed path; no review gate was weakened. No Workpad event was observed.
- Both earlier daemons are stopped. The model-correct run is `kic-completion-g46-live`, campaign `kic-completion-g46`, home `.coc/playtests/kic-completion-g46-20260919`, explicitly started with `--model xai/grok-4.6`. Frozen hashes are copied into that home; no source/package editing is allowed while it runs. Main-session player input remains natural and one turn at a time.

## Verified closeout (2026-09-19)

- TypeScript kernel check, runtime bundle build and `git diff --check` pass.
- Extension full run: **1745 passed**, recorded in `/tmp/kic-completion-ext-verified.log`. Subsequent rule dependency/version read checks: **7 passed**. Frozen 1.1.0 package/context/lifecycle checks: **26 passed**. Final Workpad/rank/projection/command checks after query-budget and foreign-campaign binding guards: **55 passed**, `/tmp/kic-completion-closeout-tests.log`. These are separate runs, not an invented single full-suite total.
- Kernel/play full run: **1518 passed, 4 failed** in 2074 seconds. All four failures were the unchanged Mod-order fixture's obsolete installed-id list, missing the already-shipped `keeper-context`. The fixture now preserves the tested provider order and includes every actual installed Mod. Its complete file then passed **4/4** in 7.98 seconds (`/tmp/kic-completion-mod-order.log`). The 34-minute full suite was not rerun after this fixture-only correction.
- Grok 4.6 / low functional acceptance: `kic-completion-g46-live`, **3/3 delivered natural turns**, 17 tool calls, 250.7 seconds, office → newspaper archive → office, followed by the player's explicit withdrawal. Actual provider identity is confirmed as `grok-4.6`; all pinned compiled/package bytes stayed unchanged during the run. No synthetic Keeper or scripted player was used. The mystery itself was not solved; this is a bounded context/agency check, not full-scenario acceptance.
- Live workspace: 18 projections, maximum **15066 bytes / 24 groups**, **358 body cache hits**, 74 captures, zero restore/write failures. Initial cold reads are represented by captures, not by that failure counter. Hits are per prepared request, not avoided queries or demonstrated latency savings. Formal receipts and current state governed travel and the unaccepted keys/payment.
- Evidence: `.coc/playtests/kic-completion-g46-live/`; its home `.coc/playtests/kic-completion-g46-20260919/` contains `code-digests.json`, `protocol.json`, `acceptance.json`, `kpi.txt`, and the retained campaign. All three task-owned daemons are stopped. Earlier wrong-version/model attempts remain intact and separately classified above.
- After the live run stopped, two extra negative guards were added and tested: include the unchanged query in the rerank UTF-8 ceiling, and reject a Workpad snapshot for a different requested campaign. The latter exposed and corrected the fake-kernel fixture's hard-coded campaign identity. Those negative paths have deterministic proof; the live run did not exercise external ranking or Workpad publication.
- Remaining #98 gates: no actual Workpad patch was observed, no remote rerank configuration/permission was supplied to this source process, and no controlled A/B/C/D/E comparison was run. Do not claim the -30% repeat-read, -15% p50 or <=5% p95 targets. The static feature remains default-off and independently usable; optional components stay independently controllable.
- No source commit, push, installed-App packaging or deployment was performed. Concurrent commits `50e5cc275` and `7f081220c` are preserved, as are the user-owned catalog-cache change and original untracked design.
- All six final verification logs have durable copies in `.coc/playtests/kic-completion-verification/`; `/tmp` paths above identify their original execution output. Final checks after the two last guards passed **55/55**. No verification process or task-owned live daemon remains running.
- Final seam review also connected the persisted scene/entity/thread indexes to candidate restoration, rather than merely storing them. The host reserves one quarter of the shared candidate budget (at most 32 of 128) for cache inspection, favors the current scene and a bounded restored set, and refuses to resurrect a locator that fresh metadata marks unavailable. Authored conclusion links produce thread references; current scene/entity/thread matches consume them. The cache's metadata scan is bounded even when references are stale or foreign.
- After that connection, the final KIC/storage/Workpad/rank/home suite passed **60/60**, and the outgoing-request suite including dormant material outside the fresh candidate pool passed **21/21**. Logs: `.coc/playtests/kic-completion-verification/final-kic.log` and `dormant-request.log`. Runtime build and diff checks pass. These later negative guards and dormant-candidate changes have deterministic proof; the retained Grok run predates them and is not represented as live proof of every final branch.

## Owner commit closeout (2026-09-19)

- Reviewed the scoped tracked and untracked KIC changes against `7f081220c` on the latest `0.9.3a` branch; retained the concurrent commits and excluded the catalog cache, original user-owned design and every `.coc` acceptance artifact.
- Re-ran the kernel type check and 112 focused extension/context/isolation tests successfully. `tests/kernel/test_mod_order.py` passed 4/4 again; `complete_order` obtains the actual installed order, including `keeper-context`, while retaining each test's selected provider precedence.
- Review reproduced one additional event race: after a mutation cleared the capsule, a source publication arriving during rehydration was ignored because the mode check treated the temporarily absent capsule as off. The host now retains the observed mode across invalidation; the old in-flight hydration is cancelled and the current source is prepared. The regression failed before the fix and passed after it. The affected final regression group then passed 73/73; `git diff --check` passed.
- This is one atomic implementation/contract/test commit. No new live table, remote-provider experiment or nine-grid evidence is claimed by this commit; the previously documented acceptance limits remain separate.

## Original acceptance-case coverage

This is a coverage map, not a claim that all 36 scenarios have passed real play.

| Original cases | Evidence and remaining limit |
| --- | --- |
| KIC-01, 02, 05, 25, 31 | Bodies and count/byte bounds reach real outgoing requests; real-kernel same-turn movement, uncertain-result refresh, scene return, context-runtime reconstruction and message pairing pass. Grok 4.6 confirms the basic scene-return path. |
| KIC-03, 04, 06, 07, 12, 13 | Static/dynamic separation, graph/entity references, original player query and existing authoritative paths are implemented. The live player can reject the commission without old source context granting possessions. Dedicated locked-door, carried-NPC, repeated-combat and semantic-counterevidence live cases remain in #98. |
| KIC-08–11 | Workpad strip/bind/publish/refusal, budgets, scope and version conflicts have automated evidence. No natural model-produced patch was observed; actual adoption and structured-ask use remain live gates. |
| KIC-14 | Real workspace RPC and evidence-store tests compare formal campaign files and preserve them. No reader, transaction, call-id or receipt is minted by the optional read/cache path. |
| KIC-15 | Candidate extraction precedes the public lookup cut and uses scene/identity/graph/lexical channels. Dedicated retrieval-quality and recall-rate measurement remains open. |
| KIC-16, 24, 28 | Historical semantic retrieval, dynamic derived caches and cache-based merge are deliberately not enabled. Existing memory correction and kernel worldline reachability remain authoritative; their P5 extensions are not claimed. |
| KIC-17–23, 34 | Source/scope filtering before transmission, optional gates, hard timeout, malformed-result fallback, exact-list caching, independent rule version and incomplete-group refusal have offline tests. External-provider quality/latency remains untested. |
| KIC-26, 27, 29 | Independent revision binding, campaign/worldline/loop/scene isolation and delayed-result generation checks have deterministic coverage. Cache and draft scope never merge game state. |
| KIC-30, 32 | Atomic storage, corrupted body/index, orphan/temp accounting, separate store writers, quotas and request limits have focused tests; bounded long-context suites pass. A large natural campaign remains a performance gate. |
| KIC-33, 35, 36 | Transport-only projection, admission separation, data-only source framing, independent switches and unchanged off behavior are covered at the existing Pi seam. This does not prove immunity to every possible semantic prompt injection or a speed benefit. |
