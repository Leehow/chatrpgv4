# Story continuity and adaptation — execution plan

## Objective
Implement issue #84 for midgame causal clarity in production TypeScript: one bounded continuity projection, lawful Keeper clarification/initiative, and reviewed campaign-local adaptation without comprehension scoring, forced progression, or source mutation.

## Scope
Production exclusively `kernel-ts`, using the existing seven RPC verbs and current RPC/extension seams. Continuity lookup and thread Mods share one bounded builder. Adaptation preparation/status/cancel is host-owned and asynchronous; creator and reviewer are independent, operations are normalized and closed, acceptance is ready-only and one-effect, and source snapshots, effective loading, safe-boundary rebase, and worldline refusal are explicit. No generic patches, source retcons, numeric mechanics invention, global gates, new scheduler, raw-provider drafting, or old Python implementation.

## Base and repository hygiene
Owned branch: `codex/story-continuity`, base `eb379510`; primary HEAD is `cefd3fb5`. Parent implements serially. The primary checkout has unrelated uncommitted NPC-journal work overlapping `docs/kernel-rpc.md`, handlers, registry, read/handlers, write/store, and a foundation test; do not integrate until ownership clears. Preserve play evidence; secrets remain ignored. No feature commits or merge yet; remaining primary dirty foundation/UI tests belong to another task and are not ours to overwrite.

## Implementation status
Implemented: continuity paths; normalized closed adaptation operations; source snapshots/effective loading; async fresh creator plus independent reviewer; explicit single-effect acceptance; attempt ownership; source rebase; and worldline conflict refusal. Keep the execution plan short and remove stale venue-repair claims. Adapted NPC knowledge now carries campaign provenance through continuity as well as look/capsule; regression is pending.

Versions used: story-thread `1.1.2`, keeper-pacing `1.2.1`. The tested setup was upgraded through normal `mods.configure` at a ready safe boundary. One corrected-prompt setup failed the immutable-version guard and was retained as evidence; new package versions were then used. Do not imply in-place mod text changes.

## Validation
The latest source/host/admission focused suite passed 30 tests. The wider extension suite is 804/807: the expected current-only host method-list failure was repaired; the two remaining failures are unchanged baseline image-gen CJK and runtime-reader old-brief expectations. Full pytest was 1228 passed, 3 failed, 1 skipped; the two continuity failures (discarded thread-budget key lines and reminder bytes over 4000) were repaired and 17 related tests then passed. Our failures were fixed and 17 affected Python cases subsequently passed; baseline exceptions remain documented pending integration check.

Real evidence: `continuity-live-v2` turn 4 had zero stalled/empty turns, but no receipts, time, or action; the Keeper distinguished public paper evidence, hearsay, and unknown links, and the player chose newspaper investigation. Setup/play/resume logs remain under `.coc/playtests/continuity-live*`. A turn-7 harbor-lodging narration while the world remained newspaper-morgue was marked invalid in `continuity-live-v2-resume/acceptance-status.json`. `continuity-venue-live` was blocked after a label-only office→hotel batch and unchosen actions; it did not claim success. A fresh DeepSeek creator and independent reviewer accepted `add_scene` Atlantic Hotel plus a route through an adaptation receipt, then ordinary move placed play there; no clue, threat, NPC presence, or forced action was introduced. Cold restart retained Atlantic Hotel. `continuity-venue-repair` is no longer running; phase-3 whole acceptance has not passed. The player chose one night at 2.50, but KP wrongly charged 3; amount/duration admission was strengthened, and the cache signature includes `label`/`travel_minutes`/`with`. Separate real DeepSeek admission probes rejected 3/two nights and accepted 2.50/one night; probes are not gameplay. Cold play attempted a 0.50 refund and hit the integer-delta blocker; an alternative 1 refund was rejected by admission, so no refund success is claimed. The decimal worker is fixing this; the main session will replay a legitimate refund using DeepSeek Flash.

All play/review lanes used DeepSeek Flash, no Astra/Grok. The human unfamiliar-with-source UI gate remains pending; no Greek transplant is claimed. Original failed runs/evidence remain retained. Correct replay wording: rejected NEW stale requests differ from idempotent replay of an accepted call.
