# Story continuity and adaptation — implementation record

## Objective
Implement issue84 with priority on midgame causal clarity, source-backed interventions, and campaign-local graph adaptation. Immutable source and kernel arithmetic/transactions; no comprehension scores, forced progression, new compulsory per-turn review, or Python production code.

## Branch and ownership
Owned `codex/story-continuity`; feature commit `c873983a`, merged primary committed `cefd3fb5` via `10c0dfe5`. Final source-priority/craft/budget repair is about to receive a local commit. Primary `0.9.2a` is unchanged and has other-task dirty foundation/UI tests/assets; no merge into primary and no push. Preserve all `.coc` evidence and retain this worktree through lifecycle, with verification failed because acceptance is partial.

## Implemented
Shared continuity lookup/thread connections; closed campaign adaptations with original-source snapshots, tool-enabled creator plus independent reviewer, ready-only acceptance, stale/cancel/replay handling, effective graph across all play consumers including NPC journal, source rebase, and worldline conflict refusal. Exact decimal cash uses kernel ledger authority. Existing-scene recent acquired clues retain distance0 so their evidence is not outranked by old remote clues. Final source packages `story-thread1.1.3` and `keeper-pacing1.2.2` keep source roles/unknown causes explicit and fit the existing combined reminder byte budget. Instructions are craft guidance, not deterministic hallucination prevention.

## Validation
Integrated full extension suite830 passed before the last one-line source-priority/craft repair. After that repair, source/host/language focused26 passed, Python thread/budget17 passed, kernel typecheck and runtime build passed. An integrated focused Python run had34 pass/1 budget fail; the budget was repaired and all17 affected tests passed. Cash worker focused RPC1 and existing TS cash module8 passed; later integrated cash/journal/table-register cases were among the34 passing. Earlier full Python run1228 pass/3 fail/1 skip is historical/pre-integration, not current full-suite success; its failures were repaired and remaining baseline cases passed after primary integration. Human UI acceptance is pending.

## Live evidence
All Keeper/adaptation review runs used `deepseek/deepseek-v4-flash`, no Astra/Grok. Campaign `continuity-live-v2` turn4 clarified public facts/hearsay/unknown links at stalled=0/empty=0, with no receipts/time/actions; the player chose newspaper research. Campaign `continuity-venue-live` accepted Atlantic Hotel+route through a real fresh creator/reviewer and ordinary move, and survived restart. Run `continuity-venue-refund` turn1 is campaign turn7: cash before6 delta0.5 after6.5, no extra night. Campaign turn8 recalled actual turn0 hospital information. The operator interrupted campaign turn9 during a hospital visit; run `continuity-venue-integrated` cold recovery retained move/roll/NPC receipts without duplicate move/roll, then campaign turn11 delivered `gabriela-night-visitor` with a real clue receipt. Do not claim the interrupted run passed. Canonical evidence is in `.coc/campaigns/<campaign>/turns` and `.coc/playtests/<run>/`.

## Unresolved acceptance
Campaign `continuity-venue-live` turns12 and13 FAILED. The player declined the bible and wanted existing facts clarified. KP invented childhood/ownership connections; after source-priority and prompt repair, run `continuity-source-correction` (campaign turn13) still invented more connections to reconcile its earlier mistake. Both failure classifications and transcripts are retained. The existing post-delivery verifier reported an unearned reveal but is advisory. No Greek transplant test or uninformed-human comprehension gate passed. Implementation is reviewable; whole-feature acceptance is not complete.

## Approved next scope (implementation pending)
The user explicitly authorized this extension (“yes”). At `6ce91b34`, implement `audit.source.v1` in `narration-audit1.1.0` through the existing tool-enabled Mod prepare/result and refusal/repair flow, including both explicit and implicit delivery seams. It is not a daemon, planner, zero-tool lane, style grader, or new compulsory review; legacy versions/capability-absent audits and the disabled optional Mod retain existing behavior. Use immutable original/effective/world/full-history/handout evidence with non-authoritative notes/memory, bound and stale-checked evidence/candidate/package bytes, exact bounded citations, and the specified supported/unsupported/unclear gate. Preserve failed turns12/13 and all artifacts; unsupported, unclear, timeout, malformed, tampered, or stale review refuses without rewrite or mutation. Implement the seams, then bounded regressions and real-play acceptance. Keep DeepSeek Flash as Keeper (no Astra/Grok); human UI gate remains pending. Do not claim success until measured.
