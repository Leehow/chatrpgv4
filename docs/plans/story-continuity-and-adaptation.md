# Story continuity and adaptation — execution plan

## Objective
Implement issue #84 for midgame causal clarity in production TypeScript: one bounded continuity projection, lawful Keeper clarification/initiative, and reviewed campaign-local adaptation without comprehension scoring, forced progression, or source mutation.

## Scope
Production exclusively `kernel-ts`, using the existing seven RPC verbs and current RPC/extension seams. Continuity lookup and thread Mods share one bounded builder. Adaptation preparation/status/cancel is host-owned and asynchronous; creator and reviewer are independent, operations are normalized and closed, acceptance is ready-only and one-effect, and source snapshots, effective loading, safe-boundary rebase, and worldline refusal are explicit. No generic patches, source retcons, numeric mechanics invention, global gates, new scheduler, raw-provider drafting, or old Python implementation.

## Base and repository hygiene
Feature commit is `c873983a`; primary committed state is `cefd3fb5`. Primary is being merged into owned `codex/story-continuity` now. Primary itself remains unchanged and has unrelated dirty foundation/UI tests/assets. Do not push or merge into primary. Retain and archive play evidence through its lifecycle; do not delete it.

## Implementation status
Continuity paths, normalized adaptation operations, source snapshots/effective loading, independent asynchronous creator/reviewer, single-effect acceptance, stale/cancel/replay handling, source rebase, worldline refusal, and journal integration are implemented. The journal is a play-facing consumer and loads `loadCampaignModule` after the world read, so adapted scenes and NPCs appear in `journal.job` too. Remaining work is source integration including journal, integrated tests, final real cold-load evidence, lifecycle retention/archive of evidence, and the pending human gate.

## Validation
The bounded feature suite's latest 30/31 failure was an invalid malformed test hypothesis fixture with undefined fields; it was corrected and the targeted budget case passed. Targeted provenance/agenda tests passed 2. Integrated full suites are pending; historic broad-suite counts are pre-integration and cannot be called current integrated full-suite green. Keep concise paths and classify invalid-for-acceptance runs without repeating every failed-run detail. The genuine DeepSeek refund evidence is retained: campaign `continuity-venue-refund`, venue `continuity-venue-live`, turn 7 had cash before 6, delta 0.5, after 6.5 and time 5, with no second night. Turn 8 retrieved the actual turn-0 transcript for Roxbury Sanitarium; the rental-dispute theory remained a theory with no movement and only an authorized notebook write. An unnecessary memory resolve was refused for having no dice/time. Main is playing the next chosen hospital visit; outcome is pending. No Greek transplant live test or human comprehension claim is made; the unfamiliar-with-source UI gate remains pending.
