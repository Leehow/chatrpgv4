# Adaptive Jev prescreen loop

Status: the preload replacement, fixed Keeper support schema and Keeper-initiated support lookup are implemented and validated. The latest complete extension suite passes 2392/2392. Broader real-table quality/performance acceptance and the separately recorded host-wide type diagnostics remain open. User confirmed the former prescreen task is paused and this task takes over its files.

## Objective

Give the existing read-only prescreener an adaptive discover/read/follow/finish loop. Each decision observes actual previous reads; new owner-issued candidates can enter the frontier. The host assembles the existing source-bound material packet for the Keeper. Success is a dependent evidence chain reaching the actual Keeper request without changing gameplay authority. More calls or a standalone mock loop is not acceptance.

## Scope and ownership

- Current latest branch: `0.9.4a`, starting HEAD `85faec8b4`.
- This task owns new `extensions/table/prescreen-loop.ts` and its focused tests. On user-confirmed takeover, the prior material-supply owner (`01a0c634-289f-76a0-96a5-51c9c7777ec3`) interrupted all child writers and provided a final hash-bound handoff at 11:40 UTC. Its final prescreen SHA was `462a4d4d0875077e284924b2865bd969cb52919368cf4b9c4795342afdc51e8a`; its last needsCoverage correction is retained.
- The prior owner also handed over the shared preparation files previously released by NPC task `01a0c672-dfbc-7f51-bea4-89aeba362af0`. NPC-specific work remains separate. The initial loop/optimization did not modify these shared files; the subsequent user-authorized six-defect repair changes context-runtime.ts/context-policy.ts only for request budgeting. Preparation-budget.ts remains untouched by this task.
- Reuse current TS kernel, DecisionAdapter, TaskLease, SourceRef, material readers and final request projection. No new public gameplay verb, generative planner, world writes, source publication, dependency changes, package, restart, push or automatic commit.
- Parent material-supply acceptance remains in `jev-prescreen-material-supply.md`; this file tracks only the newly authorized adaptive loop.

## Implementation sequence

1. Done: user confirmed takeover; additive section 124.6 freezes the host-only interface. Original shared files are preserved in `.tmp/jev-prescreen-agent-loop-baseline/`.
2. Done: typed operation selection with dynamic observations, host closures, no-progress detection, same allowance and honest partial results. No second task framework.
3. Done: replace fixed supplementation with the loop. Focused existing workspace catalog navigation follows exact owner-issued entity handles while retaining the original query binding. Native PDF continuation reads at most two new pages with the captured extraction binding; raw excerpts never inherit consultation authority. Memory reads retain original/finish owners. Reuse invalidates old retrieval assessments.
4. Focused 16/16 passed, including real TS graph relation -> focused discovery -> read -> actual context hook -> converted provider payload. Broader validation recorded below.
5. Real Jev component probe and two canonical Grok Keeper turns completed; see evidence below. Neither is a speedup claim or a completed full-campaign acceptance.

## Design evidence

- TypeSafe function-calling cookbook: closed-set tool/argument decisions dispatched by host code. https://docs.typesafe.ai/cookbooks/function_calling
- Anthropic context engineering: incremental tool-driven discovery, with explicit exploration latency tradeoffs. https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
- Current prescreen supplementation follows a fixed catalog-page path; current material-supply repair is concurrently changing packing and continuation. Integrate against the released revision rather than editing a stale copy.

## Validation

- Focused final bundle: **16/16 passed**. Loop, real request integration, source owners and new PDF continuation/extraction rejection. Log: `.tmp/jev-prescreen-agent-loop-baseline/final-focused.log`.
- Strict host TypeScript for prescreen/source and `npm run check:kernel`: passed. Final `npm run build:runtime`: passed. `git diff --check`: passed.
- Broader prescreen/workspace/source bundle: **91/96 passed** (four distinct failing assertions; a parent subtest adds the fifth failure). All four failures reproduced with the takeover-baseline prescreen loaded by an esbuild read-only substitution, without reverting the shared checkout. Failures: first-page graph unit expectation, old combined graph-body relation expectation, tiny native-packet gap retention expectation, and oversized-workspace fallback. These belong to the interrupted prior material-supply repair, not new loop failures. Logs: `prescreen-regressions.log`, `baseline-results.log`, `baseline-owner-results.log` under the baseline directory. They remain open; no assertions were weakened or unrelated fixes introduced to claim a clean full gate.
- Existing supplement tests now drive issued loop operation choices and retain the original material/freshness/timeout assertions. A missing owner's materialization reply no longer silently substitutes an unread candidate; the catalog-continuation test supplies the actual second owner's reply.
- Full `npm run test:ext`: **2358/2366 passed**, exit 1, 227.4 seconds. Eight failed test entries represent six distinct assertions: the four inherited material-supply assertions above, `jev-owner-handoff` unknown-route phase (`composing` versus `planning`), and the suite wait guard rejecting an iteration-count wait in the concurrent object-usages test. The latter two paths were not modified by this task. No full green gate is claimed.
- Final projection review found inherited private graph-unit dependency keys in the new loop's fetched material. Both Jev candidate coverage and Keeper material/gap coverage now project semantic entity context requirements; host catalog keys remain private. Fingerprint dedup uses the same public coverage shape. Operation/gap preview omissions are also explicit in Jev state. After these final guards, strict host/kernel checks and runtime build passed; the broad **91/96** bundle was rerun with exactly the same four distinct inherited failures and all 16 focused cases passing. Logs: `final-regressions.log`, `host-typecheck.log`, `kernel-typecheck.log`, `final-build.log`.

## Real-provider and real-table evidence

- Real Jev component probe (explicitly **not a playtest**): `.tmp/jev-prescreen-agent-loop-live/provider/result.json`. From five real initial office source units, Jev chose `follow` to `chapel-of-contemplation-ruins`, then `read` for its authored conditions and affordances, then finish. Three materials retained, five Jev calls, 4,919 ms total, original six-second optional allowance. The new target was absent from the initial candidate set. Actual fetched content includes the entry clue, cellar/cabinet conditions and manuscript study constraints. This proves one useful dependent selection, not general accuracy.
- Final-source real Jev probe: `.tmp/jev-prescreen-agent-loop-live/provider-final/result.json`. Same question and five initial real source units in a separate new diagnostic campaign; Jev again chose follow -> read -> finish. Two materials retained, five calls, **5,399 ms**, ready assessment. Exact private dependency keys are absent from the Keeper packet. This rerun validates the final projection/omission changes and is retained alongside the earlier probe.
- Canonical fresh real run: `.coc/playtests/jev-loop-trial-20260922/`, campaign home `.tmp/jev-prescreen-agent-loop-live/home/`. `tests/play/driver.py` launched `bin/pi-coc`, Grok Build 4.7 fast / low; main session was the sole player and sent one natural utterance at a time after the automatic opening settled. Turn 1 accepted Knott's commission and asked about the former tenants and records; turn 2 went to the Globe clipping room and stopped at its access conversation. Both delivered, then the owned daemon was stopped. No action was replayed and no evidence was deleted.
- Turn times: **77.4 s / 82.2 s**, 13 total tools. Prescreen: **2,945 ms / 2,907 ms**, five Jev calls each, eight/six materials confirmed in actual outgoing requests. Both real turns took the already-supported fast path (zero extra loop operations). This is delivery/compatibility evidence, not live-table adoption of a multi-step loop, a matched A/B or a speed improvement. The focused real-Jev probe above supplies the separate multi-step semantic evidence.
- The real table used the immediately preceding runtime build. Subsequent final changes keep optional diagnostics from evicting evidence, project dependency keys as semantic context requirements and expose preview omissions. Final source was rebuilt, the real Jev component probe repeated, and the broad focused matrix rerun. There was no App packaging, installation, restart, push or commit.
- Credentials were read through the existing App vault and supplied only to consuming processes. No key was written to a script, report or repository file.

## Remaining acceptance

The six reported full-suite assertions are closed by the repair recorded below. Real-table use of a dependent loop when the initial packet is insufficient and broader controlled quality/latency comparisons remain separate. No claim of full #108/#109/#126 completion or shipped-App acceptance is made.

## User-authorized loop optimization

2026-09-22: user requested further agent-style optimization including concurrent tools. Scope is the existing loop/host supply and their regression/probe evidence. Keep the same six-second deadline, source authority and packet contract; no package, commit or unrelated lane repairs.

Planned changes: combine coverage with routing; run at most four independent issued reads with deterministic staged publication; reuse complete material already returned by the bound catalog. Dependent discovery remains serial. Baseline source and a bundled baseline API are preserved at `.tmp/jev-loop-optimization/`. Compare baseline and optimized real-Jev probes against the same owner, question and frozen starting catalog; component timing is not a real-game speedup claim.

External checks: TypeSafe speculative fan-out evaluates independent questions on one frozen state; Anthropic parallel-tool guidance likewise forbids guessing parameters needed from earlier results. These support bounded parallel independent reads, not parallelizing the follow -> newly discovered target dependency. Sources: https://docs.typesafe.ai/models and https://www.anthropic.com/engineering/advanced-tool-use.

Implementation complete: the loop's v2 decision family combines coverage/routing and speculative independent read inclusion. Host-only concurrency keys prevent shared-owner reads from overlapping; read stages commit in issued order after all siblings settle. A stale sibling rejects the batch; a hung sibling cannot hold completed evidence beyond the caller deadline, and its named gap is retained. Catalog body reuse removes duplicate read RPCs while final owner checks remain. Read timing records parallel wall time rather than summing overlapping operations. Optional decision packing failures preserve validated material as partial instead of discarding it.

Optimization validation:

- Final targeted prescreen/workspace/source bundle: **96/101 passed**, with the same four inherited distinct assertions (five failed entries due to a parent subtest); no added failure. The five additional cases cover true read overlap/stable publication, shared-owner serialization, stale sibling rejection, a hung sibling deadline, complete catalog reuse and host NPC concurrency (existing cases also extended). Log: `.tmp/jev-loop-optimization/final-regressions.log`.
- Strict host typecheck, kernel typecheck and runtime build passed. Full extension validation: **2363/2371 passed**, exit 1, 237.6 seconds. The same eight failed entries/six distinct inherited or unmodified-path assertions remain; no new failed assertion. Log: `.tmp/jev-loop-optimization/full-extension.log`. The loop-only suite additionally passed **11/11** after strengthening the named timeout-gap assertion. `git diff --check` passed.
- Controlled real-Jev component comparison uses one TS owner, canonical wire JSON, identical query/capsule/catalog digest, unchanged six-second budget, no decision cache, and **ABBA** order. Two baseline samples: **5,293 / 4,357 ms**; optimized: **2,941 / 2,799 ms**. Mean **4,825 -> 2,870 ms**, **40.5% lower**. All four returned ready with the exact same core condition-material hash. Jev requests **5 -> 4**; workspace RPCs **4 -> 2**; optimized cycles selected four independent material reads together. Total Jev input tokens were **32,593 -> 32,179** per run, so no large token-saving claim is made.
- Evidence: `.tmp/jev-loop-optimization/paired-final-input.json`, `paired-final-results.json`, `comparison-summary.json`; immutable baseline/optimized probe bundles are retained beside them. This is a small component experiment, not p95, a broad quality evaluation or a gameplay speedup measurement. The final optional packing-failure guard was added after the comparison bundle and is covered by the context regression; it does not affect these successful probe paths.
- Earlier `paired-results.json` overlapped a test invocation and had one partial baseline timeout; it is retained as a load-affected diagnostic and excluded from the controlled timing claim. The first harness attempt stopped on raw kernel numeric-wrapper JSON serialization before any provider request; the corrected harness uses the owner's canonical serialization for both arms. No campaign or evidence was deleted.

No commit, push, package, App restart, parallel agent delegation or unrelated lane repair was performed for this optimization.

Optimization status: complete for the approved code change and bounded comparison. Matched real-table latency/quality across broader scenarios remains unproven. The measured reduction primarily removes one model round and two redundant materialization RPCs; parallel reads are exercised by both model selection and overlap/order regressions, but their isolated contribution is not separated from reuse in the timing claim.

## User-authorized repair of the six outstanding defects

2026-09-22: user explicitly requested repairing all six reported failures. Success is correct material delivery and unknown-route behavior with the failed assertions resolved by their real contracts, not a lower test standard. Scope extends to message budgeting, graph unit ordering, compact gap delivery, ordinary-resolve unknown-route handling and the corresponding relationship/wait tests. Preserve unrelated dirty work; no commit, push, packaging or App restart is authorized.

Decisions are frozen in sections 122 (unknown route) and 124.7: use public Pi message conversion for model budgets; preserve a coherent bounded identity/authored/relationship front per entity; keep useful compact gaps; replan an unknown ordinary route; wait for the actual asynchronous condition in the item test. External comparison used Anthropic's structured-input token counting and Elasticsearch's grouped hit expansion. Neither provider-specific token estimates nor a new search backend is introduced.

Completed: the previously failing cases, relevant request/kernel/loop regressions, scoped typechecks/build and final complete extension suite were run. All earlier timing and failure evidence is preserved.

Repair implementation and focused checks:

- Added shared `requestSize`, based on public Pi `convertToLlm` with private tool details excluded. Projection, baseline allowance, supplement packing/reuse, bounded tails and request telemetry use it; raw artifact/history limits retain `sizeOf`. A new regression retains 100 KB of private host metadata without charging it to the model while still refusing an oversized visible body.
- Graph catalog fronts now preserve identity + first authored unit + first relationship unit per prioritized entity before deeper tails. The relation request test checks actual authored and relation units for the same office, including its real newspaper exit, in the converted provider payload.
- Tight prescreen packets compact gap descriptions before dropping optional gaps, retaining reasons and usable continuations. Full gap details remain private. The native fallback test still requires the explicit qualification-budget reason and a partial raw excerpt under the 2,800-byte model-message bound. Its mock answers now conform to the current typed decision questions.
- Unknown ordinary resolve routing returns `replan` instead of a composing-ready partial result, matching ordinary apply. The original planning assertion remains and now also proves no settlement dispatch.
- The item creator test waits for `mods.accept` via the existing wall-clock wait helper instead of counting event-loop yields.
- All affected request/kernel/loop/context/owner/wait cases: **186/186 passed**. Scoped strict typecheck for context-policy/prescreen/ordinary-resolve, kernel typecheck, runtime build and `git diff --check` passed. Logs: `.tmp/jev-six-defect-repair/`.
- Expanding the ad hoc strict command to all of context-runtime's transitive imports still reports the **19 pre-existing diagnostics** recorded at the NPC handoff: `extensions/kernel/client.ts`, `extensions/lanes/subsession.ts`, `extensions/module/reader.ts`, `runtime/tasks.ts`. These files were not changed in this six-defect repair. They are not a newly green gate or silently repaired scope.
- Full extension results are recorded below; no new real-play or speedup claim is made by these deterministic fixes.

The first full repair run passed 2370/2372 and exposed a distinct existing test for a *missing* route answer. The semantic distinction is now explicit: a complete answer choosing `unknown` replans; an incomplete/unavailable response stays partial without a semantic replan. The original tests for both outcomes remain, with a new explicit-unknown case and no-settlement assertions. Ordinary route/owner/preflight regressions then passed **41/41**. The final full result follows; logs are `route-regressions.log` and `final-full-extension.log`.

Final repair result: **2373/2373 extension tests passed**, exit 0, 226.8 seconds, no skips or cancellations. The original eight failed entries are cleared. Scoped strict types, kernel types, runtime build and diff whitespace checks passed; the separate 19 transitive host diagnostics remain explicitly outside this six-defect repair. No test baseline was relaxed, no evidence removed, and no commit/push/package/App restart performed. Final log: `.tmp/jev-six-defect-repair/final-full-extension.log`.

## Replace preload with the shared Jev agent and fixed schema

User-authorized 2026-09-22: replace the weak existing preload with the Jev agent and deliver a fixed parameter framework to the Keeper. The pending optional question asks whether mechanical recommendations belong in this iteration; independent work proceeds on evidence and judgments, with no fabricated check parameters.

The user answered: include check-parameter suggestions, still subject to the existing admission and kernel. Reuse `runtime/jev/check-preflight.ts`; close its campaign/scope/turn binding, hung-read deadline and public-context gaps before connecting it. Extend only the corresponding resolve-options read contract and relevant tests. Suggestion failure must not remove valid source material; suggestions never settle a roll or broaden player consent.

Contract section 124.8 is the integration target. Move the reusable read-only loop into runtime/jev, remove the unconditional legacy classification stage from active preload, suppress duplicate legacy optional workspace selection while Jev owns preload, and validate the fixed request/result schemas including material-slot references. Reuse all existing source/memory/read owners, the shared lease and guarded final request path. Mandatory current state/history/package instructions remain host-owned. Later public lookup/recall consumption uses the same contract but is not silently implemented by enabling the macro-plan mode.

Preserved starting sources: `.tmp/jev-agent-replacement/`. Starting gate: 2373/2373 extension tests. Scope: shared agent and support schemas, preload adapter/context integration, schema guidance and necessary regression migration. No package, push, commit, new source store, game mutation or model-written identifiers. External practice rechecked against TypeSafe fan-out and Haystack PipelineTool; no new dependency is introduced.

Implementation now uses the shared `runtime/jev/evidence-agent.ts` and validated `keeper-support-contract.ts`. Small owner sets expose reads directly, larger sets expose bounded indexes, and a packing-limited frontier includes navigation to omitted tools. Preload purpose is explicit in Jev state. The old optional KIC workspace/reranker is suppressed only while Jev preload is active; required capsule/history and ordinary Keeper tools remain. Check-preflight uses the shared allowance, bound resolve-options, public dialogue only, and independent final validations. Final packing includes check advice, keeps exact material bytes, and falls back to unknown advice before sacrificing valid material. Full private gaps survive bounded public projection.

The starting full replacement run passed 2377/2380 (no skips); two distinct assertions failed: the NPC integration fixture still emitted/expected the retired preselected_context protocol, and a real dossier read exceeded the shared deadline while the full suite, real-provider probes and live Keeper overlapped. The NPC fixture now emits agent-operation decisions and requires an actual dossier in keeper_support. No timeout ceiling was raised. The subsequent focused suite passed 116/116, including the real kernel/context/provider check-advice seam. Final checks now run concurrently; a dedicated barrier regression verifies overlap and preservation of material when check advice becomes stale. The final full suite and isolated real-provider results are recorded below when complete.

Final evidence before the full-suite closeout:

- Strict support/prescreen TypeScript, kernel TypeScript and runtime build pass. Focused source/loop/check/NPC request tests pass **116/116**. Independent final-check barrier/stale-advice tests pass in the **35/35** request bundle; an added focused-follow regression passes with the resulting **36/36** bundle. A focused follow now exposes an already-catalogued target from an unopened group rather than treating it as no new evidence.
- Isolated final-source real Jev probes: `.tmp/jev-agent-replacement/provider-verified/source.json` and `check.json`. Both produce a schema-valid packet. Source inquiry retains four exact authored units in **4,294 ms**, five Jev calls, and honestly reports partial/no_progress; full access conditions are not claimed complete. Ordinary action preparation takes **1,868 ms**, three Jev calls, and suggests Spot Hidden / regular / zero bonus and penalty dice, advisory only. Its retrieval supplies no additional material and marks the remaining need partial. These two samples are functional evidence, not a comparative speed or broad semantic-quality result.
- Earlier probes are preserved in provider/ and provider-final/. The first source probe overlapped the complete test suite and real Keeper and hit the six-second deadline; its failure is retained, not hidden in the later result. The first isolated probe exposed the already-indexed follow visibility defect, which was repaired and covered by the focused regression.
- Canonical real Keeper run: `.coc/playtests/jev-support-schema-20260922/`, source home `.tmp/jev-prescreen-agent-loop-live/home/`. Fresh pregen, Grok Build 4.7 fast / low, main session as the sole player, one natural utterance and no replay. The final narrative settled in **150.4 s**, six tools. A fixed support packet was prepared in **2,486 ms** and verified in the actual outgoing provider request; it carried no extra material, so this is schema/check-path compatibility, not real-play retrieval-quality acceptance. The owned daemon was stopped after delivery and all evidence retained. This run precedes the final independent-check overlap, purpose guidance, bounded-gap and indexed-follow refinements; final-source component/request verification covers those refinements. No shipped-App acceptance is claimed.

The second complete run passed **2381/2384**, exposing two remaining distinct problems without any other workloads: the dossier fixture still exceeded the six-second bound, and the source-request fixture failed during teardown with ENOTEMPTY (the stale-source assertion itself passed). The name-only initial failure summary was misread as a source-rejection failure; the complete error corrects that diagnosis. Separate inspection found that source validation could start before check preparation settled, so that ordering now has its own barrier regression. The production adapter changes are: issued validated current-state/catalog read descriptors now call their existing owner directly without rebuilding the entire catalog first; rule-family descriptors still use the materialization owner and retain its exact clauses instead of overwriting them with another lookup. All check-preparation decisions are awaited before independent final owner validations start. A barrier regression enforces that ordering; the final-check overlap/stale-advice regressions remain. No deadlines, assertions or baselines were relaxed. The owner/source/NPC regression set passed **52/52**, and the two added barrier/rule-owner cases passed individually. Third complete result follows.

Third complete run: **2385/2386 passed**, all product assertions passed and the sole failure was the same source-request temporary-directory teardown ENOTEMPTY. The fixture now uses the existing suite harness pattern of five bounded removal retries at 50 ms after runtime, Git and source-document shutdown. This affects only fixture cleanup and relaxes no product assertion. This corrects the earlier diagnosis based only on the failing test name. The final full rerun follows.

Final closeout: **2386/2386 extension tests passed**, exit 0, **241.0 seconds**, no skips or cancellations. Log: `.tmp/jev-agent-replacement/closed-full-extension.log`. The formerly failing NPC integration, heterogeneous real material delivery and source-owner freshness/cleanup cases all passed in that complete run. Final scoped strict TypeScript (`final-owner-typecheck.log`), kernel TypeScript (`kernel-typecheck.log`), runtime build (`owner-build.log`) and diff whitespace checks passed. The earlier 19 transitive host diagnostics remain outside this scoped check and are not claimed fixed. All probe/play artifacts and earlier failed logs remain.

Delivered scope: enabled Jev preload replaces the old optional selector, uses the shared evidence-agent runtime, and delivers a validated keeper_support v1 packet with fixed material slots, attributed evidence, coverage/gaps and advisory ordinary-check parameters. Existing enablement and credentials remain in place. The reusable lookup/classify request purposes do not yet expose new public Keeper tools. Complex source retrieval still sometimes returns partial, and the small real probes do not establish general quality or speed improvement. No commit, push, App packaging/restart, memory update or parallel agent delegation was performed.

## Keeper-initiated indexing integration

User-authorized follow-up: wire the Keeper's later active indexing. Success is an actual normal Pi lookup call invoking the shared Jev evidence loop and returning keeper_support v1 to the next Keeper request, with no second retrieval implementation. Existing direct lookup/recall, source authoring, admission and settlement remain with their owners. The selected public seam is lookup kind=support with one evidence query; it can retrieve history through the existing memory owner without replacing semantic recall's narrower contract. Explicit lookup uses configured Jev even when automatic preload is off. Check advice always binds the actual player declaration, never the Keeper query. Contract: section 124.9. Starting green gate: 2386/2386.

Design cross-check: Haystack PipelineTool (https://docs.haystack.deepset.ai/docs/pipelinetool) supports exposing an existing reusable workflow through a small tool interface. Anthropic's tool/agent guidance (https://www.anthropic.com/engineering/writing-tools-for-agents and https://www.anthropic.com/engineering/building-effective-agents) supports explicit descriptions and grounding every iterative step in tool observations. This implementation reuses our bounded typed runtime and exact owner content instead of adopting either external framework or free-form subagent generation. Codebase-design skill used for the shared module/interface seam. No subagents, new dependencies, package, push or commit.

Active lookup implementation is wired through the normal lookup tool, canonical capability check, shared prepareKeeperSupport implementation, and ordinary result serialization. The automatic wrapper still binds player text; the explicit adapter binds the lookup query while mechanical advice remains bound to actual player text. Five negative/positive adapter cases, the real Pi next-provider-context test and canonical dispatch/system-language cases pass: 22/22 in final-focused.log; existing prescreen/check/schema regressions pass 119/119. No new provider configuration or automatic-preload enablement is required for an explicit configured lookup. Live and full-suite closeout are pending.

Active lookup evidence: the actual Pi conformance test sends lookup support with automatic preload disabled, receives schema-valid owner material, observes the exact returned JSON in the following converted provider context, then successfully uses direct module lookup and recall. The separate actual Jev probe (`.tmp/jev-active-lookup/provider/result.json`, explicitly not gameplay) returns two exact Knott source units in 3,392 ms, three evidence-loop rounds, partial/finish_partial, and no_roll advice bound to the separate actual player declaration. It executes zero mutation RPCs. No completeness or comparative-speed claim is made.

Canonical live compatibility: `.coc/playtests/jev-active-lookup-20260922/`, fresh pregen with Grok Build 4.7 fast / low, main session as sole player, one natural turn and no replay. The Keeper delivered in 74.7 s with source lookup, module lookup, apply and narrate; it chose direct reads and did not call the new support entry. Thus this is normal-play compatibility evidence, while explicit-entry execution is proven by the real Pi conformance and real-Jev probe above. The owned daemon was stopped after delivery; all evidence remains.

Runtime build, kernel typecheck and shared support/prescreen strict typecheck pass. Expanding strict checks to kernel/index and its transitive imports reports 38 diagnostics both before and after this change, with zero added diagnostics (`type-comparison.json` compares the exact preserved starting sources through a read-only TypeScript compiler host). This broader host check is not a green gate and unrelated diagnostics were not repaired. Full extension closeout follows.

Active lookup closeout: **2392/2392 extension tests passed**, exit 0, 252.7 seconds, zero skips/cancellations, in `.tmp/jev-active-lookup/full-extension.log`. Build, kernel types, shared strict types and diff whitespace checks passed; the broader host check retains exactly the 38 starting diagnostics with none added. All source, checks, execution details, partial real-Jev results and the direct-read live turn are retained. The new entry is `lookup {kind:"support",query:"..."}`; ordinary seven-verb behavior and exact lookup/recall remain intact. No package, restart, commit, push or memory update was performed.

## User-requested scoped commit

The user authorized a local commit after active lookup completion. The candidate includes the handed-over Jev material foundation, agent loop, fixed support schema, check advice and active Keeper lookup. Shared context/workspace/doc hunks are staged without the separate NPC preparation integration, item-identity work, admission changes, Grok provider changes or packaging/driver changes. Their complete working files remain untouched; the staged Jev context uses the existing standalone NPC path from HEAD. The prior 2392-case result covers the full shared working tree. An ordinary source export of the exact candidate tree is built and tested separately so that this commit does not rely on those unstaged lanes. This is an index projection, not a worktree or a source rollback. Validation follows below.

Scoped candidate validation: tree `562ee07eee10b216ba565ae37f9b97d3906d9246` was exported without unrelated lanes. Runtime build and kernel types passed; source-answer pytest passed **7/7**. The complete extension run passed **2372/2374**; the two failed entries were validation-environment issues, not modified assertions: Git-backed contract discovery needed the candidate index, and nesting the export inside the original repo loaded an extra 24 KB of ancestor project instructions, putting the fixed system checkpoint above a 96 KiB test ceiling. The unchanged contract gate then passed **3/3** with the read-only candidate index, and the unchanged complete long-campaign file passed **5/5** from an ordinary source export outside that ancestor tree. An earlier Node 22 invocation failed native ABI loading and is excluded; actual validation uses the project Node 24 and the existing read-only frozen oracle. Logs and both exports are retained under `.tmp/jev-scoped-commit/` and its recorded standalone path. Only this closeout note changes after the tested code tree. The commit is source-only and does not establish packaged-App or broad semantic/performance acceptance.
