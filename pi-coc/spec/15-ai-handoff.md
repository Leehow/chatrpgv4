# 15 — AI handoff

Assignment template:

```text
Repository/ref:
Work package:
Read first:
Allowed paths:
Forbidden paths:
Requirement IDs:
Expected behavior:
Required tests:
Non-goals:
```

Completion report:

```text
Work package:
Result: complete | partial | blocked
Changed files:
Requirements satisfied:
Behavior implemented:
Tests added:
Validation commands and results:
Evidence artifacts:
Compatibility notes:
Known limitations:
Spec deviations proposed:
Next work package:
```

First recommended assignment: `WP-030`. Bind the complete graph artifact to the validator/index before implementing every mechanic.
