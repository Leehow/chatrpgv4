{
  "name": "pi-coc-public-bootstrap",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build",
    "validate": "npm run build"
  },
  "devDependencies": { "typescript": "^5.8.3" },
  "engines": { "node": ">=22" }
}
