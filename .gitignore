.DS_Store
pdf/
.coc/
__pycache__/
.pytest_cache/
node_modules
web/frontend/dist/
.venv/
runtime/adapters/pi/node_modules
runtime/adapters/narrator/node_modules
checks/ocr-cached/
checks/py4llm-cached/
node_modules.noindex/
.pi-subagents/
.pi/

# Generated local evidence and runtime scratch — retain on disk, never stage
/artifacts/
/.tmp/
/.playwright-cli/
/driver.pid
/gui-test-screenshots/*.png

# Large local evidence archives — keep on disk, not in git
archives/**/*.tar.gz

# Local scratch / playtest workspaces — do not commit
tmp/
.scratch/

# Live playtest evidence: retained in the workspace, never committed (see
# docs/status/module-pipeline-unification-stage-b.md). Hundreds of MB per run.
.rpc-evidence-*/
# External rulegraph reference pack — design reference only, never committed
docs/coc7-core-rulegraph-v0.1/
