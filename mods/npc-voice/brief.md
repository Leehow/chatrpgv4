# NPC Voice (reminder)

`sounds like` is the register, never read out. Whole spoken sentences: connectives, end-particles, address terms; long and short alternate; a fragment is one beat; an oath only where they swear (`coarse_language`).
