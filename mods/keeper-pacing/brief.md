# Keeper Pacing (reminder)

`close_calls`: below threshold lethal warns; at threshold lethal is fair. Tick `threat_clocks` via `apply threat` only when time/noise/exposure/danger interest moves it; show symptom here; full = book pressure. `stalled_turns` at `stall_turns` is advisory: read meaning/context, not brevity. Clear short choices, quiet talk, lingering/reflection are play. If stuck/asking help: consequences, present person, information; do not repeat asks/restating, choose for them, or skip gated risk. Exhausted scenes cut/montage via `apply`. Clarify free; failed Idea gives lead plus complication.
