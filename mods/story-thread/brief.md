# Story Thread (reminder)

Critical first; offers are not quotas. `here`: gates; `next`: perceivable routes; `handed`: no invented checks; `fallback`: unreachable only. Named checks keep skill/difficulty. Choose unspecified skills from source/context; unspecified checks are neither waived nor extra rolls. Costs need source, rules or chosen actions. Acquired connections remain useful: lookup continuity/recall. Clarify only evidenced links; keep unknown causes unknown. `reentry` present: the current `player_text` discharges it only by explicitly demonstrating the selected core connection and its present stakes, or informed refusal at that level (understanding the core claim and stakes, then knowingly declining) — refusing a commission, clue, route, destination, NPC request, or authored hook without that connection is not such a refusal; if truly discharged, continue normally. Otherwise act on `mode` first (physical location never decides it): `clarify_known` — the thread already has acquired `known` evidence and no earlier assessment recorded `bridge_delivered` true; use one already acquired `known` row, state how it supports or contradicts the selected claim and why it matters now, continue the chosen action, open no adaptation and invent/deliver no new clue. `introduce_evidence` — `known` is empty, or an earlier assessment recorded `bridge_delivered` true and the player is still misframed/detached; realize the one supplied `bridge`: if `authority.clue_here` is false, prepare/accept `source_rebinding` before asserting the carrier arrived (a `new_destination` scene's initial no-clue description is creation-time graph state, not a permanent prohibition — a later reviewed `source_rebinding` is the authorized way to add an existing clue's delivery relation, and never create a flag/ruling/note/adaptation forbidding the bridge or freezing the scene clue-free); once it is true, check the current `player_text` — if the player explicitly chose to receive/read/examine it, settle only the exact clue and/or source handout receipt the chosen action needs, then state the relation and current stakes without bundling extra item transfer, elapsed reading time, movement or another unchosen commitment; if the player did not choose it, force no receipt and instead narrate a `bridge_offer` — put the exact carrier within reach, state its bearing on the selected claim and why the choice matters now, then stop with the choice open, claiming no taking/reading/accepting/spending/believing/acting and minting no receipt (the audit treats this as defer); after admission refuses a bundled/unchosen acquisition, honor it and use the `bridge_offer` path when authority is true, without resending the batch or substituting unrelated pacing. Never invent a new clue/fact or substitute an analogous incident, atmosphere, detached recap, route name or menu; keep existing clue/source/receipt and reviewed adaptation/source_rebinding authority; never choose the response, override informed refusal, force an authored scene, retry a declined offer, or repeat a delivered bridge.
