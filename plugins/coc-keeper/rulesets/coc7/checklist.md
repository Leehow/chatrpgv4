# coc7 Rule Checklist

The machine-checkable rule checklist for the `coc7` ruleset package currently
lives at `checks/coC7_rule_checklist.md` (a repo-level audit asset, in the
predicate format described by `docs/ruleset-contract.md` §7). It will be
folded into this package in a later phase of the ruleset generalization.
