# SL-52 wording probe, pre-registered 2026-09-25 before the runs

The after arm (`sl52-longgate6-t1-after`, commit 925176f10, per-row question "does the declared action seek to get, find,
reach or learn it") answered the keys row `no` at 0.82–0.88 in 3/3 runs: the accept files only the leads, as before.

This arm changes only the per-row question's instructions to the route `need` question's frame: the declared action, or
its direct consequence in the current fiction, gets, finds, reaches or learns the thing. Same fixture, flags and seed.

Outcomes, written before the runs:
- A: keys `yes` cleared in >= 2/3 runs -> the clerk files leads, keys, then the move (the ruling's intent on this turn).
- B: keys not cleared in >= 2/3 runs -> the wording does not carry the accept's consequence; the ask question cannot file
  the keys on this sentence, and the finding goes back to the owner.
Also recorded, not scored: the commission / Macario rows at the office (over-filing), and the morgue's
`globe-fire-cutoff` row (filed by the margin rule at 0.29–0.46 in the after arm).

## Result (appended after the runs)

Outcome **B**, 3/3. The keys row answered `no` at 0.81–0.82 (yes 0), and the leads row too (`no` 0.42–0.45, yes 0.19–0.24),
so the compile selected nothing and the recorded Keeper did the accept and the move itself. The consequence wording is worse
than the committed "seek" wording (which clears the leads 3/3) and was reverted; the committed arm is `sl52-longgate6-t1-after`.
