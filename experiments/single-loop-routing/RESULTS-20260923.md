# Results — turn 3 of the haunting table, 2026-09-23

Fixture: campaign `game-9456da03-779d-4dc3-acc2-ebf4bb7c06b9`, starter the-haunting, reset to sidecar commit `4891552` (after turn 2). Player input (turn 3): 「行，先去报馆。」我拿上帽子出门，直接去《环球报》剪报室，找认识的老同行帮忙翻科比特宅这些年的旧报道。

## Live baseline (installed build 67a281c3e, Keeper grok-4.7-build-fast)

6 LLM calls, 122.5 s of model time, 147.3 s wall. Actions, in order: `apply move → newspaper-morgue` (25 min) · `apply person Arty Wilmot` · `resolve first-impression (Arty)` · `resolve Persuade vs Arty (bonus die)` · `apply person Ruth Blake` · `resolve first-impression (Ruth)` · `apply clue globe-unpublished-story + clue macario-tragedy + handout + time 40` · `narrate`.

## Final runs (after the changes below)

Host-only mode (no LLM; the run stops where the LLM would be called):

| run | wall | steps | Jev calls | Jev ms | host executed | stopped at |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 6.0 s | read, route, move, read, route, infer | 17 | 4.9 s | move → newspaper-morgue | `ask_llm` (0.88) |
| 2 | 4.2 s | same | 7 | 3.1 s | same | `ask_llm` |
| 3 | 3.9 s | same | 7 | 2.9 s | same | `ask_llm` |

Full replay (the recorded Keeper messages replayed as the LLM step, minus what the host already did):

| run | wall | LLM steps (adjudicate + compose) | Jev calls | host executed | model-origin executed | matches live |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 9.0 s | 4 + 1 | 21 | move | person Arty, first-impression Arty, Persuade, person Ruth, first-impression Ruth, clues+handout+time | 8/8 + narrate |
| 2 | 7.0 s | 4 + 1 | 11 | move | same | 8/8 + narrate |
| 3 | 6.2 s | 4 + 1 | 10 | move | same | 8/8 + narrate |

Reading: the loop reproduces the live turn exactly; the host carried out the declared move before any LLM call; the four remaining LLM steps are the Keeper's scene craft (staging the gatekeeper and the archivist, deciding a Persuade is needed, revealing the clips). On this turn the loop saves one LLM call (6 → 5). Run 1's extra Jev calls are the semantic locate judging all 271 cards; runs 2–3 reuse it.

## Route answers on the final shape (run 3 of the replay)

| step | offered | exit | needs judged `now` |
| --- | --- | --- | --- |
| route 1 (Knott's office, 4 materials) | 8 | `ask_llm` 0.57 / `continue` 0.37 | move → newspaper-morgue **0.79** (selected) |
| route 2 (morgue, 6 materials) | 15 | `ask_llm` 0.88 | person Arty 0.46, person Ruth 0.32 — below gate |
| route 3 (after Arty staged + met) | 13 | `ask_llm` 0.74 | none |
| route 4 (after Persuade) | 13 | `ask_llm` 0.65 / `finish` 0.18 | none |
| route 5 (after Ruth staged + met) | 11 | `ask_llm` 0.55 / `finish` 0.21 | none |
| route 6 (after clues + handout) | 8 | `finish` 0.49 / `ask_llm` 0.31 | none (the handout, already placed by the model's batch, is no longer offered) |

## The path here (what each change did)

| runs | change | move → newspaper-morgue | outcome |
| --- | --- | --- | --- |
| 1 | pick-one over 52 candidates (all rule decisions offered) | chosen at 0.53 (< gate 0.6) | LLM |
| 2 | only live rule families (12 candidates) | 0.43 | LLM |
| 3 | + margin gate | 0.36 | LLM |
| 4 | (variance) | 0.45, margin 0.49 vs 0.18 | **executed**; next route spread 0.20/0.15/0.13 over three steps the Keeper took → LLM |
| 5–7 | fan-out (`need_N` + `exit`) | `later` 0.52–0.65, exit `ask_llm` 0.76–0.80 | LLM |
| 8–10 | criteria reworded as bookkeeping; dropped the kernel's `authority` tag and `met: false` moves | `now` 0.52–0.58, exit `continue` 0.53–0.57 | below gate |
| 11–13 | read first, then route | `now` 0.76–0.81, conf 0.64–0.72 | **executed** every run |
| 14–16 | re-read after a scene change | same; morgue route with morgue material | persons 0.40–0.47, `ask_llm` 0.80–0.86 |
| 17–19 | replay LLM step | model-origin calls refused: `call_id` must be `t<turn>-c<n>` | fixed: one ordinal for host and model |
| 20 | shared ordinal | 8/8 actions match; the handout was shown twice (candidate not consumed by the model's batch) | fixed: model-origin applies consume host keys |
| final | as above | 8/8, 3/3 runs | — |

## Misses and risks recorded (do not tune these away by hand)

- **Scene craft is not host-issuable.** Who meets the investigator at the morgue and that it costs a Persuade are not facts the kernel or the Mod states; Jev correctly says `ask_llm`. The design's saving depends on how much of a scene's demands are data (pending contacts, declared gatekeeper checks) rather than Keeper improvisation. This is the main open question for SL-02.
- **`now`/`later` for a person is ~50/50** even with the morgue material: staging the gatekeeper is "part of the declared action" only in the Keeper's reading.
- **Jev's absolute confidence is low on multi-way questions**; the margin gate is a prototype parameter, not a product threshold. Every distribution is in the traces.
- **The read step is the product prescreen** (7 Jev calls on a cold locate, 2–3 warm, 0.9–2.5 s). In the loop it is the dominant Jev cost; its reuse cache is what makes runs 2–3 cheap.
- **Ordinary checks were never routed by Jev on this turn**: the ordinary-check candidate sat at ≤ 0.22 `now` because the input declares no check; the binder (route/profile) was therefore not exercised here. It is exercised in `loop.test.mjs` with a stub.
- Not tested: a turn whose declared action is itself a check ("我搜这间屋子"), pending player choices, combat/chase sessions, cancellation mid-run, and the LLM's context projection size (recorded as `context_projection_bytes` only).

Traces: `results/final-host-only/` and `results/final-replay/` (per run: `runN.trace.jsonl`, `runN.summary.json`).

---

# SL-02 — the product driver (pre-registration written 2026-09-23, before the scored runs)

Everything above this line is the prototype's record and is unchanged. This section belongs to SL-02 of
`docs/specs/pi-native-single-loop.md`. Its scored results are appended below the pre-registration after the runs.

## Instrument

`run.mjs --llm replay` now runs `product-entry.ts`, which exercises the **product driver**, not the prototype's
`runTurn`. The pieces are:

- A real Pi `AgentSession` from the vendored build (`build/node_modules`), with `PI_COC_LOOP_ENGINE=hybrid-v1` and
  the product's `createHybridEngine`. That engine brings the step policy, the read step with the product
  prescreen, the host-issued candidates, Jev decisions, clerk writes through the kernel extension's canonical
  gateway, and the `coc-clerk` projection.
- The kernel, onboarding, module, memory and table extensions, over the emitted kernel on a disposable copy of the
  fixture, reset to the fixture's pre-turn commit. Jev is live, with the key read from the App's vault. The Mods
  extension is not loaded: neither fixture has queued Mod work at its pre-turn commit (`mods.queued` is empty for
  both, checked 2026-09-23). The clerk's Mod hooks are asserted at the extension seam instead
  (`single-loop-domain-policy.test.mjs`).
- No language model is called. The Keeper's provider is a replay:
  - each adjudicate step answers with the live Keeper's next recorded message, minus the calls the run already
    carried out (read from the Keeper verbs that succeeded this run);
  - a bind step answers with the live call that binds the chosen operation (the effect with the same key, or the
    resolve with the same decision and actor or target), and if none exists it answers like an adjudicate step;
  - the compose, or a step with no recorded call left, answers with the live turn's accepted delivery. That is its
    last recorded `narrate`/`ask` that the live kernel took, or else the delivered narration from the fixture's own
    turn record at `commit_after`.
  - A recorded call the live kernel refused is not replayed. In the fight round that call is the first `ask`
    (`options` carried `none`, which the kernel refused as `invalid_params`). The replay measures the loop, not
    that Keeper slip.
- The §32 review lane is a replay too. It answers each review with the live table's recorded verdict for the same
  verb, in order (`--admission lane`, the product default). `--admission jev` puts §32.10's typed reviewer first,
  live, and uses the replayed lane only as its fallback.

## Instrument-development runs (before this registration; not scored)

These runs were made while the instrument and the engine were being built. They are listed because they came
before the registration.

- turn3 `try1`: all 6 LLM steps were model-origin; the move went to Jev as "later" 0.73. Cause: the product read
  step's summary and material previews differed from the prototype's, and the route question lost the locate's
  own judgment (`newspaper-morgue` noul 0.97) under "done this turn". Fixed by giving the read step the
  prototype's summary shape and preview.
- turn3 `try2`: the clerk moved, and then the replay's Keeper moved again. Cause: the replay read "done" from
  custom messages the provider never receives. Fixed by reading it from the Keeper verbs that succeeded.
- turn3 `try3`: 8/8 matched, 5 LLM steps (4 adjudicate + compose); the move was clerk-origin.
- fight `fight1`: player's attack by the clerk (route "now" 0.86). NPC's defence: Jev bind answered `unknown`
  0.53. NPC's counter-attack: a route question, "later" 0.53 → LLM. 3 LLM steps.
- Changes after `fight1`, from the parameters-only ruling ("closed choices ... are decide(bind); the LLM is asked
  only when the player's words leave the target or weapon open, or for the prose"):
  - an NPC's own turn became a forced closed bind over its issued actions, not a route question about the
    player's words;
  - the player's declared session action became keyed once per turn;
  - session candidates carry the kernel's own view of the fight as detail;
  - defence options carry their rule meaning;
  - the bind question's wording no longer assumes the player chooses (an NPC's own choice is judged "for that
    person in the current situation").
- fight `fight2`, `fight3`: NPC's defence `unknown` 0.67 / 0.71 → LLM bind. NPC's counter-attack bound by Jev
  (`combat:attack` 0.81 / 0.69) and run by the clerk. The ask was the delivering step. 2 LLM steps.

## Pre-registered outcomes (3 runs per fixture per arm)

**Turn 3** (`fixtures/turn3`, `--admission lane`):

- 8/8 live actions matched in each run: move, person Arty, first-impression Arty, Persuade, person Ruth,
  first-impression Ruth, clues and handout (time as well).
- ≤ 5 LLM steps in each run.
- Every route answer is in `lane: "route"`, with each question's probabilities.
- The move is clerk-origin (`origin: "policy"`) in each run where Jev selects it; the prototype had 3/3.
- The run ends `undelivered` in the run events, because the compose is prose and the kernel extension's implicit
  narrate delivers it. That is SL-03's delivery evidence, expected and not scored.

**Fight round** (`fixtures/fight-round`, turn 6 of the "打斗测试" table, player input "继续揍他"; the player names
his target, the only opponent):

- The ticket's outcome, registered as the hypothesis: *the round closes with one LLM step (the compose), and every
  non-prose step carries the kernel's issued row as its basis.*
- My prediction, from the three development runs: **2 LLM steps**:
  - the player's attack by the clerk (basis `context.session.actions[0]`, `target` and `weapon` issued with one
    value each);
  - the NPC's defence: Jev answered `unknown` in 3/3 development runs, so an LLM bind;
  - the NPC's counter-attack: a Jev bind over its issued actions, then the clerk;
  - the player's own defence: handed back by the Keeper's `ask`, the delivering step.
- Scoring: the hypothesis is **confirmed** if a run closes with ≤ 1 LLM step, and **refuted** for a run with more.
  Per run the report states which steps were LLM steps and why.
- Basis: every clerk-origin tool row carries `basis`, with a kernel row from `table.resolve.options`. The operation
  the LLM completed carries it on its `lane: "run", event: "llm_bound"` row.

**§32 research item** (both fixtures, `--admission lane` ×3 and `--admission jev` ×3):

- For every policy-origin (clerk) write, recorded: whether §32.1 exempts it, the verdict, the reviewer, ms, the
  typed confidence and any fallback. Also: whether the typed verdict agrees with the live lane's verdict for the
  same proposal, and any refusal.
- Nothing is decided here. The numbers go to the ticket's Comments, and the decision is the owner's.

## Scored runs (after the pre-registration commit `bbf75a033`)

Traces are in `results/sl02-<fixture>-<arm>/` (`runN.trace.jsonl`, `runN.summary.json`, `summaries.json`). An
"LLM step" is one model step of the run (`step_start kind: infer`). "Clerk" means `origin: "policy"`.

### Turn 3, product driver

| arm | run | wall | LLM steps | Jev calls | move | live actions matched | first route: move `now` (gate 0.6 + margin) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| lane | 1 | 9.4 s | 5 (4 adjudicate + the prose close) | 19 | clerk, `t3-c1` | 8/8 (+ time, + narrate) | 0.67 |
| lane | 2 | 7.2 s | 5 (4 + compose) | 9 | clerk, `t3-c1` | 8/8 | 0.81 |
| lane | 3 | 7.1 s | 5 (4 + compose) | 9 | clerk, `t3-c1` | 8/8 | 0.77 |
| jev | 1 | 10.9 s | 5 (4 + compose) | 19 | clerk, `t3-c1` | 8/8 | 0.74 |
| jev | 2 | 8.6 s | 5 (4 + the prose close) | 9 | clerk, `t3-c1` | 8/8 | 0.76 |
| jev | 3 | 9.0 s | 5 (4 + the prose close) | 9 | clerk, `t3-c1` | 8/8 | 0.80 |

**The outcome: as registered.** 6/6 runs matched 8/8 with 5 LLM steps, the move was the clerk's each time, and
every route answer is in `lane: "route"` with probabilities. After the move, the route question at the morgue
(15 candidates) answered `ask_llm` 0.73–0.78 in every run. The persons and the contact checks were "later". This is
the prototype's finding again: staging the gatekeeper and the archivist is the Keeper's scene craft. The last
route answered `finish` below the gate in every run (0.23–0.32). In three runs the Keeper's closing prose came
from an `adjudicate` step, and in three from a `compose` step. The count is the same either way. Every run ended
`undelivered` in the run events: the prose close is delivered by the implicit narrate, whose evidence is SL-03's.
Live, the same turn took 6 LLM calls, 122.5 s of model time and 147.3 s wall.

### Fight round, product driver ("继续揍他", turn 6 of the 打斗测试 table)

| arm | run | wall | LLM steps | player's attack | NPC's defence | NPC's turn | delivery |
| --- | --- | --- | --- | --- | --- | --- | --- |
| lane | 1 | 6.6 s | 2 | clerk (route `now` 0.94), basis `context.session.actions[0]` | Jev `unknown` 0.54 → LLM bind | Jev `combat:attack` 0.73 → clerk | Keeper `ask` (adjudicate step) |
| lane | 2 | 4.9 s | 2 | clerk (0.93) | `unknown` 0.58 → LLM | `combat:attack` 0.74 → clerk | `ask` |
| lane | 3 | 4.6 s | 2 | clerk (0.94) | `unknown` 0.66 → LLM | `combat:attack` 0.68 → clerk | `ask` |
| jev | 1 | 6.5 s | 2 | clerk (0.90) | `unknown` 0.63 → LLM | `combat:attack` 0.71 → clerk | `ask` |
| jev | 2 | 5.3 s | 2 | clerk (0.90) | `unknown` 0.56 → LLM | `combat:attack` 0.67 → clerk | `ask` |
| jev | 3 | 5.3 s | 2 | clerk (0.90) | `unknown` 0.60 → LLM | `combat:attack` 0.71 → clerk | `ask` |

**Ticket hypothesis ("one LLM step"): refuted, 0/6.** The run was as predicted (2 LLM steps) in 6/6. Asked for
the NPC's defence (dodge, fight back or none) with the fight as the kernel shows it, Jev answered `unknown` every
time. That is its honest answer to "the NPC's free choice among options the kernel cannot rank", which the spec's
ruling sends to the LLM. Jev did bind the NPC's own next action every time, and the clerk ran it. The player's
own defence opened during the run, so the Keeper handed it back with `ask`. Live, the same turn took 5 LLM
calls, 56.6 s of model time and 82.5 s wall; replayed it takes 2 LLM steps. All 3 live actions matched in 6/6.

**Basis: partly as registered.** Every clerk step carried the kernel's issued row: the player's attack and the
NPC's attack both have `basis.path` `context.session.actions[0]`, with the row itself. The operation the LLM
completed, the NPC's defence, did **not** get its `lane: "run", event: "llm_bound"` row in any scored run. The
engine wrote that row from the `llm_proposal` slot, and the policy removes that slot when the LLM answers with
the call. Fixed after these runs: the row is now written at the bind step itself (projection port), and the
kernel row stays out of the model's note. The re-run is pre-registered below.

### The §32 research item (numbers; the decision is the owner's)

Policy-origin (clerk) writes in the 12 scored runs: 18.

- **Exempt by §32.1: 6.** These are the NPC's attack in every fight run. The actor is not an investigator, so
  there is no review and no admission row.
- **Reviewed: 12.** The move in 6 turn-3 runs and the player's attack in 6 fight runs. **Refused: 0 of 12**,
  under both reviewers.

| clerk write | reviewer | verdict | typed verdict / confidence | typed ms | lane |
| --- | --- | --- | --- | --- | --- |
| move → newspaper-morgue (turn 3), ×3 | lane (replayed) | authorized | — | — | live lane on the Keeper's same move: authorized, 2 109 ms |
| move → newspaper-morgue, ×3 | jev → lane fallback | authorized | authorized 0.88 / 0.86 / 0.89 (under the 0.9 family minimum) | 367 / 313 / 368 | fallback `low_confidence` |
| player's attack on Knott (fight), ×3 | lane (replayed) | authorized | — | — | live lane on the Keeper's same attack: authorized, 18 250 ms |
| player's attack on Knott, ×3 | jev → lane fallback | authorized | authorized 0.48 / 0.56 / 0.49 | 371 / 356 / 376 | fallback `low_confidence` |

For comparison, the model-origin reviews in the jev arm (turn 3, 4 per run, 12 in all): typed confidence
0.15–0.44, every one a fallback. The typed line verdict disagreed with the live lane verdict on 7 of the 12:
`not_authorized` on the first impression with Ruth 3/3, on the handout line of the clue batch 3/3, and once on the
first impression with Arty.

**What the numbers say, and what they do not.**

- No clerk write was ever refused, and the typed verdict class matched the live lane's in 6/6.
- The typed reviewer cleared its own family minimum on none of the 12. The moves came within 0.02–0.04 of it; the
  attacks were at 0.48–0.56.
- The cost the reviews would carry live: the lane reviews took 2.1 s and 18.3 s on the live table; the typed
  review costs 0.31–0.38 s.
- The lane arm's verdicts are the live lane's verdicts on the live Keeper's proposals, replayed. The clerk's own
  proposal wording differs: the clerk's move carries no `label`, `via` or travel minutes. So the lane arm says
  what the lane decided for the same action, not for the clerk's exact lines. Only the typed arm judged the clerk's
  exact proposal.
- n is 2 distinct clerk writes (one move, one attack), each seen 6 times. That is enough to show the instrument
  works and to put the sizes side by side, not enough to set a threshold.

## Re-run after the `llm_bound` fix (pre-registered before running)

Fight round, `--admission lane`, 3 runs, on the commit that carries this paragraph. Expected:

- unchanged: 2 LLM steps (the NPC-defence bind, then the delivering `ask`), the same clerk steps with their basis,
  and 3/3 live actions matched;
- new: one `lane: "run", event: "llm_bound"` row per run for `resolve:combat:defend:steven-knott:investigator:r3`,
  with `basis.path` `context.session.pending_defense`;
- the `coc-clerk` note the Keeper receives contains no `basis`.

**Re-run result (`results/sl02-fight-round-lane-rerun/`): as registered, 3/3.** Each run has 2 LLM steps
(`bind`, then the `ask`) and 3/3 live actions matched. The clerk steps are `t6-c1` (the player's attack) and
`t6-c3` (the NPC's attack), each with `basis.path` `context.session.actions[0]`. Each run has one `llm_bound` row
for `resolve:combat:defend:steven-knott:investigator:r3` with `basis.path` `context.session.pending_defense`. The
replay does not keep the Keeper's request text, so the absence of `basis` from the `coc-clerk` note is shown by
the seam test (`tests/extension/single-loop-domain-policy.test.mjs`, the IntentBinding/authority case), not by
these traces. Wall 5.8–6.7 s; Jev calls 6–10.

---

# SL-07 — the NPC's standing defence (pre-registration written 2026-09-23, before the scored runs)

Everything above this line is SL-02's record and is unchanged. This section belongs to SL-07
(`docs/specs/pi-native-single-loop-tickets/07-npc-standing-defense.md`, contract §11.5.2). Its scored results are
appended below the pre-registration after the runs.

## What changed since SL-02's runs

- The kernel issues `pending_defense.standing: {defense, basis}` for an NPC defender (§11.5.2): the Keeper's
  override, else the NPC record's authored `combat.defense`, else the rules default (`fight_back` when the
  engine's `combat_skill` is at least `dodge_skill`, otherwise `dodge`; a firearm reads `fight_back` as `dodge`).
- The candidate builder binds an NPC's pending defence to that standing: a `direct` clerk step, no Jev `decide`,
  no LLM `infer`. Its `basis` carries `standing`.
- `ask kind: mechanics` accepts `none`. A handout already handed over is not a candidate (`shown`, from
  `handouts_shown`).
- **Instrument change (before this registration, for the reason given):** the replay identified a resolve by its
  decision and `target`. A defence has no target the kernel reads: the live Keeper's defence carried
  `target: "investigator"` (the attacker), the clerk's carries none. Without the change the replay would re-send
  the live Keeper's defence after the clerk's (a second defence against no pending attack, refused by the kernel)
  and the baseline match would call the defence unmatched. `product-entry.ts` now identifies `combat:defend` by
  its defender (`actor`) in both places, and the per-run match row reports the defence word the run used.

## The fixture's NPC (read before the runs)

Steven Knott in `fixtures/fight-round` at `commit_before` (`1df8561`): no authored `combat.defense` (his record in
the fixture's module copy, `.coc/modules/the-haunting/module-graph.json`, has no `combat` field and no printed
profile), and the table wrote no override (`world.npc_defense` is absent). His numbers are the archetype the Keeper
pinned on turn 3 (`world.npc_profiles`: no Fighting skill, Dodge 32), and his combat participant in
`save/combat.json` has `combat_skill: 25`, `dodge_skill: 32`. So the standing is **`dodge`, basis `rule-default`**. The live Keeper chose
`fight_back` for him on turn 6; the replay will not follow that choice, because the standing is now the kernel's.

## Pre-registered outcomes: fight round (3 runs, `--admission lane`, product driver)

Player input "继续揍他" (he names his target, the only opponent). §11.5.1 (the investigator's standing defence) is
not on this branch, so the player's own defence is still handed back by the Keeper's `ask`.

- **Hypothesis (the ticket's):** the round closes on **one** LLM step, the Keeper's `ask` for the player's defence.
  Scoring per run: **confirmed** with ≤ 1 LLM step (`step_start kind: infer`), **refuted** with more; each run
  states which steps were LLM steps and why.
- **Predicted steps, in order:** read → route (the player's attack `now`) → clerk `t6-c1` (the player's attack,
  basis `context.session.actions[0]`) → clerk `t6-c2` (Knott's defence, `defense: "dodge"`, basis
  `context.session.pending_defense` with `standing: {defense: "dodge", basis: "rule-default"}`) → Knott's turn: Jev
  binds `combat:attack` (SL-02: 6/6) → clerk `t6-c3` (basis `context.session.actions[0]`) → the player's defence
  opens during the run → one LLM step, the Keeper's `ask`.
- **No Jev question and no LLM step about Knott's defence:** no `single-loop-bind` batch for
  `resolve:combat:defend:*`, and no `lane: "run", event: "llm_bound"` row for it.
- **Other paths, registered so they cannot be read favourably afterwards:** if the player's punch drops Knott
  (HP 4), the fight ends, Knott gets no turn, and the one LLM step is the prose close (still ≤ 1). If Jev answers
  `unknown` on Knott's own turn, that is an LLM bind and the run counts 2 LLM steps: **refuted** for that run, and
  the cause is recorded as the NPC's action choice, not his defence.
- **Basis:** every clerk tool row carries its kernel row; the defence row's `basis.standing.basis` is
  `rule-default` in every run.
- **The shown handout:** the fight fixture's `handouts_shown` already holds Handout 1 (Knott's commission, in the
  scene of the fight); SL-02's route questions offered it again, and these must not.
- **Live actions:** the baseline's three resolves (the player's attack, Knott's defence, Knott's attack) matched by
  decision and who, 3/3 per run where Knott gets his turn; the defence word is `dodge` (the live Keeper's was
  `fight_back`).

## Pre-registered outcomes: turn 3 (regression, 3 runs, `--admission lane`)

As SL-02 registered and found: 8/8 live actions per run, ≤ 5 LLM steps, the move clerk-origin, every route answer in
`lane: "route"`. What the handout change touches here, read from the fixture before the runs: turn 3 starts in
Knott's office with Handout 1 (Knott's commission) already in `handouts_shown`, and SL-02's first route offered it
again; now the first route has one candidate fewer. The morgue's handout (the Globe's held story) is not shown at
the start, so it is still offered there, and the live Keeper places it in its clue batch (model-origin) as before.
The first route's answer on the move is a prediction (`now` above the gate, as in SL-02's 6/6), not a certainty:
the question changes by one candidate.

## SL-07 scored runs (after the pre-registration commit `a20bf74d5`, on that commit's tree)

These runs were made on `a20bf74d5`, before `0.9.5a` (with §11.5.1) was merged into the branch. They score the
registration above as written. The merged state is registered and scored separately below.

Traces are in `results/sl07-<fixture>-lane/` (`runN.trace.jsonl`, `runN.summary.json`, `summaries.json`). "LLM
step" and "clerk" mean what they mean above.

### Fight round, product driver ("继续揍他")

| run | wall | LLM steps | player's attack | Knott's defence | Knott's turn | delivery | Jev calls |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 6.0 s | 2 (`bind`, `adjudicate`) | clerk `t6-c1` (route `now` 0.86), basis `context.session.actions[0]` | clerk `t6-c2`, `dodge`, basis `context.session.pending_defense`, `standing: {dodge, rule-default}` | Jev `combat:attack` 0.49 → below the gate → LLM bind → `t6-c3` (model) | Keeper `ask` | 9 |
| 2 | 4.5 s | 2 (same) | clerk (0.86) | clerk, `dodge`, `rule-default` | Jev `combat:attack` 0.57 → LLM bind | `ask` | 4 |
| 3 | 4.5 s | 2 (same) | clerk (0.87) | clerk, `dodge`, `rule-default` | Jev `combat:attack` 0.53 → LLM bind | `ask` | 4 |

**Ticket hypothesis ("one LLM step"): refuted, 0/3.** Each run spent 2 LLM steps: the bind of Knott's own action
and the `ask`. That is the registered alternative path, with one correction to its wording: Jev did not answer
`unknown` on Knott's turn; it answered `combat:attack` below the 0.6 gate (0.49 / 0.57 / 0.53, with `unknown` at
0.31–0.37 of the distribution), and the policy sends a low-confidence bind to the LLM exactly as it sends
`unknown`. In SL-02's registered re-run the same question answered `combat:attack` at 0.72–0.73 (`unknown`
0.17). What changed before that question is Knott's defence (the clerk's `dodge` by his standing, where SL-02's
LLM bind replayed the live Keeper's `fight_back`), so the fight Jev was shown differs; the bind batch's state is not
kept in the trace, and this record does not say which part of it moved the answer.

**What SL-07 set out to change: as registered, 3/3.**

- Knott's defence was the clerk's in every run: `t6-c2`, `defense: "dodge"`, no `single-loop-bind` question about
  it, no `llm_bound` row for it (the one `llm_bound` row per run is `resolve:combat:turn:steven-knott:r4`, Knott's
  own action).
- Its tool row carries `basis.path: "context.session.pending_defense"` and `basis.standing: {defense: "dodge",
  basis: "rule-default"}`: the fixture NPC has no authored tactic and no override, and `combat_skill` 25 <
  `dodge_skill` 32.
- Every clerk tool row carries its kernel row (`t6-c1` `context.session.actions[0]`, `t6-c2`
  `context.session.pending_defense`).
- The shown handout: SL-02's re-run offered Handout 1 in both route questions of every run (2 per run); these
  runs offered it in none.
- Live actions: the baseline's three resolves matched 3/3 per run (Knott's attack model-origin this time); the
  defence word is `dodge` where the live Keeper wrote `fight_back`. The delivery is the recorded `ask` the live
  kernel took (options `dodge` / `fight_back`); the first recorded `ask`, with `none`, was refused live and is not
  replayed, so `none` at the replay seam is shown by the kernel test and the extension seam test, not here.
- Admission: the player's attack was reviewed (`authorized`, replayed lane); Knott's defence is exempt by §32.1
  (an NPC actor), as SL-02's NPC attack was.

### Turn 3, product driver (regression)

| run | wall | LLM steps | Jev calls | move | first route: offered / move `now` | live actions matched |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 9.6 s | 5 (5 adjudicate) | 19 | clerk | 7 / 0.76 | 8/8 (+ time, + implicit narrate) |
| 2 | 7.4 s | 5 | 9 | clerk | 7 / 0.84 | 8/8 |
| 3 | 7.7 s | 5 | 9 | clerk | 7 / 0.70 | 8/8 |

**As registered, 3/3.** The first route offered 7 candidates where SL-02's offered 8: the missing one is Handout 1
(Knott's commission), already in `handouts_shown`. The move stayed above the gate (0.70–0.84; SL-02 0.67–0.81).
The morgue's handout was still offered in the routes after the move (3 offers per run) until the Keeper's clue
batch placed it, as registered. Every run ends `undelivered` in the run events, as in SL-02 (SL-03's delivery
evidence).

## SL-07 on the merged state (pre-registration written before the scored runs)

The branch merged `0.9.5a` at `618667e9b`, which brings §11.5.1 (`d8e116ead`): the investigator's defence is
settled by the host from the campaign preference (`dodge` by default) at the pending-attack boundary, and a defence
`ask` is refused as `stale_choice`. The ticket's acceptance line becomes: the round closes on **one** LLM step, the
compose, with the player's defence carried by §11.5.1 and the NPC's by its standing.

**Instrument change (declared before these runs).** The replay's compose answered with the live turn's last
accepted delivery, which for this fixture is a defence `ask` (options `dodge` / `fight_back`). Under §11.5.1 that
call is refused, so the replay would measure a refusal the live Keeper is now told never to make. `product-entry.ts`
replays a recorded delivery `ask` whose every option is a defence word as `narrate` with the same text. Nothing
else changed.

**Development run (not scored).** One fight-round run on the merged state, made while writing the change above:
2 LLM steps (the bind of Knott's own action, then the step that closed with the narrate), status `delivered`.

**Registered outcomes, 3 runs per fixture, `--admission lane`:**

- Fight round, the hypothesis: ≤ 1 LLM step per run confirms it, more refutes it. My prediction from the
  pre-merge runs and the development run is **2 LLM steps**: the one for Knott's own action (Jev's `combat:attack`
  has sat under the 0.6 gate, 0.49–0.57), and the prose close. The prediction does not change the scoring.
- Knott's defence: the clerk's, `defense: "dodge"`, `basis.standing: {dodge, rule-default}`, with no bind question
  and no `llm_bound` row about it, in 3/3.
- The player's defence against Knott's counter-attack: the host's, one `lane: "standing-defense"` row with
  `ok: true`, no `ask`. If Knott gets no turn, there is no such row, and that is recorded.
- No offer of the already-shown Handout 1.
- Turn 3: 8/8 live actions, ≤ 5 LLM steps, the move clerk-origin, every route in `lane: "route"`, and Handout 1
  not offered.

### Merged state, scored (`3452d3766`; traces in `results/sl07-merged-<fixture>-lane/`)

**Fight round:**

| run | wall | LLM steps | Knott's defence (clerk) | Knott's own turn | player's defence | delivery |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 6.9 s | 2 (`bind`, `adjudicate`) | `t6-c2` `dodge`, `standing {dodge, rule-default}` | Jev `combat:attack` 0.57 → below gate → LLM bind | host `t6-c4` `dodge` (§11.5.1) | narrate, `delivered` |
| 2 | 4.7 s | **1** (`adjudicate`) | `t6-c2` `dodge`, `rule-default` | Jev `combat:attack` 0.60 → clerk `t6-c3` | host `t6-c4` `dodge` | narrate, `delivered` |
| 3 | 4.9 s | 2 (`bind`, `adjudicate`) | `t6-c2` `dodge`, `rule-default` | Jev 0.57 → LLM bind | host `t6-c4` `dodge` | narrate, `delivered` |

**The hypothesis ("one LLM step"): confirmed in 1 of 3, refuted in 2 of 3.** Every run spent the prose close.
The runs that spent a second step spent it on Knott's own action: Jev picked `combat:attack` at exactly the
gate's edge (0.57, 0.60, 0.57), and the run went to the LLM when the pick fell below the gate. As registered, 3/3:

- Knott's defence was the clerk's by its standing, with no bind question and no `llm_bound` row about it.
- The player's defence was the host's, through §11.5.1: one `standing-defense` row, `ok: true`, no `ask`.
- Every clerk row carried its kernel row.
- Handout 1 was not offered.
- All three live resolves matched, with the defence word `dodge`.

The prose close ran as an `adjudicate` step, not a `compose`. The route after the player's defence answered
`ask_llm`, and the replayed Keeper answered with the narrate. It is one LLM step either way.

**Turn 3:**

| run | wall | LLM steps | Jev calls | move | first route: offered / move `now` | live actions |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 14.8 s | 5 | 19 | clerk | 7 / 0.73 | 8/8 (+ time, + implicit narrate) |
| 2 | 8.6 s | 5 | 9 | clerk | 7 / 0.76 | 8/8 |
| 3 | 7.9 s | 5 | 9 | clerk | 7 / 0.76 | 8/8 |

As registered, 3/3. Handout 1 was not offered. The morgue's handout was offered after the move until the
Keeper's batch placed it (3 offers per run). Every run ended `undelivered` in the run events, as in SL-02; that is
SL-03's delivery evidence.

---

# SL-08 — the NPC's standing action and combat disposition (pre-registration written 2026-09-23, before the scored runs)

Everything above this line is SL-02's and SL-07's record and is unchanged. This section belongs to SL-08
(`docs/specs/pi-native-single-loop-tickets/08-npc-standing-action.md` with its "Extension", contract §11.5.3).
Its scored results are appended below the pre-registration after the runs.

## What changed since SL-07's merged runs

- On an NPC's own turn (no attack pending) the kernel issues `session.standing_action: {action, basis,
  disposition?, read?}`: a live Keeper override (`apply npc action`, `attack` or `hold`), else the record's authored
  `combat.action` (`attack`), else the ruleset table `npc-combat-disposition.json` over the NPC's **combat
  disposition** and the fight's state (hit-point fraction, outnumbered, stance). No disposition, no rules default.
- The disposition: a Keeper override (`apply npc disposition`), else the record's authored `combat.disposition`,
  else one Jev inferred once for the campaign from the NPC's own parameters (`agenda`, `fear`, `secret`, `voice`,
  `relationship_to_investigators`, and a first impression natural-npc settled).
- The candidate builder: an NPC's turn without a standing and without a disposition reads that NPC's card and makes
  the inference a forced closed bind (one Jev question); above the gate the clerk writes it (`basis: "inferred"`),
  below it the Keeper is asked to complete the write. A standing `attack` makes the attack the forced step (direct
  with one target and one weapon); `hold`/`flee` issue nothing for the NPC and put `npc_turn` in the Keeper's note.
- The stance the table reads folds the open turn; a combat defence's rolls now name the NPC defender, so a fight
  defended in its own resolve makes him hostile.
- **No instrument change.** `product-entry.ts` is as SL-07 left it.

## The fixture's NPC (read before the runs)

Steven Knott in `fixtures/fight-round` at `commit_before` (`1df8561`):

- no `combat` field on his record in the fixture's module copy, so no authored action or disposition; `world.json`
  has no `npc_action` and no `npc_disposition`;
- his own parameters in that copy: agenda "Clear the Corbitt House's reputation so he can rent or sell…", fear
  "Being stuck with a worthless haunted white elephant…", secret "He knows only rumor and the Macario tragedy…",
  voice "Practical, impatient, money-minded…", relationship_to_investigators "employer"; natural-npc's first
  impression of turn 0: `{reaction: "strongly_favorable", disposition: "helpful"}`;
- fight state: HP 4 of 10 (0.4) before the player's punch this turn, one against one (not outnumbered), stance
  `hostile` since turn 5 (ledger).

So Knott's turn begins with the **inference**. Per the table, with HP ≤ 0.4: `fights_to_the_end` → `attack`;
`fights_then_flees` → `flee` (≤ 0.5); `avoids_fighting` → `flee` (≤ 0.75); `surrenders` → `hold` (≤ 0.75).

**Development run (not scored), declared:** one fight-round run on the branch at `f1bdffb44` (the first merge of
0.9.5a; the card's field was still `combat_action`, renamed `combat_standing` in `f3ee3418b`; the scored runs are on
`45e3901e9`, after the RD-02 merge). Jev answered the disposition question `fights_then_flees` at confidence
0.25 (probabilities: `fights_then_flees` 0.40, `unknown` 0.31, `avoids_fighting` 0.25, `surrenders` 0.04,
`fights_to_the_end` 0.00): below the gate, so the Keeper was asked to complete the write (an LLM `bind`). The
replay's Keeper can only replay the live Keeper's calls, and the live Keeper never wrote a disposition: its bind
answer was "not recorded", and it answered with the next recorded call instead (Knott's attack, model-origin).
Then the prose close (`adjudicate`). 2 LLM steps, delivered.

## Pre-registered outcomes: fight round (3 runs, `--admission lane`, product driver)

- **Hypothesis (the ticket's):** the round closes on **one** LLM step. Scoring per run: **confirmed** with ≤ 1 LLM
  step (`step_start kind: infer`), **refuted** with more; each run says which steps were LLM steps and why.
- **My prediction: refuted, 2 LLM steps in each run**, for the reason the development run showed: Knott's own
  parameters say little about how he fights, Jev's disposition answer falls below the 0.6 gate, and the Keeper is
  asked (an LLM `bind`). The prediction does not change the scoring.
- **Predicted steps, in order:** read → route (the player's attack `now`) → clerk `t6-c1` (the player's attack) →
  clerk `t6-c2` (Knott's defence, `dodge`, standing `rule-default`) → Knott's turn: the card read, then one Jev
  question (`single-loop-bind`, key `disposition`, candidate `apply:npc-disposition:steven-knott`, basis row
  `read` = the five parameters plus `first_impression`) → below the gate: LLM `bind` (the replay answers with
  Knott's recorded attack, model-origin; no disposition is written) → the host settles the player's defence
  (§11.5.1) → the prose close (1 LLM step).
- **Other paths, registered so they cannot be read favourably afterwards:**
  - Jev clears the gate with `fights_to_the_end`: the clerk writes it (`basis: "inferred"`), the next read issues
    `standing_action {attack, rule-default}`, the clerk attacks directly (basis names the standing and the
    disposition), the host settles the player's defence, and the prose close is the one LLM step: **confirmed**.
  - Jev clears the gate with `fights_then_flees` / `avoids_fighting` (→ `flee`) or `surrenders` (→ `hold`): the clerk
    writes it (`basis: "inferred"`), the standing issues no step for Knott, and the next step is the Keeper's with
    `npc_turn` in its note. The replayed Keeper cannot honour it: its next recorded call is Knott's attack, which it
    will send (the Keeper may resolve any issued action). Score the LLM steps as they fall; record that the replay
    cannot show the Keeper narrating the flight or the surrender.
  - The player's punch drops Knott (HP 4): no Knott turn, no inference; the prose close is the one step.
- **Reported per run:** Knott's disposition and its basis (`inferred`, or none written), the Jev distribution for the
  disposition question, the standing action if one was issued, every clerk tool row with its kernel row
  (`basis.path`), and the three live actions matched.

## Pre-registered outcomes: turn 3 (regression, 3 runs, `--admission lane`)

No fight on that turn, so no NPC turn, no card read, no standing: as SL-07 registered and found — 8/8 live actions
per run, ≤ 5 LLM steps, the move clerk-origin, every route answer in `lane: "route"`, Handout 1 not offered.

## SL-08 scored runs (on `45e3901e9`, after the pre-registration commit `6de789115`)

Traces are in `results/sl08-<fixture>-lane/` (`runN.trace.jsonl`, `runN.summary.json`, `summaries.json`).

### Fight round, product driver ("继续揍他")

| run | wall | LLM steps | player's attack | Knott's defence | Knott's disposition question (Jev) | Knott's action | player's defence | delivery |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 10.0 s | 2 (`bind`, `adjudicate`) | clerk `t6-c1`, basis `context.session.actions[0]` | clerk `t6-c2` `dodge`, `standing {dodge, rule-default}` | `fights_then_flees` 0.31 (p 0.45; `avoids_fighting` 0.27, `unknown` 0.24, `surrenders` 0.04, `fights_to_the_end` 0) → below gate → LLM `bind` | model-origin attack (the replay's next recorded call) | host, §11.5.1, `ok` | narrate, `delivered` |
| 2 | 9.4 s | 2 (same) | clerk | clerk `dodge`, `rule-default` | `fights_then_flees` 0.31 (p 0.44 / 0.25 / 0.26 / 0.05 / 0) → LLM `bind` | model-origin attack | host, `ok` | narrate, `delivered` |
| 3 | 9.0 s | 2 (same) | clerk | clerk `dodge`, `rule-default` | `fights_then_flees` 0.24 (p 0.39 / 0.27 / 0.29 / 0.05 / 0) → LLM `bind` | model-origin attack | host, `ok` | narrate, `delivered` |

**The hypothesis ("one LLM step"): refuted, 0/3, as predicted.** Every run spent the prose close plus one LLM
`bind`: the inference of Knott's combat disposition. The question was asked exactly as registered: one
`single-loop-bind` question, key `disposition`, candidate `apply:npc-disposition:steven-knott`, basis `{read:
"table.look", path: "combat_disposition", row: {npc: "steven-knott", read: ["agenda", "fear", "secret", "voice",
"relationship_to_investigators", "first_impression"]}}`. Jev leaned to `fights_then_flees` (0.39–0.45 of the
distribution) but reported confidence 0.24–0.31, below the 0.6 gate, with `unknown` at 0.24–0.29, so the Keeper was
asked to complete the write. The replayed Keeper never wrote a disposition on the live table, so its bind answer is
"not recorded" and it answered with its next recorded call, Knott's attack (model-origin). **Knott's disposition:
none written; basis: none** (had it been written as Jev leaned, `fights_then_flees` at HP 0.4 reads `flee`). No
standing action was issued, so no `npc_turn` note appeared.

As registered, 3/3: the player's attack and Knott's defence were the clerk's, each clerk tool row carries its kernel
row (`t6-c1` `context.session.actions[0]`, `t6-c2` `context.session.pending_defense` with `standing {dodge,
rule-default}`); the player's defence against Knott's attack was the host's (§11.5.1, one `standing-defense` row,
`ok`); Handout 1 was not offered; the three live resolves matched 3/3 per run. Jev calls 9 / 4 / 4.

What moved: in SL-07's merged runs the question the round spent was "does Knott attack" (Jev `combat:attack` at
0.57–0.60, cleared once in three). It is now "what kind of fighter is Knott", asked once per campaign; on this
fixture his own parameters (a practical, money-minded landlord and employer, first impression helpful) do not tell
Jev, and it says so. On a live table the Keeper writes it once and every later turn of Knott's reads the table
without a model step; one replayed turn cannot show that.

### Turn 3, product driver (regression)

| run | wall | LLM steps | Jev calls | move | first route: offered / move `now` | live actions |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 14.0 s | 5 | 19 | clerk | 7 / 0.73 | 8/8 (+ time, + implicit narrate) |
| 2 | 10.7 s | 5 | 9 | clerk | 7 / 0.71 | 8/8 |
| 3 | 10.5 s | 5 | 9 | clerk | 7 / 0.76 | 8/8 |

As registered, 3/3: no fight, no card read, no disposition question (0 bind questions per run), Handout 1 not
offered. Every run ends `undelivered` in the run events, as in SL-02 and SL-07.

---

# SO-04 — scene obligations as the clerk's candidates (pre-registration written 2026-09-23, before the scored runs)

Everything above this line is unchanged. This section belongs to SO-04 of
`docs/specs/scene-obligations-as-candidates-tickets.md` (spec D6, owner rulings Q1–Q7; contract §135.26). Its scored
results are appended below the pre-registration after the runs, on the commit that carries this paragraph.

## What changed since SL-08

- Product (`runtime/jev/obligation-candidates.ts`, `candidates.ts`, `step-policy.ts`, `hybrid-engine.ts`, the kernel
  extension's refusal accounting): an open obligation's `meet` step is the stated person candidate (replacing the roster
  one; its name binds as the roster's: the table's own label, else open), its `check` step an `obligation_check`
  candidate (closed Jev bind over the approaches, the ordinary binder's dice words and the intent); guarded rows withheld;
  `blocked` issues nothing; Arty's Mod first impression withheld from the clerk (`reaction: "preordained"`); precedence
  `person → mod_check → obligation_check → core-check → clue/handout → move`; the operation carries `basis.obligation`;
  "clerk did" names the obligation step, receipt and page; a refused clerk step hands the turn to the Keeper and stays
  off the Keeper's refusal budget.
- Fixture: `fixtures/turn3-obligations/`, derived by `fixture.mjs --derive turn3-obligations --from turn3`. Only the
  module slice differs: `module.register the-haunting` over the source workspace, with this worktree's `content/`
  (starter commit `e2b9520c6`, SO-01's morgue pair), registered generation 3, graph digest `cfc354e1…0d96`; the 369 files
  outside `.coc/modules/` are checked byte-identical before the tarball is written (`variant.json`). `turn.json` and
  `baseline.json` are byte copies. The source tarball and `fixtures/turn3/*` are unchanged (sha256 checked before and
  after: tarball `26f4689e…1aea`).
- Instrument (`product-entry.ts`), written before this registration:
  - `--seed <n>` sets `COC_KERNEL_SEED`, which the kernel subprocess inherits; each summary records it.
  - A bind step's replayed answer carries the note's bound values over the recorded call, as the note asks ("with the
    bound values as given and the needed parameters filled in"). Before, the recorded call went out unchanged, so an
    `infer(bind)` of the obligation check would have lost `action.obligation`.
  - The replayed admission verdicts are paired with the live call each review judged (by the decision or skill and
    target, or an effect's target, read as strings in the review request), not handed out in order. In order, the
    clerk's claimed Persuade — a review the live table never had — took the verdict meant for Ruth's first impression,
    which was then refused for want of one.

## Instrument-development runs (before this registration; not scored)

20 runs on `turn3-obligations`, `--admission lane`, seeds 1 (×4), 2, 3, 5–19; traces are in the session scratchpad, not
committed. Every run was the same shape:

- Route at Knott's office: the move `now` 0.73–0.74, the clerk's (`t3-c1`), as in SL-02–SL-08.
- Route on arrival at the morgue: the stated meeting with Arty **`later` 0.48–0.72 in 20/20**; exit `ask_llm`. The first
  label wording ("… here as the first step of …") drew `later` 0.61; the committed wording ("… in the way of … whoever is
  after … meets Arty Wilmot first") drew `later` 0.59–0.72. Ruth's roster candidate `later` 0.59–0.70.
- The replayed Keeper then staged Arty and rolled his first impression (message 1 of the live turn) and, in its next
  step, its recorded Persuade — without `action.obligation`, which the live Keeper (pre-SO-02) never sent — so the
  obligation stayed open.
- The next route selected the obligation check `now` 0.56–0.69 (above the gates by the margin rule) in 20/20; in 9/20 an
  earlier route had answered it `later` 0.91–0.93. Jev bound it (skill `Persuade` 0.83–0.95, bonus `one` 0.84–0.89,
  penalty `none` 0.78–0.85, intent `social` 0.87–0.93) and the clerk rolled it with the claim: **a second Persuade against
  Arty in the same turn.** It passed under every seed but 19.
- After the gate settled, the released clues were `later` 0.77–0.91 and the archivist's stated meeting `later` 0.60–0.85;
  the replayed Keeper carried Ruth, her first impression, the clues, the handout and the time.
- 5 LLM steps (all `adjudicate`) in 20/20; the live actions all matched once the admission pairing was fixed.

## Pre-registered outcomes (3 runs per arm, `--admission lane`, product driver)

Arms: **pass** — `turn3-obligations`, seed 1 (the clerk's roll passed in 4/4 development runs under it); **fail** —
`turn3-obligations`, seed 19 (the clerk's roll failed in 1/1); **regression** — the original `turn3`, seed 1 (generation 2
states no obligation, so nothing of SO-04 issues).

The spec's lines (Testing Decisions, "Replay"; ruling Q6), each with my prediction from the development runs:

1. *Route 2 selects the stated meeting with Arty and, after it, the gatekeeper's check as `now` above the gates in 3/3.*
   Predicted **not met** in both arms: the meeting `later` in 6/6; the check `now` above the gates in 6/6, but after the
   replayed Keeper staged Arty, not after a clerk meeting.
2. *A passing roll closes in ≤ 3 LLM steps (target 2).* Predicted **not met**: 5 in 3/3 of the pass arm.
3. *A failing roll is reported as the Keeper improvising past an open obligation, not scored.* Predicted in the fail arm:
   the obligation stays `open` after the clerk's failed roll with the failure book line on the result; the replayed Keeper's
   calls for Ruth and the clippings run model-origin, and the `apply clue` result carries `obligation_open` (a crossing).
4. *8/8 live actions by kind, family, skill and target, with the clerk's Persuade consuming the recorded one.* Predicted:
   every live row matched (move, person Arty, first impression Arty, Persuade, person Ruth, first impression Ruth, both
   clues, the handout; time and the implicit narrate as well), but the recorded Persuade is matched by the replayed
   Keeper's own unclaimed roll, and the clerk's claimed Persuade is an extra roll — so **"the clerk's Persuade consuming the
   recorded one": not met**.
5. *Every route distribution retained.* Predicted **met**: every route and bind answer in `lane: "route"` with its
   probabilities.
6. Regression (`turn3`, seed 1): as SL-08 registered and found — the nine live rows matched, 5 LLM steps, the move
   clerk-origin, no `stated_obligation` candidate, every route in `lane: "route"`.

Why 1, 2 and 4 are predicted to fail (for the owner; nothing here is tuned away):

- **The stated meeting's name is open.** §135.2 (SL-02): a person without the table's own label is staged under a name
  only the LLM can write. Arty and Ruth have none at turn 3, so each stated meeting, when Jev does select it, is an
  `infer(bind)` — an LLM step. The spec's expected "2" assumed the host carries the meetings.
- **The replayed Keeper predates SO-02.** It never claims the obligation, so its Persuade settles nothing and the clerk
  rolls the claimed check again; it also rolls Arty's first impression, which ruling Q2 takes off the clerk but not off the
  Keeper. Each of its recorded messages costs one LLM step.
- **Jev judges the meeting `later`** under both wordings tried; the spec's premise was that a stated candidate turns the
  prototype's ~50/50 into `now`.

## SO-04 scored runs (on `a9b107e6d`, the pre-registration commit)

Traces in `results/so04-turn3-obligations-pass/`, `results/so04-turn3-obligations-fail/`, `results/so04-turn3-regression/`
(`runN.trace.jsonl`, `runN.summary.json`, `summaries.json`; each summary records its seed). "LLM step" is one
`step_start kind: infer`; "clerk" is `origin: "policy"`. "Live rows" are the baseline's eleven rows (move, person Arty,
first impression Arty, Persuade, person Ruth, first impression Ruth, two clues, the handout, time, the implicit narrate).

| arm (seed) | run | wall | LLM steps | Jev calls | stated meeting (Arty) | obligation check | Jev bind | clerk's roll | Persuades vs Arty | live rows |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pass (1) | 1 | 14.3 s | 5 (5 adjudicate) | 21 | `later` 0.66 | `now` 0.58 (margin) | Persuade 0.90, one 0.87, none 0.82, social 0.92 | regular, +1 die: **settled** | Keeper extreme (unclaimed), clerk regular | 11/11 |
| pass (1) | 2 | 11.8 s | 5 | 11 | `later` 0.63 | `now` 0.68 | Persuade 0.94, one 0.87, none 0.82, social 0.87 | regular, +1: settled | Keeper, clerk | 11/11 |
| pass (1) | 3 | 18.3 s | 5 | 11 | `later` 0.68 | `now` 0.55 (margin) | Persuade 0.93, one 0.88, none 0.84, social 0.92 | regular, +1: settled | Keeper, clerk | 11/11 |
| fail (19) | 1 | 14.8 s | 5 | 21 | `later` 0.61 | `now` 0.60 | Persuade 0.94, one 0.87, none 0.85, social 0.93 | failure, +1: **open**, book + push lines | Keeper failure, clerk failure | 11/11 |
| fail (19) | 2 | 11.4 s | 5 | 11 | `later` 0.65 | `now` 0.51 (margin) | Persuade 0.94, one 0.87, none 0.85, social 0.88 | failure: open | Keeper, clerk | 11/11 |
| fail (19) | 3 | 17.3 s | 5 | 11 | `later` 0.66 | `now` 0.62 | Persuade 0.94, one 0.86, none 0.82, social 0.90 | failure: open | Keeper, clerk | 11/11 |
| regression `turn3` (1) | 1–3 | 11.7 / 8.8 / 9.7 s | 5 / 5 / 5 | 19 / 9 / 9 | roster `later` 0.63 / 0.63 / 0.58 | none offered | — | — | Keeper only | 11/11 |

Against the registration:

1. **Stated meeting and check `now` in 3/3: not met, as predicted.** The meeting was `later` (0.61–0.68) in 6/6; the
   check was `now` above the gates in 6/6 (four by the margin rule), each time after the replayed Keeper had staged Arty.
2. **Passing roll ≤ 3 LLM steps: not met, as predicted** — 5 in 3/3 (the SL-02–SL-08 count on the original fixture is
   also 5). The clerk's roll passed and settled the gate in 3/3; the released clues and the archivist's stated meeting
   were `later`, and the replayed Keeper carried them.
3. **Failing roll (seed 19), reported, not scored:** the clerk's claimed Persuade failed in 3/3; the obligation stayed
   `open` and the result carried the book's failure and push lines; the replayed Keeper staged Ruth, rolled her first
   impression and landed both clippings, the handout and the time — the Keeper improvising past an open obligation —
   and that `apply` result carried `obligation_open: globe-clippings-access` in 3/3 (a crossing the kernel reports and
   never refuses, ruling Q5).
4. **Live rows:** 11/11 in 9/9 runs. **"The clerk's Persuade consuming the recorded one": not met, as predicted** — the
   recorded Persuade is matched by the replayed Keeper's own unclaimed roll; the clerk's claimed Persuade is a second roll
   against Arty in the same turn in 6/6.
5. **Every route distribution retained: met.** Every route and bind answer is in `lane: "route"` with probabilities
   (6 route rows and 1 bind row per obligation run, 5 route rows per regression run).
6. **Regression: met.** 11/11 rows, 5 LLM steps, the move clerk-origin, no obligation candidate offered in any route.

What the runs show, and what they do not:

- Everything SO-04 issues reached Jev and the kernel as designed on the real product path: the stated meeting in the
  roster's place, Arty's Mod first impression withheld from the clerk, the guarded clues withheld until the gate
  settled and offered after, the obligation check with its closed binder (Jev bound all four questions above the gate
  in 6/6, with one bonus die, where the live Keeper also gave one), the clerk's `resolve` with `action.obligation`
  through admission and the kernel (settling the flag on a pass, the book line on a failure), and the crossing reported.
- The saving did not arrive on this turn, for the three reasons registered. The second Persuade is the one that matters
  at a live table: a Keeper that rolls the book's check without `action.obligation` leaves the obligation open, and the
  clerk then offers and rolls it again. The replayed Keeper is pre-SO-02 and never claims; a live Keeper told by the
  capsule row and the base prompt may, but nothing in the loop stops the double roll if it does not.
- Not shown: a live table (the SL-02/SL-05 gate, owed); a Keeper that claims the obligation itself; a table where Arty
  and Ruth already carry the table's own label (then the stated meetings bind without the LLM).

After 0.9.5a was merged in twice (`570e2aed8`: the prose-delivery turn close and SL-11's candidate bodies on the same
run), SO-04's contract section is §135.26 and the obligation check carries a body. One unscored replay per arm on that
state (seed 1, seed 19) had the same shape: 5 LLM steps (the last one now a compose), the clerk's claimed Persuade
settling the gate under seed 1 and leaving it open under seed 19, every live row matched.

## SO-04 after the owner's rulings (pre-registration written 2026-09-23, before the scored runs)

The rulings (recorded in `docs/specs/scene-obligations-as-candidates.md` Comments; implemented in `82bf990ee`,
`1abdf65ac`; contract §134.9 `then`, §135.2's exception, §135.26):

1. a stated meeting is data: it is staged under the table's own label if the kernel issued one, else under the person's
   record name, with no LLM step;
2. `now` on the obligation check carries its meeting: the check is offered while the meeting is owed (built from the
   row's new `then`), and `now` runs the meeting directly first, then binds and rolls the check; the meeting is not a
   routed candidate on its own. A meeting-only obligation (the archivist) stays routed, now also under the book's name.

Same fixture (`turn3-obligations`), same instrument (`product-entry.ts` as at `a9b107e6d`, merged with SL-11's live-Keeper
options, which the replay arm does not use), `--admission lane`, 3 runs per arm: **pass** seed 1, **fail** seed 19. No
development runs were made on the new rule before this registration.

Registered (the coordinator's line), with my prediction:

1. **The meeting with Arty carried directly in 3/3** — the clerk's `apply person` under "Arty Wilmot", with no Jev
   question and no LLM step for it. Predicted met, provided line 2 holds.
2. **The check `now` above the gates in 3/3**, on the route at arrival. Uncertain: in the earlier runs the check was
   `now` 0.51–0.68 only once Arty was staged, and `later` 0.91–0.93 in 9/20 routes before that; now the label says
   "meet Arty Wilmot first, then …", so I predict `now` at arrival in at most 2/3 per arm.
3. **Passing roll ≤ 3 LLM steps.** Predicted not met. Even with lines 1–2 met, the replayed Keeper (pre-SO-02) still has
   recorded messages the clerk does not cover: Arty's first impression (preordained, off the clerk), and — when the
   archivist's meeting, Ruth's first impression, the clues and the handout are `later` — Ruth and the clippings, then the
   time; each recorded message is one LLM step. Predicted 4–5.
4. **Failing roll:** reported, not scored, as before (obligation open with the book's lines; the Keeper's clues cross it).
5. Live rows 11/11 per run; every route distribution retained.

If line 2 fails in a run, the Keeper stages Arty as before (its recorded apply now carries his Chinese label), the check is
offered after, and that run is reported as such.

## SO-04 after the rulings, scored (on `58d5a36e6`)

Traces in `results/so04-ruled-pass/` (seed 1) and `results/so04-ruled-fail/` (seed 19).

| arm | run | wall | LLM steps | Jev calls | arrival route: check (carrying the meeting) | Arty staged by | later route: check | Jev bind | clerk's roll | live rows |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pass | 1 | 13.3 s | 5 | 21 | `later` 0.89 | Keeper (`城市版编辑`) | `now` 0.60 | Persuade 0.94, one 0.86, none 0.79, social 0.89 | settled | 11/11 |
| pass | 2 | 11.0 s | 5 | 11 | `later` 0.90 | Keeper | `now` 0.61 | Persuade 0.90, one, none, social | settled | 11/11 |
| pass | 3 | 10.5 s | 5 | 11 | `later` 0.87 | Keeper | `now` 0.55 (margin) | Persuade 0.93, one, none, social | settled | 11/11 |
| fail | 1 | 12.9 s | 5 | 21 | `later` 0.90 | Keeper | `now` 0.59 (margin) | Persuade 0.96, one, none, social | open; Keeper's clues crossed | 11/11 |
| fail | 2 | 10.7 s | 5 | 11 | `later` 0.91 | Keeper | `now` 0.54 (margin) | Persuade 0.95, one, none, social | open; crossed | 11/11 |
| fail | 3 | 10.8 s | 5 | 11 | `later` 0.91 | Keeper | `now` 0.61 | Persuade 0.94, one, none, social | open; crossed | 11/11 |

Against the registration:

1. **Meeting carried directly in 3/3: not met — 0/6.** Nothing carried it, because line 2 failed: the check that carries
   the meeting was `later` at arrival in 6/6 (0.87–0.91, tighter than the old meeting's 0.61–0.68). The replayed Keeper
   staged Arty as before.
2. **Check `now` above the gates in 3/3 at arrival: not met — 0/6.** It was `now` in 6/6 on the next route, once Arty was
   on stage (0.54–0.61, four of six by the margin rule), as in the earlier runs.
3. **Passing roll ≤ 3 LLM steps: not met** — 5 in 3/3, as predicted in direction (predicted 4–5).
4. Failing roll, reported: the obligation stayed open with the book's lines; the Keeper's clue batch crossed it
   (`obligation_open`) in 3/3.
5. Live rows 11/11 in 6/6; every route and bind answer retained with probabilities.

What the numbers say: the rulings removed the meeting's LLM step and its separate question, but Jev does not judge the
book's price as part of this player's declaration ("go to the Globe's clippings room and get an old colleague to help me
dig out the reports") before anyone is in front of him; with Arty on stage it does, by a small margin. The Ruth meeting
(archivist, routed, now under her record name) was `later` 0.67–0.85 and the released clues 0.81–0.90 `later`, so the
replayed Keeper still carried them. Not shown: a table where the declaration names the gatekeeper or the approach; a live
Keeper.

### The same registration on RD-04's starter (fixture re-derived after merging 0.9.5a `b669887c5`)

RD-04 migrated the haunting to mechanical shapes, so `fixtures/turn3-obligations` was re-derived with
`fixture.mjs --derive` from the current starter: generation 3, graph digest `35f98e57…4573` (starter commit
`0ed72381f`); the runs above used `cfc354e1…0d96` (SO-01's starter). The 369 files outside `.coc/modules/` are unchanged
and `fixtures/turn3/*` is byte-identical. The registered lines were re-run unchanged, 3 runs per arm (traces in
`results/so04-ruled-rd04-pass/`, `results/so04-ruled-rd04-fail/`):

| arm | run | wall | LLM steps | Jev calls | arrival: check carrying the meeting | after the Keeper staged Arty | clerk's roll | live rows |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pass (1) | 1 | 16.1 s | 5 (2 adjudicate, 3 compose: Jev budget spent after the bind) | 24 | `later` 0.82 | `now` 0.59 (margin) | settled | 11/11 |
| pass (1) | 2 | 16.6 s | 5 | 17 | `later` 0.85 | `now` 0.51 (margin) | settled | 11/11 |
| pass (1) | 3 | 15.3 s | 5 | 17 | `later` 0.81 | `now` 0.63 | settled | 11/11 |
| fail (19) | 1 | 18.0 s | 5 | 23 | `later` 0.84 | `now` 0.67 | open; Keeper's clues crossed | 11/11 |
| fail (19) | 2 | 15.3 s | 5 | 17 | `later` 0.84 | `now` 0.63 | open; crossed | 11/11 |
| fail (19) | 3 | 12.9 s | 5 | 14 | `later` 0.85 | `now` 0.62 | open; crossed | 11/11 |

Same outcome: meeting carried 0/6 (the check `later` at arrival 6/6), 5 LLM steps on a passing roll, the failing arm's
crossing reported, 11/11 live rows, every distribution retained.

## SO-04 with the obligation routed by a fact about the input (pre-registration written 2026-09-23, before the scored runs)

The ruling (spec Comments, §135.26; implemented in `1511b1601`, `467d81a28`): an obligation candidate's route question
is "is the player's declared action after any of: <what it guards>?" (`seeks` / `not` / `unknown`, the two clippings
clues named with their summaries; the archivist's question names what the gate she follows guards). `seeks` above the
gates carries the meeting under the book's name, binds and rolls the check with its claim, then routes again; any other
answer leaves the obligation to the Keeper for the run. The kernel's guards for the morgue name the two clues only: the
handout is a scene asset no obligation guards, so it is not named (the coordinator's message mentioned it).

Fixture: the current `turn3-obligations` (RD-04's starter, graph digest `35f98e57…4573`). Same instrument, `--admission
lane`, 3 runs per arm, seeds 1 and 19 as before. **The roll order changes under this rule**: if the clerk rolls the claimed
Persuade before the replayed Keeper rolls anything, it is the first roll of the kernel process, not the third, so seed 1
need not pass and seed 19 need not fail. Each run is scored by the clerk's actual roll: a passing run against the
passing-roll line, a failing run reported as the Keeper improvising past an open obligation. No development run was made
on this rule before this registration.

Registered (the coordinator's line), with my prediction:

1. **`seeks` at arrival in 3/3 per arm** — predicted met: the declaration names the clippings room and the old reports on
   the Corbitt House, which is what the two guarded clues are.
2. **The meeting carried in 3/3** — the clerk's `apply person` "Arty Wilmot" under the book's name, no Jev question and no
   LLM step for it. Predicted met where 1 holds.
3. **The check rolled with the claim** (`action.obligation`), after one Jev bind of the approach (Persuade), dice and
   intent. Predicted met where 1 holds; a bind below the gate would add an LLM step and is reported.
4. **Passing roll ≤ 3 LLM steps** — predicted 3–4, met only in part: the replayed Keeper (pre-SO-02) still has recorded
   messages the clerk does not cover — Arty's first impression (preordained, off the clerk), Ruth's first impression
   unless the clerk takes it, and the time advance — and each recorded message is one LLM step.
5. Failing roll: reported (obligation open with the book's lines; the Keeper's clues cross it), not scored.
6. Live rows 11/11 per run (the clerk's claimed Persuade now matches the recorded Persuade, since the Keeper's own
   unclaimed roll no longer comes first); every route distribution retained.

## SO-04 with the seeks/not/unknown question, scored (on `fad63b88c`)

Traces in `results/so04-seeks-seed1/` and `results/so04-seeks-seed19/`.

| seed | run | wall | LLM steps (reasons) | arrival: `seeks` (p) | selected | meeting | check | clerk's / Keeper's roll | live rows |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 1 | 15.7 s | 5 (bind: skill `unknown`; adjudicate ×4) | 0.43 (0.63 vs 0.29) | yes (margin) | clerk, "Arty Wilmot" | Jev bind: skill `unknown` 0.76 → the Keeper filled it; claimed | extreme: **settled** | 11/11 |
| 1 | 2 | 10.4 s | 5 (bind; adjudicate ×3; compose) | 0.52 (0.68 vs 0.25) | yes | clerk | skill `unknown` 0.77 → Keeper; claimed | extreme: settled | 11/11 |
| 1 | 3 | 11.0 s | 5 (bind; adjudicate ×3; compose) | 0.42 (0.61 vs 0.29) | yes | clerk | skill `unknown` 0.77 → Keeper; claimed | extreme: settled | 11/11 |
| 19 | 1 | 15.1 s | 5 (bind; adjudicate ×3; compose) | 0.49 (0.66 vs 0.26) | yes | clerk | skill `unknown` 0.80 → Keeper; claimed | regular: settled | 11/11 |
| 19 | 2 | 9.4 s | 5 (adjudicate ×4; compose) | 0.36 (0.57 vs 0.36) | **no** (below both gates) | Keeper, "城市版编辑" | not issued again this run | Keeper's unclaimed Persuade failed; obligation open; its clues crossed it | 11/11 |
| 19 | 3 | 10.1 s | 5 (bind; adjudicate ×3; compose) | 0.54 (0.69 vs 0.25) | yes | clerk | skill `unknown` 0.77 → Keeper; claimed | regular: settled | 11/11 |

Against the registration:

1. **`seeks` at arrival 3/3 per arm: met as the answer (6/6), not as a selection** — above the gates in 5/6 (all five by
   the margin rule; reported confidence 0.42–0.54, `seeks` 0.61–0.69 against `not` 0.25–0.29). In seed 19 run 2, `seeks`
   0.57 against `not` 0.36 cleared neither gate, so the obligation became Keeper-only for the run.
2. **Meeting carried: 5/6** (where 1 selected it) — the clerk's `apply person` "Arty Wilmot", no Jev question and no LLM
   step for it.
3. **Check rolled with the claim: 5/5 where selected, but not by the clerk.** With Arty just staged by the clerk, Jev
   answered the approach `unknown` (0.76–0.80; bonus `none`, penalty `none`, intent `social` above the gate), so the
   operation went to the Keeper (`infer(bind)`), whose replayed answer was its recorded Persuade with the bound values
   carried over — the claim included — and it settled the gate in 5/5 (the roll order changed as registered: seed 19
   passed in both runs where it was selected). In the earlier runs the same bind came back `Persuade` 0.83–0.96 when the
   replayed Keeper had staged Arty and rolled his first impression before the question.
4. **Passing roll ≤ 3 LLM steps: not met** — 5 in 5/5 passing runs: the approach bind (1), then the replayed Keeper's
   recorded messages the clerk did not cover (Arty's first impression; a failed first-impression roll ends that batch,
   which returns the turn to the Keeper, `batch_fallen`; Ruth, whose `seeks` was 0.20–0.33 and so Keeper-only, with her
   first impression; the clues, handout and time), then the close.
5. **Failing roll:** no claimed roll failed. Seed 19 run 2 had no claim: the Keeper's own unclaimed Persuade failed, the
   obligation stayed open and the Keeper's clue batch crossed it (`obligation_open`); the clerk did not offer the check
   again that run.
6. **Live rows 11/11 in 6/6**, and the recorded Persuade is now consumed by the claimed one in the five selected runs (one
   Persuade per run, no double roll). Every route and bind answer retained with probabilities.

# SL-12 — binding without the LLM (pre-registration written 2026-09-23, before the scored runs)

Everything above this line is unchanged. This section belongs to SL-12
(`docs/specs/pi-native-single-loop-tickets/12-binding-without-the-llm.md`; contract §135.28). Its scored results are
appended below the pre-registration after the runs, on the commit that carries this paragraph.

## What changed since SO-04's seeks runs

- Product (`42e6f1808` on `9a6404aba`'s contract): a clerk candidate never reaches `infer(bind)`. A closed parameter Jev
  does not settle takes its rules default -- the approach is the actor's highest issued profile value among the offered
  approaches (tie: the stated order), the dice words `none` -- stamped `basis.binding: "rule-default"`; a parameter with
  no default (a target, a weapon, an actor, an intent no row declares) hands the candidate to the Keeper as
  `infer(adjudicate)`, reason `clerk_unbound`. A meeting's and a roster person's `why` is composed; a roster person without
  the table's own label, and the player's manoeuvre and ending, are not issued to the clerk. Every clerk write records a
  `lane: "run"`, `event: "bind"` row with each parameter's path.
- Instrument: unchanged (`product-entry.ts` as at `fad63b88c`). The fixtures are unchanged (`turn3-obligations` on RD-04's
  starter, graph digest `35f98e57…4573`; `turn3`; `fight-round`).
- The investigator of both turn-3 fixtures (沈默, `party/investigator.json`): Persuade 70, Fast Talk 50, Intimidate 15,
  Charm 15. The rules default for the gate's approach is therefore Persuade.

No development run was made on the SL-12 code before this registration.

## Pre-registered outcomes (3 runs per arm, `--admission lane`, product driver, replayed Keeper)

Arms: **obligations seed 1** and **obligations seed 19** (`turn3-obligations`, `--seed 1` / `--seed 19`, as SO-04);
**turn3** (the original fixture, `--seed 1`, the regression); **fight-round** (the regression). A run is scored on the
passing-roll line only when the clerk's claimed roll passed; a failing claimed roll is reported, not scored. The roll
order is as in SO-04's seeks runs (the claimed Persuade is the process's first roll), but the claimed roll no longer
carries the replayed Keeper's recorded bonus die (in SO-04 the `infer(bind)` answer carried the live Keeper's
`bonus_dice: 1`; now the dice are Jev's `none` or the default `none`), so a seed need not give the SO-04 result.

1. **The approach bound without a model call in 3/3 per obligations arm, where the check is selected.** Where `seeks`
   clears the gates (SO-04: 5/6), the clerk's `resolve` carries `action.obligation`, `skill: "Persuade"`, and no `infer`
   step with purpose `bind` runs. Predicted: Jev answers the approach `unknown` as in SO-04 (0.76–0.80), so the tool row's
   `basis.binding` is `rule-default` and the `event: "bind"` row's skill path is `rule-default`; if Jev answers Persuade
   above the gate instead, the path is `jev` (reported, still no model call). A run where `seeks` is not selected leaves
   the obligation to the Keeper and is reported as such (not a failure of this line).
2. **Passing roll LLM steps: 4 (from 5).** Predicted 4 on every passing run: Arty's first impression (preordained, off the
   clerk; its failure returns the batch), Ruth with her first impression, the clues + handout + time, then the close. No
   step purpose is `bind`.
3. **Live rows 11/11** in every run, the recorded Persuade matched by the clerk's claimed one.
4. **Every route distribution retained**, and one `event: "bind"` row per clerk write (move, meeting, claimed check).
5. **turn3 (regression), seed 1:** 11/11 live rows, 5 LLM steps (adjudicate/compose, no bind), the move clerk-origin, no
   obligation candidate. Changed and predicted: the route at the morgue no longer offers Arty's or Ruth's roster
   candidate (neither has the table's own label at turn 3, so neither is issued to the clerk); the recorded apply of each
   still stages them.
6. **fight-round (regression):** 5/5 live rows (the player's attack, Knott's standing defence, Knott's attack, the two
   deliveries as recorded), 2 LLM steps. Changed and predicted: the first model step is `adjudicate` (reason
   `clerk_unbound`: Knott's combat disposition inference below the gate, SL-08's 0.34) instead of `bind`; the replayed
   Keeper answers it with the same recorded message (Knott's attack) it answered the bind with.

## SL-12 addendum registration: a passing-roll arm (written after the four registered arms ran, before this arm ran)

The four arms above ran on `846d39439` (results appended below). Neither obligations arm produced a passing claimed roll:
under seed 1 the gate's fact question fell below the gates in 3/3, so nothing was claimed; under seed 19 the check was
selected in 3/3 and its claimed Persuade (no bonus die now) failed in 3/3. The passing-roll line is therefore not scored
by them. This arm is added to score it, and nothing else about the registration changes:

- **obligations seed 4** (`turn3-obligations`, `--seed 4`, 3 runs, same instrument): seed 4 is the kernel seed under which
  the first roll of a process passes a regular Persuade of 40 (`tests/kernel/test_scene_obligations.py`, `PASS`;
  `tests/extension/scene-obligation-candidates.test.mjs`); the claimed Persuade is the first roll of the replay's kernel
  process and the investigator's Persuade is 70, so a selected, claimed check is predicted to pass.
- Scored as registered: line 1 (the approach bound without a model call where the check is selected), line 2 (4 LLM steps
  on each passing run), lines 3–4. Whether the fact question selects the check is Jev's, run by run (0.49–0.65 `seeks`
  in the six runs so far); an unselected run is reported, not scored.

## SL-12 scored runs (the four registered arms on `846d39439`, the seed-4 arm on `97227859d`; product code `42e6f1808`)

Traces in `results/sl12-obligations-seed1/`, `results/sl12-obligations-seed19/`, `results/sl12-obligations-seed4/`,
`results/sl12-turn3-regression/`, `results/sl12-fight-round/`. "LLM step" is one `step_start kind: infer`; "clerk" is
`origin: "policy"`; the approach's path is read from the `event: "bind"` row and the resolve's tool row (`basis.binding`).

| arm | run | wall | LLM steps (purposes) | Jev calls | arrival `seeks` (p vs `not`) | selected | Jev on the approach | approach bound | claimed roll | live rows |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| obligations seed 1 | 1 | 16.5 s | 5 (adjudicate ×4, compose) | 23 | 0.49 vs 0.36 | no (below both gates) | — | — | none: the Keeper's own Persuade | 11/11 |
| obligations seed 1 | 2 | 12.5 s | 5 (adjudicate ×5) | 12 | 0.56 vs 0.34 | no | — | — | none | 11/11 |
| obligations seed 1 | 3 | 11.1 s | 5 (adjudicate ×5) | 12 | 0.52 vs 0.36 | no | — | — | none | 11/11 |
| obligations seed 19 | 1 | 16.7 s | 4 (adjudicate ×4) | 24 | 0.58 vs 0.30 | yes (margin) | `unknown` 0.76 | **Persuade, rule-default, no model call** | failure: open | 11/11 |
| obligations seed 19 | 2 | 9.6 s | 4 (adjudicate ×4) | 10 | 0.65 vs 0.29 | yes | `unknown` 0.72 | Persuade, rule-default | failure: open | 11/11 |
| obligations seed 19 | 3 | 9.4 s | 4 (adjudicate ×4) | 10 | 0.65 vs 0.29 | yes | `unknown` 0.69 | Persuade, rule-default | failure: open | 11/11 |
| obligations seed 4 | 1 | 12.6 s | 5 (adjudicate ×5) | 19 | 0.59 vs 0.34 | no (ratio 1.74 < 1.8) | — | — | none | 11/11 |
| obligations seed 4 | 2 | 9.0 s | **4** (adjudicate ×4) | 10 | 0.67 vs 0.28 | yes | `unknown` 0.80 | Persuade, rule-default | **hard: settled** | 11/11 |
| obligations seed 4 | 3 | 9.0 s | **4** (adjudicate ×3, compose) | 10 | 0.66 vs 0.28 | yes | `unknown` 0.74 | Persuade, rule-default | **hard: settled** | 11/11 |

In every selected run Jev answered the dice `none` (0.81–0.85) and the intent `social` (0.84–0.89) above the gate, so the
`event: "bind"` row reads `skill rule-default`, `bonus`/`penalty`/`intent` `jev`, `goal`/`method` `composed`,
`obligation`/`target`/`actor` `stated`; the meeting's row reads `who`/`name` `stated`, `why` `composed`.

| regression | run | wall | LLM steps (purposes; reasons) | clerk | live rows |
| --- | --- | --- | --- | --- | --- |
| turn3 (seed 1) | 1–3 | 14.1 / 8.4 / 7.7 s | 5 / 5 / 5 (adjudicate and compose; no bind) | the move | 11/11 in 3/3 |
| fight-round | 1–3 | 6.7 / 4.9 / 4.9 s | 2 / 2 / 2 (adjudicate `clerk_unbound`, adjudicate `ask_llm`) | the player's attack, Knott's standing defence | 5/5 in 3/3 |

Against the registration:

1. **The approach bound without a model call: met wherever the check was selected -- 5/5** (seed 19 3/3, seed 4 2/2), all
   by the rules default (Jev `unknown` 0.69–0.80, as in SO-04), `basis.binding: "rule-default"` on the tool row, no `infer`
   step with purpose `bind` in any of the 15 runs. **"3/3 per arm" is not met as a count of runs:** the gate's fact
   question selected the check in 5/9 obligations runs (seed 1 0/3, seed 19 3/3, seed 4 2/3), against SO-04's 5/6. The
   candidates offered at arrival are the same in all nine runs (the read's located material is Jev's and may differ), so
   the spread is Jev's run-to-run variance around the margin gate (`seeks` 0.49–0.67 against `not` 0.28–0.36; the margin rule wants 1.8×); SL-12 does not touch that question.
   One change does reach it: the route at arrival no longer offers Ruth's unlabelled roster candidate (SO-04's route had
   it, `later`); whether its absence moves `seeks` cannot be told from nine runs whose values overlap SO-04's 0.57–0.69.
2. **Passing roll: 4 LLM steps (from 5) -- met, 2/2 passing runs** (seed 4 runs 2–3; the registered seeds gave none: seed 1
   was never selected and seed 19's claimed roll failed without the recorded bonus die). The failing runs (seed 19) also
   took 4: the bind step is what went, not anything that depends on the roll.
3. **Live rows 11/11 in 9/9** obligations runs; in the five selected runs the recorded Persuade is matched by the clerk's
   claimed one.
4. **Every route distribution retained; one `event: "bind"` row per clerk write** (3 in a selected run: move, meeting,
   claimed check; 1 in an unselected run: the move; 3 in the fight round: attack, defence, and the disposition's
   `outcome: "keeper"`).
5. **turn3 regression: met** -- 11/11, 5 LLM steps with no bind, the move clerk-origin; the morgue route no longer offers
   Arty's or Ruth's roster candidate, as predicted.
6. **fight-round regression: met** -- 5/5 rows, 2 LLM steps; the first is now `adjudicate` with reason `clerk_unbound`
   (Knott's disposition inference below the gate, `outcome: "keeper"`, unresolved `disposition`) where it was `bind`, and
   the replayed Keeper answers it with the same recorded message.

What the runs show, and what they do not:

- On the product path the clerk's closed binding no longer costs a model call: where the approach was Jev's `unknown` the
  rules default bound it, the kernel rolled it with the claim and settled the gate on a pass, and the turn took 4 model
  steps. None of the 15 runs had an `infer(bind)`.
- The rules default rolls without the live Keeper's recorded bonus die (Jev said `none`), so the same seed can fail where
  SO-04's Keeper-filled roll passed (seed 19). That is the ruling's trade: a default, stamped, that the Keeper may settle
  otherwise with its own operation.
- Not shown: a live Keeper reading `binding` and `left_to_you`; whether the gate's fact question can be made reliable
  (the next lever for the obligation's saving, not SL-12's).

# SL-13 — typed-feature routing (pre-registration written 2026-09-24, before the scored runs)

Everything above this line is unchanged. This section belongs to SL-13
(`docs/specs/pi-native-single-loop-tickets/13-typed-feature-routing.md`; contract §135.30). Its scored results are appended
below the pre-registration after the runs, on the commit that carries this paragraph.

## What changed since SL-12's runs

- Product (`a34455cbf`, on the contract commits `d4b09f5e4` and `fe54e7432`): after
  the run's first read and before its first route question, one Jev `decide(compile)` (family `single-loop-compile`) reads
  the player's declaration into closed features whose options are the read's own rows (`destination`: the kernel's move
  rows; `addressee`: the people present by the table's label; `ask`: the open obligations' demands, the unguarded clues,
  the unshown handouts; `act`: the resolve intents, or the session's issued actions in a fight; `target`: the fight's
  attack targets; `item`: what an investigator carries), each with `none` and `unclear`. Predicates in the policy select:
  the move whose `to` is the cleared destination; the obligation check whose demand is the cleared ask (not when the
  addressee cleared on someone else or `none`, nor when the act cleared outside the check's intents); a meeting-only
  obligation's person by the addressee or the ask; the investigator's attack by act and target. Only a cleared answer
  (the route's gates, unchanged) fires; a candidate the compile decided and did not select is consumed for the run; the
  rest fall through to the route question. A selecting compile replaces the first route question.
- Instrument: `experiments/single-loop-routing/gate-fixture.mjs` builds three fixtures from the live gate #3 campaign
  (`chatrpgv4-wt-integ-sl/.coc/campaigns/gate3-haunting-2329`, read only; copied into a scratch stage, set to the last
  recorded commit `1e321ff`, untracked lane files removed from the copy, tarred once as `fixtures/gate3/workspace.tar.gz`):
  `gate3-t1` (before `c65d6a2` → after `380440f`), `gate3-t2` (`380440f` → `cecf84b`), `gate3-t3` (`cecf84b` → `1e321ff`).
  Each `baseline.json` is the recorded Keeper of that turn from the play driver's RPC event log
  (`.coc/playtests/gate3-haunting-2329-20260924T032940Z/events.jsonl`): every assistant message's tool calls in order,
  whether each was taken, the turn's admission rows and receipts. The live clerk's move of turn 3 (policy origin, `t3-c1`)
  is prepended to that turn's first recorded message, marked `clerk_live`, so a run whose clerk misses it still moves (the
  replay drops a recorded call the run already carried out). `fixture.mjs` reads a shared tarball named in `turn.json`.
  `product-entry.ts` gains `--compile off` (the engine without the compile: the SL-12 policy) and records the compile row
  and every clerk selection by who made it (`compile` / `route`).

## Instrument-development runs (before this registration; not scored)

One run per gate turn on the control arm (`--compile off`, seed 1), to check the fixtures replay; the compile arm was not
run before this registration.

| fixture | selections | LLM steps | live rows | note |
| --- | --- | --- | --- | --- |
| gate3-t1 | route: the move (the live gate's same question sat at 0.61 vs 0.37 and did not) | 5 | move yes (policy); the office writes of the recorded first message NO | the recorded Keeper wrote the keys clue, handout, cash, the key definition and the time at the office before its move; after the clerk's move they are refused `not_here` |
| gate3-t2 | none (the fact question did not select the check) | 2 | 2/2 (the Keeper's Persuade claimed the obligation) | |
| gate3-t3 | route: the move | 6 | move yes (policy) | the first recorded resolve on Knott was refused live and is not a row (the fixture was rebuilt to drop refused calls from the rows after this run) |

## Pre-registered outcomes (5 runs per gate arm, 3 per regression; `--admission lane`, product driver, replayed Keeper, `--seed 1` unless named)

Arms: **gate3-t1**, **gate3-t2**, **gate3-t3** with the compile (the acceptance arms), and the same three with
`--compile off` (the control: the SL-12 policy on the same instrument, for the delta; not an acceptance line);
**turn3-obligations** `--seed 4` (the SL-12 passing-roll seed) and **fight-round** (the regressions). "Selected" means the
named candidate is in a `lane: "route"` row's `selected` (compile or route) of the run; "by the compile" means the compile
row's. "Jev decisions" are the run's `lane: "route"` rows (compile, route, bind, bind-ordinary); the prescreen's own Jev
calls are reported beside them.

1. **gate3-t1: `apply:move:newspaper-morgue` selected by the compile in ≥ 4/5** (acceptance). Predicted 5/5, destination
   cleared at ≥ 0.8. The compile replaces the first route question, so the run's Jev decisions equal the control's
   median or fewer. LLM steps: predicted 5 in each run, as the control's (the recorded Keeper's office writes are refused
   after the clerk's move and the rest of its messages replay); the live rows of the recorded first message stay NO for
   the same reason, reported, not scored.
2. **gate3-t2: `resolve:obligation:globe-clippings-access` selected by the compile in ≥ 4/5** (acceptance). Predicted 5/5:
   ask cleared on the demand, addressee on Arty (the table's label 戴绿眼罩的编辑) or not cleared, act `social`. Its approach
   then binds by SL-12 (Jev's `Persuade` or the rules default `Persuade`, no model call), the clerk rolls it with the claim
   (the seed-1 outcome reported), and the replayed Keeper's recorded Persuade is dropped as done. LLM steps: predicted 1
   (the recorded narrate) where the control has 2; live rows 2/2 (the recorded Persuade matched by the clerk's).
3. **gate3-t3: `apply:move:commission-briefing` selected by the compile in ≥ 4/5** (acceptance). Predicted 5/5. The
   obligation check at the morgue is not selected in 5/5 (the act clears as `combat` or the addressee as `none`: Knott is
   not present, or the ask is not the demand). After the move the fight is the Keeper's, as in the control: LLM steps
   predicted equal to the control's, live rows equal.
4. **The control arm** (not acceptance): the route question selects the gate turns' candidates at whatever rate Jev gives
   it on this instrument (the live gate: 0/1, 0/1, 1/1; the development runs: 1/1, 0/1, 1/1); reported beside the compile
   arm per turn.
5. **turn3-obligations, seed 4 (regression, 3 runs):** the move selected in 3/3 (now by the compile), 11/11 live rows, LLM
   steps 4 or 5 per run as in SL-12 (4 when the morgue's fact question selects the check, 5 when it does not; the compile
   does not reach the morgue's candidates: it is asked once, at the office). Unchanged or better.
6. **fight-round (regression, 3 runs):** 5/5 live rows, 2 LLM steps, as in SL-12. The player's attack on Knott selected
   by the compile (act `combat:attack`, target Knott) in ≥ 2/3; where it is not, the route selects it as before.
7. **Every compile row** carries each asked family's rows, choice, confidence, distribution and `cleared`, and the fired
   predicates; no run asks more than one compile.


## SL-13 scored runs (on `1098b9249`, the pre-registration commit; product code `a34455cbf`)

Traces in `results/sl13-gate3-t{1,2,3}/`, `results/sl13-gate3-t{1,2,3}-control/`, `results/sl13-obligations-seed4/`,
`results/sl13-fight-round/`. "Jev decisions" are the run's `lane: "route"` rows in order (c = compile, r = route, b = bind);
the prescreen's own Jev calls are the rest of the run's Jev total. "LLM step" is one `step_start kind: infer`.

### The gate #3 turns, compile arm (acceptance) and control arm (`--compile off`, the SL-12 policy)

| arm | runs | selected (by) | the deciding answer, per run | Jev decisions per run | LLM steps | live rows | wall (s) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| t1 compile | 5 | **5/5** `apply:move:newspaper-morgue` (compile) | destination 0.99 / 1.0 / 0.99 / 1.0 / 1.0 | c r r (3) | 5 ×5 | 5/11 ×5 | 9.9 6.9 8.2 8.8 6.8 |
| t1 control | 5 | 5/5 (route) | need `now` 0.82 / 0.81 / 0.79 / 0.80 / 0.79 | r r r (3) | 5 ×5 | 5/11 ×5 | 10.1 7.3 7.7 6.6 7.1 |
| t2 compile | 5 | **5/5** `resolve:obligation:globe-clippings-access` (compile) | ask 0.75 / 0.75 / 0.79 / 0.77 / 0.71; addressee Arty 0.62 / 0.44 / 0.61 / 0.60 / 0.31 (cleared in 3); act `social` 0.95–0.97 | c b r (3) | **1** ×5 | 2/2 ×5 | 10.4 7.9 8.9 8.4 4.8 |
| t2 control | 5 | 0/5 | fact `not` 0.15 / 0.20 / 0.22 / 0.30 / 0.17 (`seeks` 0.36–0.43) | r r (2) | 2 ×5 | 2/2 ×5 | 9.7 7.6 7.6 7.0 4.3 |
| t3 compile | 5 | **5/5** `apply:move:commission-briefing` (compile) | destination 0.99 ×5; the morgue's check decided by ask `none` 0.94–0.95 (not selected ×5) | c r r b r (r) (5–6) | 6 ×5 | 7/7 ×5 | 15.8 14.8 13.6 12.0 13.4 |
| t3 control | 5 | 5/5 (route) | need `now` 0.87–0.89 | r r r b r r (6) | 6 ×5 | 7/7 ×5 | 15.7 12.8 12.5 11.8 12.8 |

In t2 the selected check then bound by SL-12 with no model call: Jev `Persuade` 0.72–0.79, dice `none`, intent `social`
0.85–0.91 (every parameter path `jev`); the clerk's claimed Persuade (seed 1, no bonus die) failed in 5/5, the obligation
stayed open, and the replayed Keeper's recorded Persuade was dropped as done (its recorded narrate is the refusal).

### Regressions

| arm | runs | selections | Jev decisions per run | LLM steps | live rows | wall (s) | SL-12 (same arm) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| turn3-obligations seed 4 | 3 | move by the compile 3/3 (destination 1.0 ×3); the morgue's fact question selected the check 1/3 (run 3, passed) | c r r r r / c r r r r / c r b r r r | 5 / 5 / 4 | 11/11 ×3 | 18.2 15.0 9.1 | move by route 3/3, check 2/3; r×5 / r r b r r r ×2; 5 / 4 / 4; 11/11 ×3 |
| fight-round | 3 | the attack by the **route** 3/3 (the compile did not select it) | c r b r (4) | 2 ×3 | 5/5 ×3 | 8.0 6.4 6.6 | r b r (3); 2 ×3; 5/5 ×3 |

Against the registration:

1. **t1: met, 5/5** by the compile (destination 0.99–1.0). The Jev decisions equal the control's (3). LLM steps 5, as
   predicted; the office writes of the recorded first message are refused `not_here` after the clerk's move in both arms.
   **Not shown by these runs:** the live gate's miss. On this instrument the control's route also selects the move 5/5
   (need `now` 0.79–0.82, where the live table's question sat at 0.61 vs 0.37), so turn 1's replay does not reproduce the
   live failure; what the compile adds here is the margin (0.99–1.0 against 0.79–0.82), not the outcome.
2. **t2: met, 5/5** by the compile, where the control selects 0/5 (the live gate's failure reproduces: `not` over `seeks`).
   LLM steps 1 (from 2), as predicted; live rows 2/2. The addressee cleared in only 3/5 (0.31–0.62): the ask alone fired the
   predicate in the other two, as §135.30 allows. Jev decisions 3 against the control's 2: the one more is SL-12's bind of
   the check the compile selected (the budget line "plus SL-12's binds").
3. **t3: met, 5/5** by the compile (destination 0.99). The morgue's obligation check was not selected in 5/5, as
   predicted, but by a different feature than the two named: the ask cleared as `none` (0.94–0.95); the act `combat` never
   cleared (0.45–0.54) and the addressee cleared as `none` (0.99). LLM steps and live rows equal the control's; Jev
   decisions 5–6 against the control's 6. Here too the control selects the move 5/5 (0.87–0.89).
4. **Control arm:** 5/5, 0/5, 5/5 on this instrument (the live gate: 0/1, 0/1, 1/1).
5. **turn3-obligations: met.** Move 3/3 (now by the compile), 11/11 live rows, LLM steps 5 / 5 / 4 within the registered
   4 or 5 (SL-12: 5 / 4 / 4; the difference is the morgue's fact question, 1/3 here and 2/3 there, which the compile does
   not reach). Jev decisions equal SL-12's run for run in shape (the compile in place of the first route).
6. **fight-round: live rows and LLM steps met (5/5, 2); the attack by the compile: missed, 0/3.** The act cleared
   `combat:attack` at 0.99 in 3/3, but the target ("继续揍他": the only fighter the attack row issues) came back `unclear`
   0.24 / `steven-knott` 0.25 / `steven-knott` 0.28 (e.g. `unclear` 0.49 against `target_1` 0.45), below both gates, so the
   attack fell through and the route selected it as before. The run therefore spent one Jev decision more than SL-12's
   (4 against 3).
7. **Met:** one compile row per run in all 21 runs with the compile (15 gate, 3 obligations, 3 fight), with each asked family's rows, choice,
   confidence, distribution and `cleared`, and the fired predicates.

What the runs show, and what they do not:

- The typed question settles what the need question could not on the one turn whose failure the replay reproduces
  (t2: 0/5 → 5/5, one model step fewer), and it settles the two moves with a wider margin (0.99–1.0) than the need question
  gives on this instrument (0.79–0.89).
- The turn-1 live miss (need 0.61 vs 0.37) does not reproduce on the replay, so these runs cannot say whether the compile
  would have fixed that exact table; a live gate can.
- A pronoun addressed at the one fighter the kernel issues is not a cleared target (fight-round). Whether the attack
  predicate should fire on the act alone when the kernel issues a single target is the owner's call (it would make the
  predicate read "the attack whose target the kernel already bound"); the predicate was not changed after the
  registration.


# SL-13 follow-up (sl13b) — the compile after a later read; the route's seeks selects nothing; the read's material (pre-registration written 2026-09-24, before the scored runs)

Everything above this line is unchanged. Product code: `5d17a2087` (contract `b9a1c71fa`, §135.30.1) and `d22d3738a`
(contract `143d3a178`, §135.6's read reason). Instrument: `e715c2cdf` and the commit that carries this paragraph.

## What changed since SL-13's runs

- **The compile is due whenever an offered candidate a predicate can select is outside the keys the run's compiles
  were asked over** (was: once, before the run's first route). Live gate #4's turn 1 had no compile: its first read issued
  no move (the research exits waited on `knott-research-leads`), and the exits the Keeper's clue unlocked reached only the
  route.
- **An obligation check or a stated meeting is selected only by the compile** (owner ruling 2026-09-24); the route's
  `seeks` answer is recorded, selects nothing, and the candidate is consumed.
- **Fixture `gate4-t1`** (live gate #4, `gate4-haunting-0214`, turn 1, `adca154` → `28b1a51`; the sidecar repository has
  no `turn 1:` commit, turn 1 landed inside turn 2's): the live clerk's move now heads the first recorded Keeper message
  sent *after* it (message 1 here; gate #3's turn-3 move stays at message 0), so the replayed Keeper does not move before
  the clue that unlocked the exit. The gate #3 fixtures rebuilt with the same script are content-identical to the
  committed ones (baselines differ only in the note's wording; the tarballs only in the stage repository's index and
  reflog), so the committed `gate3-t1..t3` are used.
- **The prescreen arm** (`--prescreen off` = `PI_COC_JEV_PRESELECT=0`). Every read of live gate #4 said
  `prescreen: {status: "not_run"}`: the source-mode driver was launched without the Jev preselect setting, which the
  replays (and the owner's App, SL-00 inventory) have on. So the replays so far measured a table with 10 prescreen
  materials in front of Jev, and the live gate one with none. Both arms are run.

## Pre-registered outcomes (5 runs per arm, `--llm replay --admission lane --seed 1`; arms: prescreen `on` and `off`)

1. **gate4-t1, both arms.** The first read issues no move; no compile before the first route (the route asks, exit
   `ask_llm` as live); the replayed Keeper's message 0 (the commission's clues, handout, cash) unlocks the exits; the next
   step is a compile, which selects `apply:move:newspaper-morgue` in ≥ 4/5 per arm (destination ≥ 0.9). After the move the
   morgue's read issues the gate's check and the way back, so a second compile is asked; the obligation check is **not
   selected** in ≥ 4/5 per arm (the declaration addresses nobody: addressee `none` blocks it, or the ask does not clear),
   and the clerk never has a `resolve` refused by admission. LLM steps ≤ 3 (message 0, message 1, the delivery; live: 4,
   one of them the adjudication after the clerk's refused check). Jev decisions r c c r (4) plus the binds, if any.
2. **gate3-t1, both arms.** The move by the first compile in ≥ 4/5 (SL-13: 5/5 at 0.99–1.0). A second compile at the morgue
   (new: the gate's check, the way back); the check not selected in ≥ 4/5. LLM steps 5, as SL-13.
3. **gate3-t2, prescreen on.** The check selected by the compile's ask in ≥ 4/5 (SL-13: 5/5, ask 0.71–0.79); LLM steps 1.
   **Prescreen off:** the ask on the obligation does not clear in ≥ 4/5 (the measurement worker's 0.354 with no material,
   0/10 cleared), and now nothing else selects it: the check is selected in ≤ 1/5, and the Keeper's recorded Persuade
   runs (LLM steps 2).
4. **gate3-t3, both arms.** The move to the office by the compile in ≥ 4/5; the morgue's check never selected. After the
   move, a second compile over the office's new candidates (the way back to the morgue) selects nothing. LLM steps 6 and
   live rows 7/7, as SL-13.
5. Every compile row carries its features; every read row its prescreen status and, when it did not run, the reason.

## sl13b scored runs (on `e0d421251`; traces in `results/sl13b-<fixture>-prescreen-{on,off}/`)

Jev decisions: the run's `lane: "route"` rows in order (c compile, r route, b bind); "Jev" is the run's total with the
prescreen's own calls. Reads: the prescreen status and material count of each read.

| arm | selected (by) | deciding answers | Jev decisions | Jev total | LLM steps | reads (materials) | wall (s) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| gate4-t1 on | move 5/5 (compile #1); check 0/5 | destination 1.0 ×5; morgue compile: ask **obligation 0.85–0.93 (cleared)**, addressee `none` 0.69–0.78 (cleared: the guard blocks) | r c c r r | 9–20 | 4 ×5 | office 5, morgue 5–7 | 6.6–12.0 |
| gate4-t1 off | move 5/5 (compile #1); check 0/5 | destination 0.98–0.99; morgue: ask obligation 0.95–0.97, addressee `none` 0.72–0.81 | r c c r r | 5 | 4 ×5 | 0, 0 (`not_run`) | 4.0–5.1 |
| gate3-t1 on | move 5/5 (compile #1); check 0/5 | destination 0.99–1.0; morgue: ask obligation 0.60–0.74, addressee `none` 0.60–0.72 | c c r r | 9–19 | 5 ×5 | 5, 7 | 6.7–9.9 |
| gate3-t1 off | move 5/5; check 0/5 | destination 0.74–0.84; morgue: ask obligation 0.79–0.85, addressee `none` 0.61–0.72 | c c r r | 4 | 5 ×5 | 0, 0 | 3.8–4.5 |
| gate3-t2 on | **check 5/5 (compile)** | ask obligation 0.82–0.87 (reported 0.77–0.83); addressee Arty 0.47–0.59 (cleared 3/5); act social 0.95–0.97 | c b r | 10–15 | **1** ×5 | 9–10 | 7.5–10.3 |
| gate3-t2 off | **check 0/5** | ask: `none` 0.35–0.39, obligation 0.26–0.31, clue 0.18–0.22 (never cleared); route fact question `not` 0.15–0.18 / `seeks` 0.20–0.24 (recorded, selects nothing) | c r r | 3 | 2 ×5 | 0 | 2.6–3.7 |
| gate3-t3 on | move 5/5 (compile #1); check 0/5 | destination 0.99; ask `none` 0.92–0.95; office compile: destination `none` 0.98, addressee Knott 1.0 | c c r r (b r r) | 17–24 | 6 ×5 | 8, 6–7 | 13.4–15.6 |
| gate3-t3 off | move 5/5; check 0/5 | destination 0.98; ask `none` 0.88–0.91 | c c r r b r r | 7 | 6 ×5 | 0, 0 | 6.0–7.0 |

Live rows (the baseline's actions matched by the replay's taken calls): gate4-t1 9/10 in every run (the miss is the
recorded `cash` effect, whose target is null and the instrument's matcher compares targets; an instrument artefact),
gate3-t1 5/11 (as SL-13: the recorded office writes are refused `not_here` after the clerk's move), gate3-t2 2/2, gate3-t3
7/7. No run had a clerk `resolve` refused by admission.

Against the registration:

1. **gate4-t1: the compile runs and selects the move, 10/10** (both arms; the live turn had no compile at all). The morgue's
   check **was not selected, 10/10**, but not for the registered reasons in full: the ask cleared on the obligation in
   every run (0.85–0.97: "翻科比特宅这些年的旧报道" is what the gate guards), and the addressee's cleared `none` is the only
   thing that blocked it. LLM steps: **4, not ≤ 3 (missed)**: the recorded messages 0 and 1, then the replayed Keeper has
   no more tool messages and answers the next adjudication with the delivered text, and the turn close steers one compose
   -- the live table's 4 calls had the same shape (messages 2 and 3 were prose). The live table's refused clerk resolve
   (3.6 s) and the adjudication it bought are gone from the path; the replay cannot say what the live Keeper would have
   written instead. Jev decisions r c c r r (one route more than registered: after the Keeper's second message).
2. **gate3-t1: met** (move 10/10, check 0/10, LLM 5). The ask cleared on the obligation at the morgue here too (0.60–0.85);
   the addressee guard blocked it.
3. **gate3-t2: met in both arms.** With the prescreen, 5/5 by the compile, LLM steps 1. Without it, 0/5, and the ask's
   distribution is the live gate #4 turn 2's (live: obligation 0.35 / none 0.36 / clue 0.17): the material, not the row
   words, is what moved the ask from ~0.3 to ~0.85.
4. **gate3-t3: met** (move 10/10, check never selected, LLM 6, live rows 7/7).
5. **Met:** every compile row carries its features; every read row its prescreen status (`not_run` rows carry
   `reason`).

What this shows and does not show:

- The fix for the missing compile holds on the one live turn that lacked it, in both material arms.
- The owner's ruling holds: the route's `seeks` never selected anything, and no clerk resolve was refused. But for a
  declaration that names what the gate guards without addressing its keeper (gate #4 turn 1, gate #3 turn 1), the
  compile's ask clears on the obligation in 20/20 runs; the addressee guard (`none` cleared at 0.60–0.81) is the only
  thing between that and the same admission refusal. An addressee answering `unclear` or below the gate would let the
  check through.
- The material decides turn 2: 5/5 with it, 0/5 without. Live gate #4 ran every read without it
  (`PI_COC_JEV_PRESELECT` not set in its source-mode launch; the owner's App has the setting on).


# SL-19 — the disposition's rules default from the card; the first blow selected by the compile (pre-registration written 2026-09-24, before the scored runs)

Everything above this line is unchanged. Contract: §11.5.3's amendment (2026-09-24) and §135.30.2. Product code and this
paragraph: the SL-19 commits on `claude/sl19-20260924` (base `b8dd818bd`).

## What changed

- **The card's word as the disposition's rules default.** A person without a disposition whose card states a tactic -- the
  book's (`combat.defense`, basis `authored`) or the Keeper's (`apply npc defense`, basis `keeper`) -- carries
  `combat_disposition.default` through the disposition table's new `tactic_dispositions` (`fight_back` → `fights_then_flees`,
  `dodge` → `avoids_fighting`). The clerk's disposition bind takes it when Jev does not clear (`rule: card_disposition`).
  A rule-default tactic (Fighting against Dodge) is not the card's word.
- **Knott says nothing.** `content/starters/the-haunting` prints no `combat` and no `mechanics.profile` for him; at gate #6
  before turn 3 his card is `mechanics: null`, tactic `{null, rule-default}`; after an archetype pin, `{dodge,
  rule-default}`. So on the recorded fixtures his default is absent.
- **The first blow.** `table.resolve.options.context.first_blow` (outside a fight, one investigator: the people present the
  engine can fight, the investigator's weapons) becomes the clerk candidate `resolve:combat:first-blow` (authority
  `first_blow`), selected only by the compile's `first_blow` predicate (act `combat` + a cleared target among the row's);
  outside a fight the `target` rows are the addressee rows when the row exists. Knott at gates #5/#6 had no stat block
  before turn 3, so the row names nobody there.
- **Instrument.** `world-variant.mjs` (a fixture whose only difference is world state committed on top of
  `commit_before`); the replay's `resolveKey` reads a recorded `intent: combat` attack with a target and no decision as
  `combat:attack`, so the replayed Keeper does not throw the clerk's first blow a second time.
- **Fixtures.** `gate6-t3` (live gate #6, `gate6-haunting-0335`, turn 3, `c0ae9fd` → `8cd89ca`, "我回诺特办公室，揪住他的领子一拳打过去…",
  built with `gate-fixture.mjs`); `gate6-t3-card` (its variant: the live turn's own `ordinary_adult` pin for Knott, made
  `pinned_turn: 2`, and a Keeper tactic `{defense: dodge}` written before the turn); `fight-round-card` (`fight-round` with
  the same Keeper tactic before turn 6).

## Pre-registered outcomes (3 runs per fixture, `--llm replay --admission lane --seed 1`, prescreen on, live Jev)

"Keeper step for the disposition" = a `step_end kind: infer` with `reason: clerk_unbound` for `apply:npc-disposition:*`.

1. **fight-round (as recorded; control).** Nothing SL-19 adds is reachable: in a fight there is no first-blow row, and
   Knott's tactic is rule-default, so no `default`. The disposition is Jev's or the Keeper's exactly as in SL-13: a Keeper
   step for it in every run where Jev stays below the gate (SL-08..SL-13: always). Live rows 5/5, LLM steps 2 (SL-13).
2. **fight-round-card.** The disposition bound **without a Keeper step in 3/3**: Jev's word when it clears (path `jev`),
   else the card's default `avoids_fighting` (path `rule-default`, `rule: card_disposition`, the write's `read:
   ["combat_tactic"]`). Knott's standing from `avoids_fighting` is `hold` or `flee`, never `attack`, so his turn is the
   Keeper's (the replayed Keeper sends its recorded attack for him). LLM steps ≤ 2.
3. **gate6-t3 (as recorded; control).** No first-blow row at the office (Knott has no numbers): the first blow selected by
   the compile in **0/3, by construction**; the Keeper pins the archetype and attacks as live. If a later read (after the
   Keeper's pin) owes a compile that selects the first blow before the Keeper's recorded attack, it is reported. The
   disposition: no card word (rule-default tactic after the pin), so a Keeper step for it unless Jev clears (live 0.40).
4. **gate6-t3-card.** After the clerk's move the office read issues the first blow (target Knott; weapons `.38 Revolver`,
   `unarmed`) and a compile is owed. **The first blow selected by the compile in ≥ 2/3** (act `combat` and target Knott
   cleared); a run whose act stays below the gate (gate #6 read 0.45, gate #5 0.90) is a miss reported with its
   distribution, not repaired here. When selected: the weapon bound by Jev (`unarmed` expected), the clerk's resolve opens
   the fight, Knott's standing defence is the clerk's direct step, and on his turn **the disposition is bound without a
   Keeper step in 3/3** (Jev or the card's `avoids_fighting`). LLM steps below the live turn's 7 when the first blow is
   selected.
5. Every compile row at the office of `gate6-t3-card` carries `target` with the addressee's rows; `fight-round*` compile
   rows are as SL-13's (in-session target rows).

## SL-19 scored runs (on `4360b100b`, the pre-registration commit; product code `9377a7ac6`)

Traces in `results/sl19-{fight-round,fight-round-card,gate6-t3,gate6-t3-card}/`. "Keeper step for the disposition" is a
`step_end kind: infer, reason: clerk_unbound` for `apply:npc-disposition:*`; "LLM steps" are `step_start kind: infer`.

| fixture | disposition (Jev answer; how bound) | Keeper step for it | first blow by the compile | office compile: act / target | LLM steps | wall (s) |
| --- | --- | --- | --- | --- | --- | --- |
| fight-round (control) | fights_then_flees 0.33 / 0.26 / 0.28 → Keeper (low_confidence) | **1 ×3** | n/a (in a fight) | in-session: `combat:attack` 0.98–0.99, target `steven-knott` 0.23–0.27 / unclear (not cleared) | 2 ×3 | 7.3 5.6 5.5 |
| fight-round-card | fights_then_flees 0.29 / 0.28 / 0.35 → **rules default `avoids_fighting` (card_disposition)**, clerk write | **0 ×3** | n/a | as the control | 2 ×3 | 7.2 6.0 6.2 |
| gate6-t3 (control) | offline (Jev budget) / unknown 0.26 / unknown 0.27 → Keeper | **1 ×3** | office: 0/3 (no row: Knott has no numbers); **after the Keeper's archetype pin a later compile selected it in 1/3** (run 3: act combat 0.85, target Knott 0.99, weapon `unarmed` 0.96; the clerk's resolve opened the fight) | combat 0.64 C / 0.56 / 0.88 C; no target rows | 5 / 5 / 4 | 14.6 12.2 11.8 |
| gate6-t3-card | run 1 offline (budget), 0.29, 0.33 → **rules default `avoids_fighting`** | **0 ×3** | **0/3** | act combat 0.46 / social 0.50 / social 0.44 (never cleared); **target Knott 0.98 / 0.98 / 0.99 (cleared)** | 5 ×3 | 14.0 13.1 14.1 |

Against the registration:

1. **fight-round: met.** Nothing SL-19 adds reached it; the disposition went to the Keeper 3/3 as before (SL-13: 2 LLM steps).
2. **fight-round-card: met, 3/3.** Jev never cleared (0.28–0.35, top `fights_then_flees` 0.42–0.48); the card's default was
   written by the clerk (bind row path `rule-default`, `rule: card_disposition`), with no Keeper step. LLM steps stay 2:
   his standing from `avoids_fighting` is not an attack, so his turn is the Keeper's (the replay sends his recorded
   attack), then the compose.
3. **gate6-t3: met, with the reported extra.** No first-blow row at the office 3/3. In run 3 the Keeper's archetype pin made
   Knott fightable, the next read issued the first blow, the owed compile read act `combat` 0.85 and target Knott 0.99,
   and the clerk threw the punch (one LLM step fewer). The disposition went to the Keeper 3/3 (his card says nothing).
4. **gate6-t3-card: disposition met (3/3 without a Keeper step); first blow missed (0/3, registered ≥ 2/3).** The target
   rows worked (Knott cleared 0.98–0.99 every run); the act did not: `combat` 0.46 and `social` 0.50 / 0.44 for "揪住他的领子一拳
   打过去，逼他说清这房子到底死过什么人" -- the same reading as live gate #6 (0.45), against gate #5's 0.90. With the act below the gate
   the first blow falls through and is compile-only, so the Keeper threw the punch as recorded. Not repaired in SL-19: whether
   a cleared target at a fightable person with the act split between `combat` and `social` should select the first blow is
   the owner's to rule (it would read "act not cleared on another act" instead of "act cleared on combat").
5. **Met:** every office compile of `gate6-t3-card` carried `target` with the addressee's row (Knott); `fight-round*`
   compile rows are in-session, as SL-13's.
