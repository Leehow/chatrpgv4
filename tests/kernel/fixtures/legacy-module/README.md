# Frozen legacy module fixture

This synthetic graph was compiled by the pre-retirement text-bundle pipeline at commit 8601d66b from tests/kernel/fixtures/bundle-tiny, using module_helpers.build_whole_book and module.install. It is a compatibility fixture, not real PDF or gameplay acceptance evidence.

The graph, metadata, source references and asset paths are copied unchanged. Tests load the published files directly to ensure old books remain usable without restoring the retired builder. The producing run is retained locally under .coc/research/legacy-fixture.
