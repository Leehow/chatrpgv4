{
  "name": "coc-keeper-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Electron desktop shell for the pi-coc COC Keeper web UI (pi-coc track).",
  "author": "chatrpg",
  "main": "electron/main.mjs",
  "scripts": {
    "start": "electron .",
    "build:wizard": "npm --prefix wizard install && npm --prefix wizard run build",
    "build:payload": "node scripts/payload.mjs",
    "dist": "npm run build:wizard && node scripts/payload.mjs && electron-builder --mac"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  }
}
