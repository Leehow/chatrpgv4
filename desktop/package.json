{
  "name": "coc-keeper-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Electron desktop shell for the pi-coc COC Keeper web UI (pi-coc track).",
  "author": "chatrpg",
  "main": "electron/main.mjs",
  "scripts": {
    "start": "electron .",
    "build:web": "npm --prefix ../web/frontend install && npm --prefix ../web/frontend run build",
    "build:payload": "node scripts/payload.mjs",
    "dist": "npm run build:web && node scripts/payload.mjs && electron-builder --mac"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  }
}
